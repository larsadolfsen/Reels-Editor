# Backlog

Running list of things to do, picked one at a time. Add items as they come up (in any thread); check them off here once shipped and merged.

## To do

**The original Phase 1–6 roadmap is complete** (2026-07-20). The items below are the next stretch, brainstormed in full on 2026-07-20 so build sessions go straight to writing an implementation plan — **each item's design doc has zero open questions**. The list is deliberately unordered: the build session picks whichever item it takes on (process rules from [2026-07-17-major-plan-revision-design.md](specs/2026-07-17-major-plan-revision-design.md) still apply — plan with small parallel-worktree tasks, end with a visual checkpoint).

- [ ] **Audio: volume, music, real waveforms** — [design](specs/2026-07-20-audio-volume-music-design.md): `ClipLayer.volume`/`muted`, `MediaItem.kind`, new `MusicTrack` entity (`Project.music`), volume filters + `amix` in export, synced `<audio>` in preview, `GET /api/media/{id}/peaks` real waveform in the AUDIO row, new AUDIO context-panel section — **in progress on `claude/audio-subsystem-032b53`**
## Done

- [x] **Insert model** — [design](specs/2026-07-20-insert-model-design.md) · [plan](.superpowers/sdd/task-3-brief.md): left-rail TEXT/CAPTIONS buttons now insert items directly (TEXT calls the existing `addTextBlockAndEdit()` to add a block + select it + open the TEXT panel + enter on-stage edit; CAPTIONS already created-or-opened the track correctly and needed no change), with a small plus badge (`UI.iconRail`'s new `badge` option, `icon-rail.css`) marking both as insert-capable; the right-panel nav machinery (`showPanel`/`onTimelineSelect`/`PANEL_NAV_ITEMS`/`openXPanel`/`reRenderAfterRestore`/`PANEL_NAV_HANDLERS`) was extracted verbatim from `static/editor.js` into a new `static/panel-nav.js` first as a pure-move prerequisite. Timeline clicks opening the right panel and the VIDEO/TEXT `+` row buttons were already correct pre-existing behavior, confirmed unchanged. Live-verified end-to-end on a throwaway project (created + deleted via the API): TEXT rail click inserts a new block each click (idempotent, 0→1→2); CAPTIONS rail click creates the track once then just re-opens it on repeat clicks; timeline-block clicks open the matching panel for VIDEO/TEXT/CAPTIONS rows (synthetic clip/word data injected directly on `project` since native file-picker import can't be automated); the TEXT row's `+` button produces the same insert as the rail button; undo (`Ctrl+Z`/`undoEdit()`) after a rail insert removes the just-added block and correctly re-opens the TEXT panel via `reRenderAfterRestore` in its new home, and redo (`Ctrl+Y`/`redoEdit()`) restores it. `pytest -q` green (232 passed).

- [x] **Right panel tabs** — [design](specs/2026-07-20-right-panel-tabs-design.md) · [plan](plans/2026-07-21-right-panel-tabs.md): replaced the right-panel accordions with a four-icon tab bar (`UI.tabBar`) on TEXT/CAPTIONS/VIDEO/VIDEO BOX; caption HIGHLIGHT moved into Design, a Closed-caption tab lists caption words inline. Shipped, browser-verified, 224 pytest green, whole-branch reviewed clean (merged `233958f`).
- [x] **Export progress** — [design](specs/2026-07-20-export-progress-design.md): shipped in a prior session — export is a background job (`app/export_jobs.py`), ffmpeg `-progress` parsed to a percent via `app/media.py`'s `run_export`/`percent_from_progress_line`, `GET /api/exports/{job_id}` polled by `static/export-progress.js`'s progress bar in the EXPORT panel. (Checked off here 2026-07-21 — the code and CLAUDE.md map already reflect it as complete.)
- [x] **Image/photo clips** — [design](specs/2026-07-20-image-clips-design.md): shipped in a prior session — stills as clips (`MediaItem.kind="image"`, `-loop 1` export in `app/ffmpeg_cmd.py`, `<img>` preview driven by `static/image-clip-playback.js`'s virtual clock). (Checked off here 2026-07-21 — the code and CLAUDE.md map already reflect it as complete.)

- [x] **Slice + Timeline Editing (zoom, drag-reorder, slice-disabled-state — increment 2 of 2)** — [design](specs/2026-07-20-slice-timeline-editing-design.md) · [plan](plans/2026-07-21-slice-timeline-editing-zoom-reorder.md): finishes the item, on top of increment 1's slice feature (below). **Zoom**: `static/timeline.js`'s fixed `PX_PER_SEC = 60` constant became a `pxPerSecond` module state — auto fit-to-width by default (`fitToWidthPx`, computed fresh from `#timeline-scroll`'s clientWidth), stepping ×1.5 per `#zoom-in` click up to a 200px/s cap, `#zoom-out` snapping back to fit-to-width at the floor; every position-math helper (`render`, `tick`, `timeAtX`, `renderRuler`) now reads the resolved `px` value, and `Timeline.PX_PER_SEC` (read elsewhere by `editor.js`'s ruler-click/playhead-drag snap logic) became a live getter so those call sites stay correct without themselves changing. Zoom is view state only — not persisted, not in undo history, reset to fit-to-width via `Timeline.resetZoom()` on every project open. **Auto-scroll**: `tick()` (the playback RAF loop) nudges `#timeline-scroll`'s `scrollLeft` to keep the playhead within a 40px margin of either visible edge; only wired to `tick`, so it never fights manual scrubbing/scrolling. **Drag-to-reorder**: a new `static/timeline-clip-drag.js` (loaded after `timeline.js`, per the established convention — see increment 1's own load-order bug below) delegates a `mousedown` listener on the persistent `#row-video` container; a drag past a 4px threshold follows the pointer via CSS `transform`, snaps a drop indicator to the nearest clip boundary, and calls a new shared `VideoPanel.moveClipTo(clipId, newIndex)` (`static/panel-video.js`, replacing the old two-clip `moveClip(a,b)` swap the VIDEO panel's move-up/down buttons used) — splice-remove/reinsert/renumber, so it generalizes to any target index, not just adjacent swaps. Below the threshold the block's existing click-to-select still fires normally. **Slice-disabled state**: a new pure `Timeline.isSliceDisabled(clips, t, eps)` in `static/timeline-slice.js` (reused by `sliceClip`'s own no-op guard, DRY) drives a `.disabled` CSS class toggle on `#slice-action` (the scissors icon only, never `#playhead-grip`) whenever the playhead sits outside every clip or within ε of a boundary — the click handler already no-op'd safely there from increment 1, this makes it visible. **Task review (sonnet) caught a real Important bug in the drag-reorder math**: `reorderBoundaries`/`nearestReorderIndex` computed the drop-boundary positions as if the dragged clip had already been removed and the rest of the sequence had reflowed to compact together — but on screen, sibling blocks never actually move during the drag (only the dragged block gets the CSS transform), so for any drag of a clip other than the sequence's *last* one, every boundary after the dragged clip's original slot was off by exactly that clip's own width, producing a visibly wrong drop-indicator position and wrong drop index. The bug was invisible in the plan's own worked example and the implementer's own hand-trace because both happened to only test dragging the last clip (the one case where removal doesn't shift anything). Fixed by snapping against the real, un-reflowed full-sequence boundaries and translating the matched boundary into the post-removal index separately; re-verified via an independent hand-trace of first/middle/last-clip drags across every boundary point, all correct. Live-verified end-to-end in-browser on a throwaway project (synthetic clips injected via direct API calls, since native file-picker import can't be automated): zoom in/out rescales all timeline rows in sync and floors/caps correctly (confirmed via `Timeline.PX_PER_SEC`/DOM measurements); `Timeline.tick()` auto-scrolls to keep the playhead visible during a simulated 0→15s playback sweep; slice-disabled correctly true at both clip boundaries, past the sequence end, and on an empty timeline, false mid-clip; an actual slice-button click at mid-clip splits the clip correctly; two synthetic drag gestures (dispatched as real `mousedown`/`mousemove`/`mouseup` DOM events) — dragging a middle clip to the end, and dragging the first clip into the last clip's own on-screen span (the exact case the bug broke) — both produced the correct resulting order with zero console errors, and the reorder survived a full page reload. Export wasn't smoke-tested live since the synthetic clips reference fake file paths with no real media to encode — covered instead by `tests/test_export_smoke.py`'s existing pytest coverage. `pytest -q` green (197, unchanged — this increment is JS/HTML/CSS-only). Verified on a throwaway project (created via direct API call, deleted via API when done), no real project data touched. Process note: this session's shared repo checkout (no isolated git worktrees existed beyond `main`) collided twice with other concurrent sessions — once switching the shared checkout's branch mid-review (caught and restored before any commit was lost), and repeatedly on the shared unnamespaced `.superpowers/sdd/` scratch-file names (task report files got overwritten by unrelated sessions' same-named files mid-task, self-healing by the time each was needed) — worth namespacing those scratch paths per-plan/session if it keeps causing review friction.

- [x] **Slice (increment 1 of Slice + Timeline Editing)** — [design](specs/2026-07-20-slice-timeline-editing-design.md) · [plan](plans/2026-07-21-slice-clip.md): the timeline scissors button (`#slice-action`, "coming soon" before) now cuts the video clip under the playhead into two at that point. Canonical pure `slice_clip(clips, t)` in `app/timeline.py` (built on `locate`; first clip keeps `in_point..s`, second is a new `ClipLayer` `s..out_point` at `order+1` with later orders shifted, both halves sharing `media_id`/`fill_mode`/`speed`; no-op within ε=0.05 s of a boundary or outside any clip) is fully pytest-covered (6 cases incl. trimmed-clip source mapping and a 2× speed case). JS mirror `Timeline.sliceClip` lives in a new `static/timeline-slice.js` that also self-wires the button (reads `Preview.locate`/`Preview.currentTimelineTime` + the `project`/`saveProject`/`renderTimeline` globals) — no `editor.js`/`preview.js` change. **Task review (sonnet) caught a real Critical bug the plan itself introduced:** the plan told the implementer to load `timeline-slice.js` *before* `timeline.js`, but `timeline.js` does `window.Timeline = (()=>{…})()` (replaces the object), which wiped `Timeline.sliceClip` — so the scissors button threw `TypeError` and did nothing. Fixed by loading it after `timeline-snap.js` (the established convention for timeline.js extensions); confirmed live that `Timeline.sliceClip` now exists and the button splits a clip with zero console errors. Live-verified on a throwaway project end-to-end: mid-clip click splits 1→2 clips ([0,2]+[2,4]); boundary/empty click is a harmless no-op; pure mirror matches the backend. Scope note: **only slice** shipped — zoom, drag-to-reorder, and the slice-button disabled visual state remain on the backlog item above. `pytest -q` green (197: 191 + 6 slice tests). Verified on a throwaway project (created + deleted via API), no real data touched.

- [x] **Copy/duplicate items** — [design](specs/2026-07-20-copy-duplicate-design.md) · [plan](plans/2026-07-21-copy-duplicate.md): Ctrl+D + a "Duplicate" button in the VIDEO/TEXT panels duplicate the selected clip or text block. Pure frontend state mutation (the model already supports it via fresh `crypto.randomUUID()` ids), so no backend/pytest — verified live in-browser on a throwaway project. A clip copy spreads all fields (media_id/trim/`fill_mode`/`speed`), gets `order = original+1` with every later clip shifted up by one, carries the `clipDurations` cache entry, and is selected. A text-block copy deep-copies the block AND its preset (new ids, `preset_id` re-linked, preset `x`/`y` +20 so it's visibly offset) and is selected. Ctrl+D lives in editor.js's keydown handler after the redo branch, reusing the existing input/contentEditable focus guard and `preventDefault` (browser bookmark), dispatching on `selected.type` (video/text only; captions/video-boxes out of scope). **Task review (sonnet) caught a real Critical aliasing bug** the plan's own code carried: the block copy shallow-spread `formatting_runs` (an array), so the copy shared the source's runs by reference — editing rich-text on one twin would silently mutate the other. Fixed by cloning per-element (`(src.formatting_runs || []).map((r) => ({ ...r }))`); confirmed live (mutating the copy's runs left the original untouched). Live-verified end-to-end: clip copy at order+1 with all fields carried and selected; text copy with independent preset + independent runs + `+20/+20` offset + selected; Ctrl+D duplicates the selection; Ctrl+D while typing in an input is correctly ignored; zero console errors. `pytest -q` green (191, unchanged — JS/HTML-only). Verification used a throwaway project (created + deleted via the API) per the new live-verify-on-throwaway rule, so no real project data was touched.

- [x] **Clip speed control** — [design](specs/2026-07-20-clip-speed-design.md) · [plan](plans/2026-07-21-clip-speed-control.md): per-clip playback speed 0.5×–2.0× (`ClipLayer.speed: float`, defaulting 1.0 so old projects are unchanged). The two pure timeline functions became the single source of truth: `clip_duration = (out-in)/speed`, `locate`'s timeline→source conversion multiplies by speed (mirrored in `static/preview.js`/`static/timeline.js`). Export gains `setpts=(PTS-STARTPTS)/speed` (video) + `atempo=speed` (audio, pitch-preserving) in both `build_export_cmd` and `build_audio_cmd`, and scales synthesized-silence duration by `/speed` — all **gated on `speed != 1.0`** so a 1.0× clip's export command is byte-identical to before (every pre-existing `test_ffmpeg_cmd` assertion untouched). Preview sets `player.playbackRate`. A `UI.numberField` SPEED row (0.5–2.0, step 0.1, ×) in the VIDEO panel drives it. **Verification found the design doc's "just change the two pure functions, every consumer follows" premise was materially incomplete**: 6 inline `out_point - in_point` timeline-domain computations bypassed `clipDuration()` (preview.js's source→timeline inverse `computeTimelineTime`, clip-sequence.js's drag-split scan + `splitAt` + carrying `speed` onto both split halves, editor.js's timeline-start accumulation, panel-video.js's delete-reseek math, timeline-snap.js's boundary math) — each fixed by hand — while the `clipDurations` cache (source-domain trim clamp bound) and the source-domain clip-end checks were correctly left alone. Executed as **batched parallel subagent development** (Batch 1: foundation model+math; Batch 2: export ∥ JS-mirrors in parallel isolated worktrees; Batch 3: UI panel), each batch reviewed per-task then merged+pushed to main. Final whole-branch review (opus) returned Ready-to-merge with two Minor findings; applied the useful one — a `gt=0` Pydantic constraint on `speed` so `clip_duration`'s divide can't hit zero from a corrupt/hand-edited JSON (UI already clamps 0.5–2.0). `pytest -q` green (191 passed: 181 baseline + 3 timeline + 5 ffmpeg + 2 model). Live-verified in-browser on a real 2-clip project: SPEED renders at 1.0×; 2× halves the timeline block (90px) + sets `playbackRate` 2 + persists + `seqDur` 4.5; 0.5× doubles it (360px, seqDur 9); `locate` maps timeline→source correctly (t=0.5→src 1.0 at 2×); drag-splitting a 2× clip splits at the right source frame with both halves keeping speed 2; a pre-feature project loads as 1.0× (backward compat); zero console errors. **Incident flagged to user:** live verification used the real `join-check` project (only one with playable clips; media import can't be automated), and the browser's on-unload keepalive-save flushed a mid-test in-memory split to disk, corrupting it (4 clips + a doubled-backslash path bug from a console typo); reconstructed to the original 2-clip state (verified both media files exist on disk) — but exact original `updated_at` couldn't be preserved and the clips now carry an explicit `speed: 1.0` (functionally identical to the prior absent field).

- [x] **Undo/redo** — [design](specs/2026-07-20-undo-redo-design.md) · [plan](plans/2026-07-21-undo-redo.md): per-session in-memory undo/redo over whole-project JSON snapshots. A new pure `window.UndoHistory` (`static/undo-history.js`) is a two-stack state machine (`record`/`undo`/`redo`/`reset`, cap 50, dedupes against the undo-stack top) with zero DOM/fetch access. `editor.js`'s `saveProject(recordHistory = true)` is the single capture chokepoint every mutation already flows through — it records the *previous* snapshot before persisting the new one; `openProject()` seeds the baseline and resets history so it never crosses projects. `applyRestore()`/`reRenderAfterRestore()` parse a restored snapshot back into `project`, fully rebuild the stage/timeline/media panel, and re-open whichever context panel was showing (falling back to FILES if the previously-selected clip/video-box no longer exists post-restore) before persisting via `saveProject(false)` so the restore itself isn't re-recorded. Ctrl+Z (undo), Ctrl+Y and Ctrl+Shift+Z (redo) are wired into the existing global keydown handler, reusing its existing input/contentEditable focus guard unchanged — so native text-undo inside the stage editor or a number field is untouched. Two real bugs found via task/final review, both fixed directly: (1) the failed-save retry path called `saveProject()` with the default `recordHistory=true`, double-recording and requiring two Ctrl+Z presses to reach the real prior state after any save-failure-then-retry — fixed by having the retry call `saveProject(false)`, since nothing changed since the failed attempt; (2) `reRenderAfterRestore()` only matched `selected.type === "captions"` (the panel-nav entry), not `"caption"` (singular, a selected caption group from a timeline click), so undo/redo with a caption group selected fell back to FILES instead of reopening CAPTIONS — fixed by matching both. Verified live in-browser end-to-end: edit→undo→redo round-trips correctly on stage/timeline/panel; a new edit after undo clears the redo stack; 55 sequential edits cap the undo stack at 50 without error; history resets on project switch; the retry-dedupe fix confirmed by simulating a real save failure + retry and asserting the undo-stack depth is unchanged; the caption-group fix confirmed by selecting a caption group, undoing, and checking `#panel-captions` (not `#panel-files`) reopens. `pytest -q` green (181 passed, JS-only feature — no backend change).
- [x] **Empty new project + timeline add buttons + multiple text blocks** — [design](specs/2026-07-20-empty-project-and-multi-text-design.md) · [plan](plans/2026-07-21-empty-project-multi-text.md): new projects now start with `clips: []`, `text_blocks: []`, `captions: null` (deleted `static/seed.js` and its sample-caption seeding, and `editor.js` no longer auto-creates a text block on TEXT-panel open); fixed the stale-video-on-new-project bug (`preview.js`'s clipless `load()` branch now also calls `player.load()`, not just `removeAttribute("src")`); the VIDEO and TEXT timeline rows both gained a small dashed `+` button (VIDEO's reuses the existing pick-file→probe→`addClip()` path, TEXT's calls a new `addTextBlockAndEdit()` that creates a block at the playhead, selects it, opens the TEXT panel, and enters on-stage edit mode immediately); and the TEXT panel now supports real multiple blocks — an explicit `selectedTextBlockId` + `currentTextBlock()` accessor (falls back to the first block, never auto-creates) replaced the old implicit-`text_blocks[0]` assumption baked into `ensureTextBlock()`, with per-block selection wired from both timeline clicks and stage clicks, an empty-state TEXT panel when zero blocks exist, and a "Delete text" button + guarded Delete key to remove the selected block. `editor.js` (762 lines) and `preview.js` (488 lines) were both already over `CLAUDE.md`'s 400-line hard limit before this item could add any code, so three prerequisite pure-refactor tasks extracted `static/panel-text.js`/`static/panel-captions.js`/`static/clip-sequence.js` out of `editor.js` and `static/preview-text.js`/`static/preview-captions.js` out of `preview.js` first (editor.js ended at 398 lines, preview.js at 261) — none of this batch's four earlier design docs anticipated the split, since both files had grown past the limit since they were written. Two real bugs found and fixed live during batch verification, neither introduced by this batch's tasks: (1) `timeline.js`'s TEXT-row selection check read `selected.item.id` unguarded, crashing whenever the TEXT panel was opened without a specific block selected (e.g. via the left-rail icon) and at least one text block existed — fixed by mirroring the VIDEO BOX row's existing `!!selected.item` guard; (2) `#text-empty-state` carried `class="style-group"` (`display: grid`), which silently overrode its `[hidden]` attribute at equal CSS specificity — the same class of bug as an earlier documented `.style-field[hidden]` fix — so the empty-state message stayed visible alongside the accordions; fixed by adding the matching `.style-group[hidden]` rule. Verified live in-browser end-to-end (a fresh project has zero clips/blocks/captions and no stale video frame after switching from a clip-bearing project; the VIDEO/TEXT + buttons render and work, including immediate on-stage edit mode for a newly created block; two independently created blocks are separately selectable from the timeline with fully isolated presets; the delete button and Delete key both remove the selected block, the latter correctly guarded against firing while typing in a block — confirmed via a real, non-synthetic focus change, since a synthetic `.blur()` doesn't reliably fire in headless automation, the same documented quirk as an earlier batch) and via `pytest -q` (181 passed, +1 new two-block `ass_render` test).
- [x] **Media library management** — [design](specs/2026-07-20-media-library-management-design.md): rename a media item (`MediaItem.name`, empty falls back to the file_path basename via a `display_name` property mirrored in JS), an in-use indicator (usage-count chip + disabled trash with a "used by N clips" tooltip), and remove-when-unused. Both features would otherwise have added code to `static/editor.js`, which was already 887 lines — well over `CLAUDE.md`'s 400-line hard limit — so two prerequisite pure-refactor tasks extracted `static/panel-media.js` (MEDIA/FILES panel) and `static/panel-video.js` (VIDEO panel) out of it first, following the established `panel-video-box.js` one-file-per-panel-section pattern; neither original design doc anticipated this, since `editor.js`'s size had grown past the limit since they were written. Display-name resolution propagates to all three places a media item's name shows: the MEDIA panel row, the VIDEO panel's name label, and the timeline's VIDEO-row clip block. Task review caught one real bug: `panel-video.js`'s clip-delete flow never refreshed the MEDIA panel, so a media item's usage indicator went stale after its last clip was deleted — fixed directly (`MediaPanel.render()` added to `deleteClip`). Verified live in-browser via the JS console (native file-picker import can't be automated): rename commits on blur and is cancelled by Escape (verified the code path is exercised correctly, not just DOM state, by dispatching real blur/keydown events — a real `.blur()` call doesn't reliably fire in headless automation, a testing-only quirk unrelated to the app), an empty/whitespace commit clears the override back to the file-derived name, the VIDEO panel picks up the renamed value, an unused item's trash removes it from `media_library` without touching `clips`, and deleting a clip immediately re-enables its media item's trash button. `pytest -q` green (180 passed, +9 new tests). **Note:** live verification mutated a real saved project's data (deleted a clip) rather than test fixtures — restored via a best-effort reconstruction from the pre-test state (exact original clip trim points weren't captured beforehand); flagged to the user.
- [x] **Crop-to-fill** — [design](specs/2026-07-20-crop-to-fill-design.md): per-clip choice between today's letterbox (`fit`) and a center-crop (`fill`) — `ClipLayer.fill_mode: str = "fit"`, branching `app/ffmpeg_cmd.py`'s per-clip filter chain (`force_original_aspect_ratio=increase,crop=W:H` vs the existing `decrease,pad=W:H:...`, sharing the same trim/setpts prefix and fps suffix so neither branch duplicates that logic) and the preview `<video>`'s CSS `object-fit` (a `.fill-mode-fill` class toggled in `static/preview.js`'s `applyFillModeClass()`, called from both `playClipAt()` and `seek()`'s clip-switch branch — the two places `#player`'s active clip changes). VIDEO panel gained a FIT/FILL `UI.buttonGroup` row (`static/panel-video.js`, itself newly extracted from `editor.js` by the media-library-management item earlier in this same batch). Verified live: toggling FILL immediately applies the CSS class and persists `fill_mode`; toggling back to FIT removes it; `pytest -q` green (180 passed, +2 new tests asserting per-clip mixed fit/fill filter-chain output in a single export command). Final whole-batch review found one real cross-task integration gap only visible at the whole-branch level: `editor.js`'s pre-existing `insertClipIntoSequence()` (drag-to-stitch/split) predated `fill_mode` and didn't carry it through a clip split, silently reverting a FILL clip's second half to FIT — fixed directly.
- [x] **Caption per-word timing estimation** — [design](specs/2026-07-20-caption-word-estimate-design.md): karaoke highlighting now sweeps word-by-word across a multi-word caption entry (e.g. "talks about this" authored as one chunk with one start/end) instead of lighting the whole phrase at once. A new pure `estimate_word_timings`/`Timeline.estimateWordTimings`, mirrored in `app/caption_word_estimate.py` and `static/caption-word-estimate.js`, splits a multi-word entry into per-word sub-ranges by character-offset interpolation within the entry's own duration; a single-word entry (real transcription output) passes through unchanged. Wired into the single existing grouping choke point on each side (`app/ass_render.py`'s `group_words`, `static/timeline.js`'s `groupWords`), so every caller — preview highlighting, the timeline CAPTIONS row, playhead snapping, ASS export — picked it up with zero other call-site changes. No data model changes; the Caption-words drill-down (shipped earlier the same day) needed no edits since it already authors chunk-level text/timing. Verified live: seeded a multi-word entry via the console, confirmed `Timeline.groupWords` internally expands it into 3 individually-timed words (grouped into one timeline block since all 3 fit under the default `max_words_per_line=4`) and that the stage highlight sweeps word-by-word ("talks"→"about"→"this" at increasing timestamps) instead of lighting the whole phrase at once; `pytest -q` green (171 passed).
- [x] **Autosave indicator (failed/retry state)** — [design](specs/2026-07-20-autosave-indicator-design.md): adapted in-flight — an indicator already existed (`static/ui-save-indicator.js`, mounted in `#panel-brand`, added during Phase 2), so rather than build a second/duplicate chip in the timeline toolbar as the doc originally proposed, extended the existing one in place. The real gap was that `saveProject()` had no error handling — a failed `Api.saveProject()` call threw unhandled, leaving the indicator stuck on "Saving…" forever with no way to retry. `saveProject()` (`static/editor.js`) now wraps the call in try/catch; on failure it logs to console and calls a new `setFailed(retryFn)` (danger-styled "Save failed — retry", clickable, retry just re-invokes `saveProject()`). `UI.saveIndicator` also gained in-flight counting (overlapping saves no longer flicker Saved/Saving) and a 2s fade-to-icon-only timeout on "Saved" (`is-faded` class). Verified live: simulated a network failure by monkey-patching `window.fetch`, confirmed the indicator turned red/clickable with "Save failed — retry", restored `fetch` and clicked to retry, confirmed it recovered to "Saved" then faded; `pytest -q` green (146 passed at the time, JS/CSS-only change), no console errors outside the intentionally-simulated failure.
- [x] **Export settings** — [design](specs/2026-07-20-export-settings-design.md): output filename + HIGH/MEDIUM quality preset. `Project.export_filename`/`export_quality` (new fields, empty/`"high"` defaults preserve old-project behavior); `app/ffmpeg_cmd.py` maps quality to `-crf` (18/23) at both mp4-output call sites — corrected one doc/code mismatch along the way: the doc said `-preset medium`, but the code has always used `-preset fast`, left unchanged, only CRF varies. `app/main.py`'s export route derives the output filename from `sanitize_export_filename(p.export_filename)` (strips path separators and other filesystem-unsafe characters, trims dots/whitespace, drops a redundant `.mp4` if typed) when set, else falls back to the existing `{name}-{id[:8]}` stem; `resolve_export_path()` appends `-2`, `-3`, … on collision. New `static/panel-export.js` (mirroring `panel-video-box.js`'s one-file-per-context-panel-section pattern, since `editor.js` is already over the project's 400-line file-size guideline) renders FILENAME/QUALITY rows in `#panel-export`, wired by a one-line `ExportPanel.render()` call from `openExportPanel()`. Verified live: created a project via the picker, set filename to "my clip name" + MEDIUM quality, confirmed both persisted to the in-memory project and to disk via the API; `pytest -q` green (162 passed, +16 new tests covering CRF mapping, sanitization, and collision suffixing).
- [x] **Caption word-timing editing** — [design](specs/2026-07-20-caption-word-timing-design.md): `t_start`/`t_end` in the Caption-words drill-down (`static/caption-panel-words.js`) are now editable `<input type="number" step="0.1">` fields (replacing the old display-only time label) validated by a small pure `clampWordTiming()` helper — `t_start` clamped to `>= 0`, edit rejected (field reverts to the stored value, no save) unless `t_start < t_end`; overlaps with neighboring words are allowed, per spec. A valid commit updates the `CaptionWord`, saves, and re-renders both the stage caption preview and the timeline CAPTIONS row (`renderTimeline()`, the project's established post-edit pattern). Verified live: seeded caption words via `seed.js`'s dev-convenience default, edited a word's start time from 0 to 0.2 (persisted to the model), then attempted an invalid edit (start > end) and confirmed the field reverted and the model was untouched; `pytest -q` green (162 passed, JS-only change, untested layer per the design doc's stated convention for thin UI wiring).
- [x] **Delete clip** — [design](specs/2026-07-20-delete-clip-design.md): "Delete clip" button in the VIDEO panel (danger-styled, mirrors the existing `#video-box-delete` pattern; introduced a `--danger` token in `tokens.css` since both delete buttons referenced an undefined one) + a Delete key handler guarded to `selected.type === "video"` with focus not in a form field. Removing a clip renumbers the remaining clips' `order` gap-free, drops the now-stale `clipDurations` cache entry, and — only if the playhead was inside the deleted clip's range — reseeks it to the deleted clip's former start, clamped so deleting the last clip can't seek past the new (shorter) total duration. The media-library item is untouched (separate item). Verified live: order renumbering, Delete-key guard against wrong selection types and focused inputs, last-clip deletion with the playhead inside it (no crash, clamps correctly), zero console errors; `pytest -q` green (146 passed, JS/HTML/CSS-only change).
- [x] **Snap playhead** — [design](specs/2026-07-20-snap-playhead-design.md): ruler-click and playhead-grip-drag seeking now snap to the nearest clip/text-block-start-or-end/caption-group-start within an 8px tolerance, via a new pure, dependency-free `static/timeline-snap.js` (`Timeline.snapTime`/`Timeline.collectBoundaries`, plus an additive `Timeline.PX_PER_SEC` export from the existing `timeline.js`). Holding Alt during either the click or the drag bypasses snapping, re-checked live per event so it can be toggled mid-drag. Transport step buttons, arrow-key nudge, and clip-drag/drop are untouched. Verified live: pure-function correctness (true nearest-match, tolerance math, empty-boundaries no-throw), and — since real seek-through-fake-media doesn't reflect real usage in a scripted browser — confirmed the actual wiring by spying on `Timeline.snapTime` through both the ruler-click and grip-drag paths, showing it's called with the right arguments on a plain click/drag and correctly skipped when Alt is held; zero console errors; `pytest -q` green (146 passed, JS-only change). Final whole-batch review (with delete-clip) found no Critical/Important issues — only two Minor items (undefined `--danger` token, one redundant re-render), both fixed directly.
- [x] **Right panel 8-column grid** — [depth doc](specs/2026-07-19-right-panel-grid-design.md) · [plan](plans/2026-07-19-right-panel-grid.md): redesigned `#style-panel`'s content layout onto a fixed 8-column grid (`repeat(8, 28px)`, 8px gap, `.col-1`…`.col-8` utilities, panel padding `18px 20px`); nested `.style-group > .style-row` gets a systemic fix (`grid-column: 1 / -1`) so its own children can still place themselves within the grid. `UI.numberField`/`UI.colorSwatch`/`UI.buttonGroup` each gained a `span`/`containerSpan` option (defaults 8 for numberField/buttonGroup container, `showLabel ? 8 : 1` for colorSwatch, 1 per buttonGroup option) so callers position controls explicitly instead of relying on flex/1fr guesswork. The original plan predated several panels that shipped since — explicit spans were wired across not just FONT/BOX/TIME/STYLES/VIDEO/CAPTIONS/SETTINGS/EXPORT but also PROJECTS/VIDEO BOX/LAYERS, and the stale plan was adapted in-flight to match current code (weight picker, absolute x/y fields, caption accordions, FILL mode) rather than followed literally. Verified live in-browser: nested rows render at their expected 280px width, zero unplaced grid children, no console errors, screenshots of the TEXT panel confirmed correct alignment; `pytest -q` green (146 passed).
- [x] **Phase 2 — Project management** — [plan](plans/2026-07-19-phase-2-project-management.md): server-sourced project list/switch/create/rename/delete/duplicate (`static/panel-projects.js`, `ui-project-picker.js`, store/main routes); survives cleared browser storage
- [x] **Phase 3 — Video Box (picture-in-picture)** — [plan](plans/2026-07-19-phase-3-video-box/index.md): banded video boxes end-to-end (data model, ASS/ffmpeg banding, drag UI, preview, timeline row, panels)
- [x] **Phase 4 — Captions** — [plan](plans/2026-07-19-phase-4-captions.md): faster-whisper transcription, CAPTIONS panel accordions, karaoke highlight (current-word/progressive-fill), preview + export burn-in
- [x] **Phase 5 — Rich-text formatting** — [plan](plans/2026-07-19-phase-5-rich-text-formatting.md): per-selection `FormatRun` overrides incl. highlight, range-aware word-wrap, per-line ASS highlight rects
- [x] **Phase 6 — Export polish** — [plan](plans/2026-07-20-phase-6-export-polish.md): silent-audio synthesis for video-only clips (export + transcription), preview clip-join prefetch, whole-pipeline export smoke test
- [x] "Fit text to box" — shipped as the BOX SIZE row's FILL mode ([design](specs/2026-07-19-fit-text-to-box-design.md), `static/font-fit.js`)
- [x] Font-weight picker replacing the Bold toggle — shipped ([design](specs/2026-07-19-font-weight-picker-design.md), `TextPreset.weight`, per-weight font files)
- [x] Removed all pre-existing inline `style="..."` attributes from `static/index.html`, per the no-inline-styles convention (`CLAUDE.md`): `text-size-field`'s `flex: 1` and the SIZE row's `align-items`/`gap` moved to id-scoped rules (`#text-size-field`, `#text-size-row`) in `static/css/components/style-panel.css`; the three play/pause, panel-open/close, and sun/moon icon-swap SVGs (previously toggled via `style="display:none"` + JS setting `.style.display` directly) now use a new `.icon-hidden { display: none; }` utility class (`static/css/components/button-group.css`) toggled via `classList.toggle` in `editor.js`/`preview.js` — same rationale as before (SVG elements don't reliably honor the `hidden` attribute) but no inline styles anywhere. Verified live: play/pause, theme, and panel-collapse icon swaps all still toggle correctly; `pytest -q` green (48 passed), no console errors.
- [x] Redefine the HORIZONTAL/VERTICAL coordinate fields as absolute, 1-indexed pixel coordinates of a point on the box — see [design doc](specs/2026-07-19-absolute-pixel-coordinates-design.md) · [plan](plans/2026-07-19-absolute-pixel-coordinates.md). `TextPreset.x` now means the box's left/center/right edge depending on `align`; `TextPreset.y` always means the box's top edge. Removed `pos_row`/`pos_col`/`offset_x`/`offset_y` entirely (no migration, per design). `ass_render.py`'s `_style()` now picks ASS numpad alignment codes `\an7`/`\an8`/`\an9` from `align` instead of a hardcoded center anchor — also fixes a pre-existing bug where multi-line text was always center-justified in export regardless of `align`; `_box_dialogue()`'s left-edge math is align-aware to match. `preview.js`/`stage.css` replaced the blanket `-50%/-50%` transform with three align-specific CSS modifier classes. The POSITION accordion's HORIZONTAL/VERTICAL fields edit `x`/`y` directly; the 3x3 grid is now a stateless one-shot shortcut with no persisted "active" cell. Executed via subagent-driven-development (4 tasks, each independently reviewed clean; final whole-branch review found no Critical/Important issues, only stale CLAUDE.md wording in 3 spots, fixed directly). Rebased onto main after a concurrent session had already landed a duplicate of the CSS/preview.js piece (`2bee18d`) plus an unrelated font-weight-picker refactor — resolved by keeping both (weight field + align-aware position). `pytest -q` green (52 passed).
- [x] Preview playback now works with zero video clips — `preview.js` gained an internal virtual clock (`performance.now()`-based, module-local `virtualTime`/`virtualPlaying`/rAF loop) that drives `play`/`pause`/`restart`/`seek`/`currentTimelineTime` whenever `clips.length === 0`, instead of the `<video>` element's `timeupdate`/`play`/`pause` events; duration is the max end time across text blocks and caption words (mirroring `static/timeline.js`'s existing `totalDuration()`, which already handled this case for the ruler/toolbar). Added a new `Preview.isPaused()` so callers never read `player.paused` directly — `editor.js`'s one direct read (the Up-arrow play/pause shortcut) was updated to use it; every other call site already went through `Preview.play()/pause()/seek()`, so no other changes were needed outside `preview.js`. Verified live end-to-end: emptied `project.clips`, pressed play, confirmed the time readout advanced via the virtual clock, auto-paused (and flipped the play/pause icon) exactly at the text block's end time, and that seek/restart both work correctly in this mode. `pytest -q` green (48 passed, JS-only), no console errors.
- [x] Merged the standalone POSITION accordion into BOX — TEXT ALIGN + the anchor grid/offset X/Y controls now live at the bottom of BOX's body (after SIZE/Background/Border, with a new divider), leaving the TEXT panel with just STYLES/FONT/BOX/TIME. `UI.accordionSection`'s already-established pattern (it re-parents the body div into the header container at call time) meant this was a pure DOM relocation in `static/index.html` + dropping one `UI.accordionSection(...)` call + adding one `UI.divider(...)` call in `static/editor.js` — `TextPanel.renderAlign()`/`renderPosition()` needed no changes since they already just populate existing element ids, wherever those ids currently live in the DOM. Verified live: BOX accordion now shows SIZE → Background/Border → TEXT ALIGN → POSITION in one panel, no separate POSITION header remains, and clicking a TEXT ALIGN button still correctly updates `preset.align`. `pytest -q` green (48 passed, JS/HTML-only), no console errors.
- [x] BOX's Background/Border rows redesigned: Background is now swatch-only (no checkbox, no "Background" text label — `UI.colorSwatch` gained an optional `showLabel` param, default true, that keeps `aria-label` either way), plus a new Opacity field (`box_background_opacity: int = 100`, a brand-new `TextPreset` field needing no store.py migration since it's not a rename) that drives both the ASS export alpha (`ass_render.py`'s `_box_dialogue`, converting the 0-100 opacity percent to an inverted ASS alpha byte) and the live preview (`preview.js`'s new `hexToRgba()` helper). Picking a Background color now also sets `box_background = true` directly — the checkbox is gone entirely; how to clear back to transparent afterward stays a separate, already-planned item. Border Color's swatch moved onto the same row as BORDER (PX)/RADIUS (PX), also label-less. Added a `pytest` case confirming a 50% opacity value produces an intermediate ASS alpha byte (`0x80`), neither of the old binary 00/FF values. `pytest -q` green (48 passed, +1 new test); verified live (no checkbox, Background/Opacity share a row, Border Color shares the BORDER/RADIUS row) after working around the same preview-browser stale-cache artifact noted in earlier entries — confirmed via fresh-fetch + DOM/`curl` checks that the real server always serves current code.
- [x] Stage text is now clickable regardless of which right-panel section is open — `preview.js`'s `renderText()` no longer gates `pointerEvents`/`UI.textInteraction` mounting behind `block.id === selectedTextBlockId`; every rendered block always gets `cursor: text` + click-to-edit (resize handles stay gated behind an active TEXT-panel selection, unchanged). A new `Preview.setOnStageTextActivate(fn)` (registered once at startup in `editor.js`, distinct from the per-render `setSelectedTextBlock`) fires when an unselected block's edit mode is entered, calling `openTextPanel()` so the same click both switches the right panel to TEXT and lands the caret ready to type. Found and fixed a real bug along the way: re-appending the same `editingDiv` across a synchronous re-render (which happens when the click's `onEditStart` triggers `openTextPanel()`'s re-render mid-click) silently drops browser focus without firing `blur` — fixed by re-focusing + restoring the caret to the end of the content when the reused div isn't `document.activeElement`. Verified live end-to-end: clicked stage text from FILES panel, confirmed in one click the TEXT panel opened, `document.activeElement` was the text div, and dispatched real keystrokes that actually appended to `block.heading`. `pytest -q` green (47 passed, JS-only), no console errors.
- [x] TEXT ALIGN's three icon buttons were rendering inside `.btn-group`'s stretched 1fr-column layout (button-group.css), so they filled the row instead of sitting as compact squares. Scoped fix in `static/css/components/style-panel.css`: `#text-align-group { grid-auto-columns: min-content; justify-content: start; }` plus explicit 28×28px sizing on its buttons — matches `.icon-btn`'s footprint (Bold/Italic/Underline) without touching the shared `.btn-group`/`UI.buttonGroup` component used by other rows (POSITION row/col, BOX size mode). Verified live: `getComputedStyle` on a button confirms 28×28px/0 padding; `pytest -q` green (47 passed, CSS-only).
- [x] Remove the white border around the video stage — dropped `border: 1px solid var(--border);` from `#stage` in `static/css/components/stage.css`. `pytest -q` green (47 passed, CSS-only).
- [x] Combine BOX's two independent WIDTH/HEIGHT FIT/FIXED button-group rows into one combined SIZE row (FIT/FREE — scoped down from the full 3-way ask; the third "Fit text to box" mode needs its own design pass and is re-added above as its own item) — one `UI.buttonGroup` now sets both `box_width_mode`/`box_height_mode` together, WIDTH (PX)/HEIGHT (PX) fields still shown/hidden together (`static/editor.js`, `static/index.html`). Backend model (`app/models.py`, `app/ass_render.py`, `static/preview.js`) intentionally untouched — both fields still independently stored, just always written in sync by this UI. Along the way, found and fixed a real pre-existing bug: `.style-field[hidden] { display: none; }` was missing in `static/css/components/style-panel.css`, so `.style-field { display: flex }`'s equal-specificity rule silently overrode `[hidden]` and the WIDTH/HEIGHT fields never actually disappeared in FIT mode (present since the original two-row Box UI, not introduced by this change). Verified live: FIT/FREE toggle correctly drives both dimensions together, stage box visibly resizes; `pytest -q` green (47 passed).
- [x] Three more small TEXT-panel polish items, dispatched as parallel worktree agents: "Save current style" now snapshots `pos_row`/`pos_col`/`offset_x`/`offset_y` into the saved preset alongside font/box/align (`static/text-panel-style.js`); TEXT ALIGN's LEFT/CENTER/RIGHT buttons now render Lucide align icons instead of text, via a new optional `icon` field on `UI.buttonGroup` that falls back to `aria-label` for accessibility (`static/ui-button-group.js`, `static/text-panel-align.js`); added font-size step buttons (Lucide up/down arrows) beside SIZE (PX) that jump through a fixed preset scale (12/14/16/18/21/24/36/45/56px), snapping to the nearest preset and clamping at the ends (`static/text-panel-font-style.js`, `static/index.html`). One agent's worktree (font-size buttons) had branched before the prior batch's IIFE-wrap landed, so its patch conflicted on apply — hand-ported the same change into the current file structure instead of using its raw diff. `pytest -q` green (47 passed), verified live (DOM-level check after working around a stale-cache artifact in the preview browser pane — the real server always served fresh content per `curl`).
- [x] Clear `selectedTextBlockId` when leaving the TEXT panel — investigated: already fixed by commit `9d95c63` (`showPanel()`'s `if (type !== "text") Preview.setSelectedTextBlock(null, null);`), which predates the Phase 1 review that flagged it; every `renderTextPanel()` call site is gated behind `showPanel("text")` first, so there's no path that leaves it stale. Verified live: opening TEXT then switching to FILES renders no `.text-block` element at all (no lingering click-to-edit/drag/resize). No code change needed.

- [x] Six small TEXT-panel polish items, dispatched as parallel worktree agents: renamed POSITION's OFFSET H/V labels to HORIZONTAL/VERTICAL + hardened their `onChange` handlers with `Math.round()` (`static/text-panel-position.js`); removed the divider below the Font Family settings row and renamed+reordered the STYLE accordion to "STYLES" first in the TEXT panel (`static/index.html`, `static/editor.js`); shrank `.color-swatch` from 32×32px to 28×28px to match `.icon-btn` (`static/css/components/color-swatch.css`); wrapped `wireTextStyleToggle` in an IIFE for scoping consistency (`static/text-panel-font-style.js`); added an opt-in `decimals` option to `UI.numberField` and used it so TIME's START/END always show one decimal place (`static/ui-number-field.js`, `static/text-panel-time.js`). Zero file conflicts between agents (each item scoped to disjoint files); `pytest -q` green (47 passed) and verified live in browser after integration.

- [x] **Phase 1 — Text styling complete** — [depth doc](specs/2026-07-17-phase-1-text-styling-complete-design.md) · [parallel decomposition](specs/2026-07-18-phase-1-parallel-decomposition-design.md) · [plan](plans/2026-07-18-phase-1-parallel-decomposition.md): finished the Text Box plan's Task 12 (`CLAUDE.md` docs), added the saved-style preset library (`GET/POST /api/presets`, `TextPreset.usage_count`), restructured the TEXT panel into five accordions (FONT/STYLE/BOX/POSITION/TIME) each wired by its own `static/text-panel-*.js` file, replaced the side-panel heading textarea with inline on-stage editing, and added drag-to-reposition (`static/ui-text-interaction.js`). Executed via a component-per-file parallel decomposition (one file per accordion/control instead of one task per accordion) so independent tasks ran as true parallel subagents with zero shared-file conflicts. Two real bugs found and fixed during manual verification: an empty-heading text block couldn't be clicked into once the side-panel textarea was removed, and drag-to-reposition's offset math needed rounding to satisfy `TextPreset`'s Pydantic `int` fields. Final whole-branch review: ready to merge, no Critical/Important findings, 47/47 tests passing.
- [x] Font Family row: two stacked lines (label above, value+chevron below) — see [2026-07-17-font-family-drilldown-design.md](specs/2026-07-17-font-family-drilldown-design.md), task 7
- [x] Font Family drill-down header: align with `#style-panel-collapse-toggle` — see [2026-07-17-font-family-drilldown-design.md](specs/2026-07-17-font-family-drilldown-design.md), task 8
- [x] Divider between FONT and MISC accordion content — see [2026-07-17-font-family-drilldown-design.md](specs/2026-07-17-font-family-drilldown-design.md), task 12
- [x] Font list rows: replace Apply-button flow with hover-to-preview + click-to-save — see [2026-07-17-font-family-drilldown-design.md](specs/2026-07-17-font-family-drilldown-design.md), task 9
- [x] Font list: checkmark on applied font, pinned to top of list — see [2026-07-17-font-family-drilldown-design.md](specs/2026-07-17-font-family-drilldown-design.md), task 10
- [x] Font list row background: transparent at rest, lighter grey on hover only — see [2026-07-17-font-family-drilldown-design.md](specs/2026-07-17-font-family-drilldown-design.md), task 11
- [x] `UI.resizeHandles` component (Text Box plan Task 6) — see [2026-07-17-text-box.md](plans/2026-07-17-text-box.md)
- [x] Font Family revision: manual verification in browser — see [2026-07-17-font-family-drilldown-design.md](specs/2026-07-17-font-family-drilldown-design.md), task 13
- [x] Text Box component, Tasks 1-5 (Pillow/fontTools dep, `TextPreset` box fields + migration, word-wrap, ASS rounded-rect box rendering) — see [2026-07-17-text-box.md](plans/2026-07-17-text-box.md); merged, `pytest -q` green (40 passed)
- [x] Text Box component, Tasks 7-11 (BOX accordion markup/wiring, `preview.js` box CSS rendering, stage resize handles) — see [2026-07-17-text-box.md](plans/2026-07-17-text-box.md); merged, verified live in browser
- [x] `UI.divider` component, extracted from one-off `.style-divider` CSS, used for the VIDEO/TEXT panel dividers and between Font Family drill-down rows — see `static/ui-divider.js`
- [x] Text Box component, Task 13 (end-to-end verification) — see [2026-07-17-text-box.md](plans/2026-07-17-text-box.md); `pytest -q` green (43 passed). Found and fixed two real bugs along the way: export never wired ASS rendering into ffmpeg at all (no text/box/captions ever burned into any export, unrelated to Text Box specifically), and the box dialogue referenced a non-existent ASS style causing a position offset in export vs. preview — both confirmed fixed via a real export + frame extraction, see [2026-07-17-text-box-design.md](specs/2026-07-17-text-box-design.md)'s Open questions section
- [x] Accordion header label vertical centering + section spacing/divider — `line-height: 1` on `.accordion-header` (was inheriting a larger default, pushing text off-center); divider moved from the header's `border-top` to the body's `border-bottom` + `padding-bottom` for breathing room. Verified numerically (header text/row vertically centered to sub-pixel precision; all three TEXT-panel accordion bodies have the padding+border) — see `static/css/components/accordion.css`
- [x] Logo dot and REEL positioning — investigated, confirmed already correct: pixel-accurate match to the design-foundation mockup (dot's `border-radius: 2px` "rounded square" look, spacing, vertical alignment all intentional/as-designed). No change needed.
- [x] Move REEL logo from top bar to the bottom of the left nav rail — removed `#brand`/`#topbar-divider` from `#topbar`, added a shrunk `#panel-brand` (6px dot, 10px name, 8px tag, vertically stacked) pinned to `#panel`'s bottom via `margin-top: auto`. Verified numerically (16px from rail bottom, fits the 72px width, no console errors) — see `static/css/components/panel.css`
- [x] Remove the "LOCAL" tag from the relocated REEL logo — dropped `#brand-tag` span + its now-dead CSS rule, leaving just the dot + "REEL" name in `#panel-brand`. Verified: no console errors, 43/43 tests pass.
- [x] BOX accordion: remove the checkmark, set background to transparent by default — investigated 2026-07-18: no checkmark element exists anywhere near the BOX accordion header or its Background swatch (only `.style-checkbox` in `style-panel.css`, a plain native checkbox); `TextPreset.box_background` already defaults to `False` in `app/models.py`. Verified live in browser: cleared `localStorage`, loaded a brand-new project, opened a fresh text block's BOX accordion — Background checkbox is unchecked and width/height mode is FIT, matching `defaultTextPreset()` in `editor.js`. (An older, already-saved project had a checked/fixed-size box, but that's persisted prior test data, not the default.) No code change needed; `pytest -q` green (43 passed).
- [x] Move the safe-zones toggle button from the top bar into the timeline toolbar row — moved `#safe-zones-toggle` into `#zoom-controls` (next to zoom −/+), replaced the "SAFE ZONES" text label with a hand-inlined Lucide shield-icon SVG styled as a plain `.icon-btn` (dropped the old outline-button variant and its dedicated `[aria-pressed]` CSS rule, now covered by the shared `.icon-btn[aria-pressed="true"]` rule). Verified: `pytest -q` green (43 passed), DOM inspection confirms the button now lives in `#timeline-toolbar` and is gone from `#topbar`, no console errors.
- [x] Export icon in the left rail that opens a dedicated export right panel — added an EXPORT entry to `PANEL_NAV_ITEMS`/`PANEL_NAV_HANDLERS` (download icon), `openExportPanel()` following the `openSettingsPanel()` pattern, and a `#panel-export` section holding the relocated `#export` button + `#export-result` (same ids/wiring, moved out of `#topbar`/`#stage-wrap`); pure location/IA change, no new export options. Verified: `pytest -q` green (43 passed), browser-verified end-to-end — clicking the relocated button fires the real export request and updates `#export-result`, no console errors.
- [x] Redesign safe-zone guide overlay for clarity — replaced thin dashed outlines with shaded bands (`color-mix` tint), a solid accent edge on the interior-facing border, and opaque label chips (same visual recipe as `.slice-btn`) so it stays legible over any video content underneath; all values sourced from existing `tokens.css` custom properties. Verified: `pytest -q` green (43 passed), toggled live in browser via `#safe-zones-toggle` and confirmed `#safe-zones` un-hides, no console errors.
- [x] Settings icon in the left rail that opens a dedicated settings panel — added a SETTINGS entry to `PANEL_NAV_ITEMS`/`PANEL_NAV_HANDLERS` (gear icon) following the existing FILES/TEXT/CAPTIONS pattern, added `#panel-settings` to `#style-panel` with an APPEARANCE group containing the relocated `#theme-toggle` (moved out of `#topbar`; id/classes unchanged so existing JS wiring kept working). Theme-only for this pass, per confirmed scope. Verified live in browser: settings icon opens the panel, toggling switches `data-theme` and persists to `localStorage` across reload, `pytest -q` green (43 passed), no console errors.
- [x] Remove the top header/`#topbar` entirely — removed the empty `<header id="topbar">` from `index.html` and its rules from `layout.css`; `main` is now `#app`'s only child. Relocated the one still-live piece of functionality, the project-name display, to `document.title` (`"<name> – Reels Editor"`) rather than dropping it or squeezing it into the left rail's fixed-width `#panel-brand`. Verified: `pytest -q` green (43 passed), DOM inspection confirms no topbar, `main` fills the full window height from `top: 0`, tab title shows the project name, no console errors.
