# Codebase map — TikTok-Reels

Local web editor that assembles 4–6 mp4 clips into one vertical reel with trim, editable karaoke captions, a preset-styled heading block, and picture-in-picture video boxes, exported to 1080×1920 mp4. Multi-project: a full-screen picker at cold start plus an in-editor PROJECTS panel (open/rename/duplicate/delete). See `docs/superpowers/specs/2026-07-09-first-reel-design.md` and `docs/superpowers/plans/2026-07-09-first-reel.md`.

## Run commands

- Tests: `.venv/Scripts/python -m pytest -q`
- Frontend tests: `node --test "tests/js/**/*.test.js"` (pure JS modules only — no DOM, no dependencies)
- Server: `.venv/Scripts/python -m uvicorn app.main:app --reload` (then open http://127.0.0.1:8000)
- Setup: `python -m venv .venv && .venv/Scripts/pip install -e .[dev]`
- Requires `ffmpeg`/`ffprobe` on PATH for clip probing/export (not required for `pytest`, which mocks subprocess calls).
- Cloud deploy: Railway, GitHub-connected, builds from the repo's `Dockerfile`. See `docs/deploy-railway.md` for one-time setup and the post-deploy verification checklist.

## Conventions

- No JS build step/bundler — icons come from `UI.icon(name, {size})` (`static/ui-icon.js`), never hand-inlined `<svg>` markup. Adding a new icon means adding a new [Lucide](https://lucide.dev) `ICON_PATHS` entry to `ui-icon.js` (kebab-case Lucide name -> inner `<path>`/shape markup), not pasting an `<svg>` at the call site — `tests/js/no-raw-svg.test.js` forbids hand-inlined SVGs in `static/*.js`/`static/index.html` and will fail the suite if one appears.
- Reusable JS logic — UI components (`window.UI.*`) and API/service calls (`window.Api.*`) — each live in their own file, one function/component per file; never grouped into a shared `*-components.js`-style catch-all.
- Prefer small units that each own one feature or function over broad shared abstractions — microcomponents on the frontend, small focused modules on the backend (e.g. `media.py`/`timeline.py`/`ass_render.py`/`font_metrics.py`/`ffmpeg_cmd.py` staying separate rather than merging). Default to a new small file rather than extending an existing multi-purpose one.
- Every `static/*.js` and `static/css/**/*.css` file opens with a one- or two-line comment stating that file's purpose/role, so its job is clear without reading the whole file. Keep this comment current when a file's role changes.
- No inline `style="..."` attributes in `static/index.html` or JS-rendered markup — all styling lives in `static/css/**` component files as classes, even for small one-off tweaks.

## File structure

```
app/
  __init__.py       # package marker
  main.py           # FastAPI app wiring only (routes -> modules, static mount); DATA_DIR resolves via _resolve_data_dir() from the DATA_DIR env var (default "data"), added 2026-07-23 for cloud hosting
  auth.py            # session-cookie signing for the shared-password login gate (added 2026-07-23, cloud hosting): create_session_token/verify_session_token (itsdangerous URLSafeTimedSerializer, 30-day max age), no accounts/DB
  models.py         # Pydantic data model (Project, ProjectSummary, MediaItem, ClipLayer, VideoBoxLayer, ImageBoxLayer, TextPreset, FormatRun, TextBlockLayer, CaptionWord, CaptionTrack); `ImageBoxLayer` (added image/photo PiP feature) mirrors `VideoBoxLayer` but times via `start`/`duration` instead of in/out trim (images have no source timeline), `z_index: int = -1` same convention; `MediaItem.source_path` (added 2026-07-31, copy-on-import) preserves the original external path for dedup-only, never read for playback
  store.py          # project JSON persistence (save/load/list/delete) + global presets.json
  media.py           # ffprobe command building/duration parsing, extension-based image detection (is_image_path, added 2026-07-21, image/photo clips), serves media files, native file picker (`pick_file(kind="video"|"audio")`/`_filedialog_options(kind)`, audio filter added 2026-07-22 for music import), copy-on-import (`copy_into_media_dir(source_path, media_id, data_dir) -> Path`, added 2026-07-31, copy-on-import: copies source into `<data_dir>/media/<media_id><ext>`, raises FileNotFoundError for missing source)
  timeline.py         # pure sequence math (order, durations, timeline time -> clip+source time); `image_box_end(b)` + `banded_layers(project)` (image/photo PiP feature) partition text blocks, video boxes, and image boxes into z-order export bands, one `("image_box", i)` entry per `ImageBoxLayer`
  ass_render.py        # ASS subtitle generation: text-block dialogue (styles + \pos/\fad entrance); karaoke caption dialogue via render_caption_ass(), word-wrap/pagination against the caption box's fixed size via app/caption_layout.py's paginate_words (added 2026-07-24, caption box sizing, replacing the old flat group_words/max_words_per_line chunking); both apply TextPreset.text_case (added 2026-07-24, case styling) via app/text_case.py's apply_text_case before any measuring/wrapping/emitting, substituting a model_copy of the block/words so the caller's data is never mutated
  text_case.py          # pure apply_text_case(text, text_case) -> str (added 2026-07-24, case styling): "upper"/"lower" -> str.upper()/lower(), anything else passes through; shared contract with static/text-case.js's TextCase.apply
  font_metrics.py       # ASS export word-wrap: pure wrap_text() + Pillow/fontTools measurement adapter for the vendored fonts
  ffmpeg_cmd.py         # pure ffmpeg export-command builder: trim/scale/pad/concat + optional ASS burn; image clips (MediaItem.kind == "image") get `-loop 1 -t <duration>` prepended to their input (added 2026-07-21, image/photo clips); per-clip `ClipLayer.volume`/`muted` apply a `volume=<v>` filter to each clip's audio chain (added 2026-07-22, audio batch 2); `Project.music` (when set and audible) adds the music file as an extra input, trims to the clip sequence's duration, and `amix`es it with the concatenated clip audio, replacing the final audio map `[a]` with `[amix]` (added 2026-07-22, audio batch 3); banded export additionally supports `"image_box"` bands — a `-loop 1 -t <duration>` looped image input scaled/positioned and overlaid, same compositing slot as a `"video_box"` band (image/photo PiP feature)
  filmstrip.py          # timeline VIDEO-row filmstrip generation (added 2026-07-23, timeline thumbnails): pure frame_interval(duration, max_frames=120) and frame_count(duration, interval) — returns ceil(duration / interval), capped at max_frames; `generate_filmstrip(media_id, file_path, data_dir) -> Path` caches one horizontal sprite-sheet JPEG per media file at `data/filmstrips/{media_id}.jpg`, one sampled frame per `frame_interval()` seconds via ffmpeg's fps+tile filters; images degenerate to a 1-frame sprite through the same code path (no branching)
  waveform.py           # waveform peak extraction for the timeline AUDIO row (added 2026-07-22, audio batch 5): `downsample_pcm16` (pure, max-abs-per-bucket PCM downsampling) + `peaks_for_media` (ffmpeg PCM decode, cached to `data/peaks/{media_id}.json`)
  transcribe.py          # faster-whisper wrapper -> CaptionWords (words_from_segments pure, transcribe_file lazy-loads CUDA model)
  caption_word_estimate.py # pure estimate_word_timings(word) -> list[CaptionWord]: splits a multi-word CaptionWord into per-word estimated sub-ranges, character-offset-proportional
  caption_layout.py     # pure paginate_words(words, measure, box_width_px, box_height_px, font_size_px, line_height=1.15) -> list[list[list[CaptionWord]]] (added 2026-07-24, caption box sizing): packs words onto lines by measured pixel width, paginates lines by box height, so caption rendering always fits the fixed box instead of a manual max-words-per-line count
  box_mask.py           # pure straight-line mask geometry (added 2026-07-29, box edge mask): mask_polygon(width, height, angle, offset, flip) -> clockwise polygon of the KEPT region in box-local px, Sutherland-Hodgman half-plane clip of the box rect; mirrored in static/box-mask.js, pinned by tests/test_box_mask_js.py
  mask_image.py         # export-side mask rasterization (added 2026-07-29, box edge mask): write_mask_png(path, width, height, angle, offset, flip) draws app/box_mask.py's kept-region polygon with Pillow as an RGBA PNG — opaque white inside, transparent outside — written next to the .ass sidecars for ffmpeg to alphaextract/alphamerge
  shape_render.py        # export-side shape rasterization (added 2026-07-31, vector shapes): write_shape_png(path, width, height, fill_color, opacity, corner_radius) draws a filled rounded-rect RGBA PNG with Pillow — alpha = round(opacity * 255) inside the rect (clamped corner_radius to min(width, height) / 2), 0 outside — written next to the .ass/mask sidecars for ffmpeg to overlay directly
  export_jobs.py         # in-memory export job registry: start_job/get_job/update_progress, jobs run on a background thread (real or injectable-sync in tests), not persisted
static/
  tool-mode.js       # window.ToolMode.{get, set, onChange} (added 2026-07-24, top-toolbar; extended 2026-07-31, shape-tool feature): DOM-free current-tool ("select"|"text"|"shape") state holder for the stage's active tool; no persistence, resets to "select" on reload
  ui-rail-tool-button.js # UI.railToolButton(container) (added 2026-07-30, selector-tool-rail): renders the Select tool button into #rail-tool (top of the left icon rail, sibling of #panel-nav), one .icon-rail-btn.icon-rail-btn-icon-only wired to window.ToolMode; Text/Shape have no counterpart here, see panel-nav.js's TEXT/SHAPE rail entries (remove-text-tool-top-bar, shape-tool feature)
  stage-tool-cursor.js # (added 2026-07-29, arrow-tool cursor) self-registered ToolMode subscriber that mirrors the active tool onto `#stage` as a `data-tool` attribute, so the stage's cursors are CSS state (stage.css) that follows a tool switch immediately instead of inline styles that would only update on the next re-render
  stage-click-router.js # (added 2026-07-24, top-toolbar) self-registered #stage click listener: in Text-tool mode, a click that isn't on an existing .text-block inserts a new text block (addTextBlockAndEdit(position)) centered at the click point (CanvasPoint.fromClient, canvas-point.js) then reverts ToolMode to "select" before the async insert resolves (so a second click mid-flight can't start a second insert); no-ops entirely in Select-tool and Shape-tool modes (Shape's own drag gesture is stage-shape-draw.js)
  canvas-point.js       # window.CanvasPoint.fromClient(clientX, clientY, rect) (added 2026-07-31, shape-tool feature): pure client-px -> 1080x1920 canvas-px conversion, clamped to canvas bounds; extracted from stage-click-router.js's former private canvasPointFromClient so stage-shape-draw.js can reuse the same conversion
  shape-draw-rect.js    # window.ShapeDragRect.fromPoints(p1, p2) (added 2026-07-31, shape-tool feature): pure geometry normalizing the Shape tool's two drag-corner canvas points into a top-left x/y + non-negative width/height rect regardless of drag direction, clamped to the 1080x1920 canvas
  stage-shape-draw.js   # drag-to-create for the Shape stage tool (window.ToolMode === "shape", added 2026-07-31, shape-tool feature): #stage mousedown with Shape armed tracks the drag via a live .shape-box preview div in #overlay; on mouseup, a drag under 8 canvas px in either dimension is a no-op (tool stays armed for another try), otherwise it reverts ToolMode to "select" (before the async work, mirroring stage-click-router.js's Text-tool ordering) then creates a ShapeLayer sized to the drawn rect with `start` set to the current playhead time (not the ShapeDefaults 0, so the shape's visible window begins where the user drew it rather than at the head of the reel), selects it, and opens the Shape panel; depends on CanvasPoint, ShapeDragRect, ShapePanel.createShapeAt()
  index.html         # editor page: the 3-column main (collapsible MEDIA panel, with a `#panel-brand` mark pinned to its bottom | center column: 9:16 stage filling height + timeline strip below it | selection-driven context panel: FILES/VIDEO/TEXT/CAPTIONS/SETTINGS/EXPORT); as of 2026-07-31 (transcript sidebar) the center column is further subdivided into `#stage-column` (wrapping `#stage` + `#transport`, carries the old centering styles) and sibling `#transcript-sidebar` (paragraph-style caption transcript, auto-scrolling live highlight of active sentence/word during playback, click-to-seek wiring), per the north-star mockup layout; the TEXT context-panel section (`#panel-text`) has a four-tab bar in order Style/Design/Box/Time (`UI.tabBar`, `static/ui-tab-bar.js`, replacing the old `UI.accordionSection` stack), each pane a mount point filled at wiring time by the shared style sections (`static/style-tab-*.js` -> `static/style-section-*.js`, added 2026-07-29/30, shared style sections batches 1-6) rather than hand-written markup — `#text-style-body`, `#text-font-body` and `#text-box-body` are empty (or near-empty, see the Box tab note below) `<div>`s in this file, and `#text-time-body` keeps its own markup for the single-panel `text-panel-time.js`; no side-panel heading field: the heading is edited directly on the stage (`static/ui-text-interaction.js`, added 2026-07-18). Design's body holds, in order, Font Family, Weight, SIZE/Italic/Underline/case, Color, Outline, Shadow, and Highlight — all shared `style-section-*.js` files, composed by `style-tab-design.js`; the Box tab's `#text-box-body` still hand-writes its Background and Border settings-rows (`text-panel-background.js`/`text-panel-border.js`, unaffected by the shared-sections work) ahead of the shared `#text-box-shared-body` mount (WIDTH/HEIGHT, TEXT ALIGN, POSITION, via `style-tab-box.js`); the Style tab (`#text-style-body`) is a single shared `style-tab-style.js` mount (the saved-style card grid); TIME holds start/end (`text-panel-time.js`); the `#panel-settings` context-panel section (added 2026-07-18) holds the `#theme-toggle` dark/light theme button (moved out of the now-removed `#topbar`, one labeled `.style-row` under an APPEARANCE `.style-group-label`) — its own SETTINGS entry in the left icon rail (`#panel-nav`) opens it; the `#panel-export` context-panel section (added 2026-07-18) holds the relocated `#export` button + `#export-result` output (same ids/wiring, moved out of the now-removed `#topbar`/`#stage-wrap`) — its own EXPORT entry in `#panel-nav` opens it, a pure location/IA change with no new export options. As of 2026-07-20 (projects/video-box/layers work): two more context-panel sections — `#panel-projects` (project list + "+ New Project", `panel-projects.js`), `#panel-video-box` (add-picker + trim/time/position/size fields, `panel-video-box.js`) — each with its own PROJECTS/VIDEO BOX entry in `#panel-nav`; `#panel-brand` now hosts the `#save-indicator` ("Saving…/Saved", `ui-save-indicator.js`); and a full-screen `#project-picker` overlay (`ui-project-picker.js`) shows at cold start when no valid `localStorage.projectId` exists. (That batch also added a `#panel-layers` LAYERS side-panel for z-order reordering; it and its LAYERS nav entry were removed when the timeline's TEXT/VIDEO BOX rows merged into one draggable overlay row — see "Unified overlay layer stack (z-order)" below.) As of 2026-07-21 (image/photo clips): the stage holds an `#image-player` `<img>` sibling to `#player` (`<video>`), hidden via `.stage-hidden` (see `preview.js`) when not the active clip's kind. As of 2026-07-22 (audio subsystem): one more context-panel section, `#panel-audio` ("ADD MUSIC" / volume+mute+replace+remove detail view, `panel-audio.js`) with its own AUDIO entry in `#panel-nav` (as of 2026-07-29, audio panel, clicking the timeline's AUDIO row no longer opens this panel — see below). As of 2026-07-29 (settings-row None state): the TEXT and CAPTIONS Box tabs each hold a Background settings row (`#text-box-background-row`/`#caption-box-background-row`) and a Border settings row (`#text-box-border-row`/`#caption-box-border-row`) instead of inline background/border fields, and the CAPTIONS Design tab gained a Highlight settings row (`#caption-highlight-row`, `#caption-highlight-body` is gone — the Highlight controls now live in the same `#caption-font-body` pane as FONT); five new drill-down subpanel divs back these rows: `#panel-text-background`, `#panel-text-border`, `#panel-captions-background`, `#panel-captions-border`, `#panel-captions-highlight`. As of 2026-07-29/30 (shared style sections, batches 1-6): the TEXT and CAPTIONS panels no longer hold duplicated control markup at all — every Design/Box/Style tab body is an empty (or near-empty, for the two Box tabs — see above) mount point, 15 of the original `text-panel-*.js`/`caption-panel-*.js` control files were replaced by shared `style-section-*.js`/`style-tab-*.js` files loaded via 21 contiguous `<script>` tags (`font-size-scale.js` through `style-tab-style.js`, in the master plan's fixed order), and every drill-down subpage is created at runtime by one shared `StylePanelHost` instead of being hand-written here. CAPTIONS' `#panel-captions-highlight` subpanel (`static/style-section-highlight.js`, shared with TEXT) now holds only the MARKER on/off toggle + color + radius; the karaoke MODE group moved to its own `#panel-captions-spotlight` subpanel (`static/style-section-spotlight.js`, CAPTIONS-only, added 2026-07-30) since a static box-background highlight and a per-word karaoke mode are different features that used to be bundled under one row/label. As of 2026-07-29 (box edge mask): both box panels gained a third Mask tab body (`#video-box-mask-body`/`#image-box-mask-body`) holding the mask toggle/ANGLE/OFFSET/Flip controls. As of 2026-07-29 (audio panel): the timeline's main clip row's on-screen label (`#label-video`) now reads `MAIN` instead of `VIDEO` — display text only, the row's key/id and every JS reference stay `video`/`row-video`/`label-video`; `#panel-audio` (ADD MUSIC background-music import) is unchanged. As of 2026-07-30 (AUTO tab relocation): the timeline audio track's automations (AUTO CAPTION + AUTO SILENCE) that briefly lived in their own standalone `#panel-audio-track` context-panel section (with an AUTO rail entry) moved into `#panel-video` as a third "Auto" tab alongside Design/Time — `#panel-video`'s existing content (header, name, tab bar, all three tab bodies, delete button) is now wrapped in a sibling `#video-main` div so the language drill-down subpage (`#video-audio-language`, renamed from `#panel-audio-track-language`) can hide/show it the same way TEXT/CAPTIONS' drill-downs hide their own `-main` wrapper; `#video-auto-body` holds the AUTO CAPTION/AUTO SILENCE markup verbatim (same ids: `#audio-language-row`, `#audio-auto-caption-btn`, `#auto-slice-*`). The standalone `#panel-audio-track` section, its own internal (now-redundant single-tab) tab bar, and the AUTO rail entry are gone; clicking the timeline's AUDIO row now opens the music-import `#panel-audio` again (`openAudioPanel()`), not this Auto content.
  editor.js           # UI state + API calls + DOM wiring (thin); owns the client-side TextPreset stand-in, MEDIA panel collapse state, and selection state driving which context-panel section (VIDEO/TEXT/CAPTIONS) is open; also wires transport step-back/step-forward/restart buttons and global keyboard shortcuts (Left/Right nudge 0.1s, Up toggles play/pause, Down restarts, Delete removes the selected clip when `selected.type === "video"` or calls `deleteSelectedTextBlock()` when `selected.type === "text"`; no text-block-duplicate feature either — Ctrl/Cmd+D and `duplicateTextBlock()` were removed 2026-07-30 alongside the TEXT panel's Duplicate button, same reasoning as panel-video.js's earlier clip-duplicate removal; V/T/P/F/C/S/E, added 2026-07-31 rail-nav-tooltips, mirror the left icon rail's Select/Text/Projects/Files/Captions/Settings/Export entries — `V`/`T` call `ToolMode.set("select")`/`navOnSelect("text")` (panel-nav.js), the rest call `navOnSelect(value)` directly; `+`/`-` and `G`, added 2026-07-31 timeline-toolbar-tooltips, mirror the timeline toolbar's Zoom in/Zoom out/safe-zone-guide buttons — each just calls `.click()` on the existing `#zoom-in`/`#zoom-out`/`#safe-zones-toggle` button rather than duplicating their handlers (zoomIn/zoomOut live inside timeline.js's own closure, not exposed on `window.Timeline`); all gated `!mod` so Ctrl/Cmd+P/F/S/C/V/G/+/- keep their native browser behavior), skipped while focus is in an input/textarea/select; drives a requestAnimationFrame loop (Timeline.tick) while the video is playing so the playhead moves smoothly instead of only updating on the low-frequency timeupdate event; also owns the multi-project flow (cold-start Api.ensureProject -> openProject or showPickerScreen, PROJECTS panel, save indicator) and playhead-grip scrubbing; TEXT-panel orchestration (renderTextPanel/ensureTextPreset/currentTextBlock/etc.) extracted to panel-text.js 2026-07-21 (583 lines, down from 762); CAPTIONS-panel orchestration and clip-sequence helpers further extracted to panel-captions.js/clip-sequence.js 2026-07-21 (398 lines, down from 583) — the stray `clampTrim()`/`video-order-divider` wiring (neither owned by captions or clip-sequence) moved to panel-video.js in the same pass, since that's their sole consumer — no behavior change; right-panel navigation (`showPanel`/`onTimelineSelect`/`openXPanel`/`reRenderAfterRestore`/`PANEL_NAV_ITEMS`/`PANEL_NAV_HANDLERS`) extracted to panel-nav.js 2026-07-21, pure move; undo/redo (added 2026-07-21, Task 2 of the undo-redo plan, consuming `static/undo-history.js`'s `window.UndoHistory.{record,undo,redo,reset,_debug}`): `saveProject(recordHistory = true)` records the *previous* `lastSavedJson` snapshot into `UndoHistory` before overwriting it (module var `lastSavedJson`, the undo baseline); `openProject` calls `UndoHistory.reset()` and reseeds `lastSavedJson` right after loading, so history never crosses projects; `applyRestore(json)` parses a snapshot back into `project`, calls `reRenderAfterRestore()` (via the now-moved panel-nav.js, rebuilds MediaPanel/Preview/timeline, then re-opens the previously-selected panel — video/video-box/text/captions handled explicitly, other types via `PANEL_NAV_HANDLERS`, falling back to `openFilesPanel()` if the selected entity no longer exists), then `saveProject(false)` to persist without re-recording; `undoEdit()`/`redoEdit()` wrap `UndoHistory.undo`/`redo` with `applyRestore`; the keydown handler's existing input/textarea/select/contentEditable focus guard gates Ctrl/Cmd+Z (undo) and Ctrl/Cmd+Y or Ctrl/Cmd+Shift+Z (redo), added before the arrow-key branches
  panel-text.js         # TEXT context-panel section: FONT/STYLES/BOX/TIME tab-bar orchestration (UI.tabBar, replacing the old per-section accordions) for the selected text block (renderTextPanel/renderBoxTab), preset defaulting (defaultTextPreset/ensureTextPreset), multi-block selection (currentTextBlock/selectTextBlock/addTextBlock/addTextBlockAndEdit/deleteSelectedTextBlock), and stage resize/move handlers (stageScale/handleBoxResize/handleBoxResizeEnd/handleBoxMove/handleBoxMoveEnd); extracted from editor.js 2026-07-21, pure move, zero behavior change
  panel-captions.js     # CAPTIONS context-panel section: caption track/preset creation (defaultCaptionPreset/ensureCaptionPreset/ensureCaptionTrack) — defaultCaptionPreset now always creates a fixed 900x350 box (box_width_mode/box_height_mode="fixed", added 2026-07-24 caption box sizing, replacing the old max_words_per_line=4 default), and ensureCaptionPreset self-heals any preset that isn't already fixed-size-valid (old saved projects) to the same defaults; defaultCaptionPreset also seeds the `spotlight_*` fields (added 2026-07-30, spotlight per-word styles: `spotlight_color`, `spotlight_outline_color`/`spotlight_outline_px`, `spotlight_shadow`/`spotlight_shadow_color`/`spotlight_shadow_offset_x`/`_offset_y`/`_blur`, `spotlight_highlight`/`spotlight_highlight_color`/`spotlight_highlight_border_radius`) and ensureCaptionPreset gained a second self-heal for the same change: a preset whose `highlight_mode` is still the legacy `"background"` value is migrated to `highlight_mode = "current_word"` with `spotlight_highlight = true` and `spotlight_highlight_color` seeded from the old `highlight_color`, preserving its old on-stage look under the new mode/field split; renderCaptionPanel orchestration, its tab-bar/divider wiring (UI.tabBar; tab order Closed captions/Filler words/Style/Design/Box — Closed captions first, added 2026-07-23; Design tab is one body again, `#caption-font-body` alone — it briefly grouped a separate FONT + HIGHLIGHT body pair, undone 2026-07-29 settings-row None state when Highlight became a settings row living inside the same body). As of 2026-07-29 (audio panel): panel-captions.js no longer owns transcription — `runAutoCaption()` and the Language settings row moved to the AUDIO panel's Auto tab (`static/audio-panel-auto-caption.js`/`static/audio-panel-language.js`); the Closed-captions tab body (`#caption-words-body`) now holds only the transcript word list (`caption-panel-words.js`). As of 2026-07-31 (auto-caption/filler-words panel move): transcription moved back into this panel's own tab — the "Filler words" tab is renamed "Auto" (`#caption-auto-body`, icon switched from `slice` to `sparkles` to match VIDEO panel's own Auto tab), and `renderCaptionPanel()` now calls `CaptionPanel.renderLanguage()` (`static/caption-panel-language.js`) alongside `CaptionPanel.renderFillerWords()`/`CaptionPanel.renderWords()`; see `static/caption-panel-auto-caption.js`/`caption-panel-language.js` and the "Captions & transcription" inventory entry below; as of 2026-07-31 (transcript sidebar) `renderCaptionPreview()` calls `TranscriptSidebar.render(project)` to keep the transcript sidebar in sync with caption edits.
  clip-sequence.js      # main VIDEO-track sequence mutation helpers: insertClipIntoSequence (split-and-insert at a drop point), stitchVideoBoxIntoSequence (video box -> sequence clip), importMedia() (multi-file import via native file picker, media-library only, no timeline auto-insert; renamed from addClip() 2026-07-22, media-import-multi-select-sort feature; delegates to Api.importMedia 2026-07-31, copy-on-import) + the #add-clip listener; appendMediaClipToSequence(m) (added 2026-07-24: appends a MediaItem as a new clip at the end of the VIDEO sequence, self-contained save/reload/re-render/auto-caption, wired to panel-media.js's plus icon); extracted from editor.js 2026-07-21, pure move, zero behavior change
  caption-clip-sync.js  # window.CaptionClipSync.{clipRanges, shiftCaptionsAfterEdit, resyncCaptionsAfterReorder} (added 2026-07-30, caption/clip sync): pure helpers keeping caption words aligned with the MAIN clip they overlap in time — clipRanges(clips) mirrors app/timeline.py's clip_starts walk; shiftCaptionsAfterEdit(words, editStart, oldDuration, newDuration) handles a delete or insert as one splice point (delete: newDuration=0, insert: oldDuration=0); resyncCaptionsAfterReorder(words, oldRanges, newRanges) shifts each word by its owning clip's own start delta, for drag-reorder's non-monotonic permutation. No new persisted fields — the clip/caption relationship is always recomputed from timestamps, never stored. Consumed by panel-video.js's deleteClip()/moveClipTo() and clip-sequence.js's insertClipIntoSequence()
  preview.js            # 9:16 stage playback + virtual clock + timeline seek (thin); delegates text-block overlay rendering to preview-text.js (window.PreviewText) and caption overlay rendering to preview-captions.js (window.PreviewCaptions) via thin wrappers, keeping window.Preview's public API unchanged (extracted 2026-07-21); transport row is 4 icon buttons (step-back/pause/play/step-forward); #overlay children (text blocks, caption block, video-box <video>s) each set CSS z-index from their model's z_index; applyFillModeClass(clip, el) toggles the given element's `.fill-mode-fill` class (stage.css) between FIT (object-fit: contain) and FILL (object-fit: cover) per ClipLayer.fill_mode. Image clips (added 2026-07-21, image/photo clips): `clipKind(c)`/`isImageActive()` look up `MediaItem.kind` via the loaded `mediaById` map; `playClipAt`/`seek` hide the active video element and show `#image-player` (an `<img>`, `static/index.html`) for image clips, driving timing via `window.ImageClipPlayback` (`static/image-clip-playback.js`) instead of the `<video>` element's `timeupdate`; `doPlay`/`doPause`/`isPaused`/`computeTimelineTime` all branch on `isImageActive()` to defer to `ImageClipPlayback` instead of the active player. Black-flash-free clip cuts (added 2026-07-24): two real `<video>` elements alternate as `activePlayer` (on-stage) / `standbyPlayer` (hidden) — `playerA` is `#player`, `playerB` is created in JS and appended as its sibling, both carrying a shared `.stage-media` class (`stage.css`) instead of per-id styling; `prepareStandby(nextIndex)` loads+seeks the next clip into `standbyPlayer` as soon as the current clip starts, and `playClipAt` swaps which element is active (class toggle only) instead of setting `.src` on the visible element — avoids the black frame a `<video>`'s "emptied" event causes on a `.src` change, which is what produced the flash between cuts; falls back to the old direct-`.src`-swap path if the standby element wasn't ready in time. `installPlayerListeners(el)` attaches the same timeupdate/play/pause handling to both elements, gated to whichever is currently `activePlayer`; since the active DOM node now changes over time, external callers no longer bind to `#player` directly — they subscribe via `Preview.onTimeUpdate(fn)`/`Preview.onPlayStateChange(fn)` (editor.js) or read `Preview.currentSourceTime()` for the active element's raw currentTime (panel-video.js's Set In/Out buttons). `load(project)` also rebinds the module's `textProject`/`textPresets` (the project every overlay repaint renders from) and paints the overlays once at t=0 (fixed 2026-07-29): those used to be assigned only by `renderText()`, so after an undo/redo — where `applyRestore()` (editor.js) reparses `project` into brand-new objects and calls `load()` — the next timeupdate/seek/virtual tick repainted the *pre-undo* text blocks, captions and video/image boxes over whatever fresh state a panel had just rendered, which is why the stage kept stale box positions (and a stale mask clip-path) while the model and side panel showed the restored values; as of 2026-07-31 (transcript sidebar) calls `TranscriptSidebar.render(project)` when loading so the sidebar renders alongside the main stage.
  preview-text.js        # window.PreviewText: stage text-block overlay rendering (renderText) + selection state (selectedTextBlockId/editingBlockId/editingDiv/boxResizeCallbacks/onStageTextActivate/activeFormatSelection) — extracted from preview.js 2026-07-21, pure move; also tracks each block's UI.textInteraction() return handle (Task 6, keyed by block id in `interactionHandles`, cleared/rebuilt each renderText() call) and exposes enterEditMode(blockId) to drop a newly-created block straight into on-stage edit mode; each rendered `.text-block` div carries `dataset.blockId`, and `getBoxSizeCanvasPx(blockId)` (added 2026-07-22, positioning-rules) reads that block's live rendered size back in 1080x1920 canvas px for the POSITION single-row icon anchor shortcut. Case styling (added 2026-07-24, preset.text_case): the div's DOM text stays exactly as-typed (contentEditable writes it back to block.heading on edit-end) — case is displayed via `div.style.textTransform = TextCase.cssValue(preset.text_case)`; there is no font-size-fitting use of `TextCase.apply()` (the old FILL auto-refit path was deleted 2026-07-30, fit/fixed auto-freeze)
  preview-captions.js    # window.PreviewCaptions: stateless caption overlay rendering — extracted from preview.js 2026-07-21, pure move; renderCaptions() paginates project.captions.words via CaptionLayout.paginateWords (added 2026-07-24, caption box sizing, replacing the old flat groupWords/activeCaptionGroup chunking) against the caption box's fixed size, finds the page active at the current timelineTime, and renders one `.caption-block` div containing one `.caption-line` div per line, memoized per (words, box size, font); getBoxSizeCanvasPx() (added 2026-07-22, positioning-rules) reads the rendered `.caption-block`'s size back in 1080x1920 canvas px, same purpose as preview-text.js's. Case styling (added 2026-07-24, preset.text_case): same CSS-display/measured-transform split as preview-text.js — `div.style.textTransform` for display, the measurer handed to `CaptionLayout.paginateWords` wraps text through `TextCase.apply()`, pagination memo key includes `preset.text_case`; `track.words` themselves are never transformed. Highlight/Spotlight backgrounds (reworked 2026-07-30, spotlight per-word styles): each line's words render inside a `line-height: 1` inline-block wrapper — `preset.highlight` (an always-on background behind visible text) paints that wrapper's background, and the active word's own `spotlight_color`/`spotlight_outline_color`/`spotlight_outline_px`/`spotlight_shadow*`/`spotlight_highlight*` (per-word karaoke styling — color always applied when `highlight_mode !== "off"`, outline/shadow only in `"current_word"` since ASS's `\k` sweep can't carry them through `"progressive_fill"`, `spotlight_highlight` gating a background rect independent of mode) paint the active `<span>` the same tight way — both using the module's `HIGHLIGHT_PAD_EM` constant applied equally on all 4 sides, mirroring `app/ass_render.py`'s `HIGHLIGHT_PAD_EM`/`_active_word_highlight_dialogues`
  image-clip-playback.js # window.ImageClipPlayback.{start,resume,pause,stop,seekTo,isPlaying,getElapsed} (added 2026-07-21, image/photo clips): DOM-free per-clip virtual playback clock (requestAnimationFrame) for image clips, letting preview.js hand off between #player (<video>) and #image-player (<img>) without losing timing
  preview-audio.js       # window.PreviewAudio.{load,play,pause,seek} (added 2026-07-22, audio batch 4): one module-level <audio> element synced to the timeline clock for Project.music playback; preview.js calls applyClipAudio(clip) (sets the active video element's volume/muted, clamped <=1.0) at every clip-switch point and PreviewAudio.{load,play,pause,seek} at load/doPlay/doPause/doRestart/seek/sequence-end
  transcript-sentences.js # window.TranscriptSentences.groupBySentence(words) -> [{start, end, words}] (added 2026-07-31, transcript sidebar): pure sentence-grouping helper, splits a flat CaptionWord[] list into sentences by punctuation (./!/?), a trailing run with no terminal punctuation still forms a final group; consumed by transcript-sidebar.js
  transcript-sidebar.js   # window.TranscriptSidebar.render(project) (added 2026-07-31, transcript sidebar): renders `#transcript-sidebar` div (one `.transcript-sentence` div per sentence, one `.transcript-word` span per word), self-registers onto `Preview.onTimeUpdate` for live highlight/auto-scroll during playback, word click calls `Preview.seek(word.t_start)`. Called from `preview.js`'s `load()` and `panel-captions.js`'s `renderCaptionPreview()`.
  timeline.js            # Timeline strip: toolbar/ruler/playhead/playhead-handle box/CAPTIONS/VIDEO/AUDIO rows plus one merged overlays row (`#row-overlays`/`#label-overlays`, unified overlay layer stack, see below), pure row-position math; zoomable scale showing a `visibleSeconds` window across #timeline-scroll's width (zoom −/+ toolbar buttons, 10s steps); auto-fits `visibleSeconds` to the video clips' own timeline length on every render (`fitVisibleSeconds(videoDuration(project))`, added 2026-07-23, changed 2026-07-29 to fit video length only instead of the project's overall max duration — so a caption/text/video-box layer running longer than the video no longer leaves blank timeline space in the default view) unless the user has manually zoomed (`manualZoom` flag, set by zoomIn/zoomOut, cleared by resetZoom on project open) — so adding clips grows the visible window automatically until manual zoom takes over; render()'s 5th `actions = {}` param (`onAddClip`/`onAddText`/`onSelectAudio`, Task 6 2026-07-21 + audio batch 7 2026-07-22) renders a small dashed "+" button (`.row-add-btn`) after the VIDEO row's clip sequence / overlays row's last lane (at x=0 when empty) as a visible way to add a clip/text block, and clicking the AUDIO row opens the AUDIO (Auto) panel (`#panel-audio-track` via `openAudioTrackPanel()` as of 2026-07-29, audio panel — previously opened the music-import `#panel-audio`; see "Audio" below); the AUDIO row itself renders via TimelineAudioRow.render (see timeline-audio-row.js); VIDEO-row loop (added 2026-07-23, timeline thumbnails) now calls TimelineVideoRow.render (see timeline-video-row.js) for each clip whose media isn't `kind === "audio"` — that row's on-screen label reads MAIN as of 2026-07-29 (audio panel; row key/id stays `video`). `renderOverlaysRow(project, px, selected, onSelect)` (unified overlay layer stack, replacing the old separate TEXT row and VIDEO BOX row) renders one 44px lane per text block or video box — via `OverlayLayers.mergedEntries(project)` (see timeline-overlay-layers.js) — each lane showing its item exactly as before (text block keeps its resize handle, video box keeps its drag-to-timeline-insert wiring) plus a "TEXT"/"VIDEO BOX" label and a hover-reveal drag grip (`.overlay-lane-handle`, see timeline-overlay-layer-drag.js); `setRowVisible("overlays", ...)` replaces the old separate `setRowVisible("text", ...)`/`setRowVisible("videobox", ...)` calls; CAPTIONS stays its own separate, fixed, non-draggable row (not part of the overlay stack, matching the old Layers panel's scope). As of 2026-07-31 (locked overlay layers): each overlay lane shows a "lock" icon in place of its drag grip when `entry.item.locked`, and `ensureFixedRowLockIcons()` prepends a static non-interactive "lock" icon to the MAIN and AUDIO row labels since those rows were never reorderable to begin with.
  timeline-audio-row.js  # window.TimelineAudioRow.render(project, pxPerSec, onReady) (added 2026-07-22, audio batch 5): real per-clip waveforms (canvas per clip, trimmed to in/out, scaled to zoom) + the music track's waveform layered behind them in the same AUDIO row, replacing the old static dummy pseudo-random bars; peaks fetched via Api.getMediaPeaks and cached client-side (peaksCache), fetched once per media id
  timeline-video-row.js  # window.TimelineVideoRow.render(blockDiv, clip, media, px, onReady) (added 2026-07-23, timeline thumbnails; reworked 2026-07-24 continuous-filmstrip fix): draws a per-clip `<canvas class="video-clip-filmstrip">` inside each VIDEO-row timeline block — full-block-height 9:16 tiles on a GLOBAL row grid (Filmstrip.tilesForBlock, so slice boundaries never shift/clip thumbnails and any-width blocks show their cropped tile window instead of the old <1-tile stripe fallback); mirrors timeline-audio-row.js's fetch-once-cache-client-side pattern — a clip whose filmstrip hasn't loaded (or failed) keeps the existing striped CSS placeholder
  caption-word-estimate.js # Timeline.estimateWordTimings(word): pure, mirrors app/caption_word_estimate.py — splits a multi-word CaptionWord into per-word estimated sub-ranges
  caption-layout.js     # window.CaptionLayout.paginateWords(words, measureFn, boxWidthPx, boxHeightPx, fontSizePx, lineHeightEm=1.15) (added 2026-07-24, caption box sizing): pure JS mirror of app/caption_layout.py's paginate_words, same page/line/word output shape; depends on Timeline.estimateWordTimings (loaded after caption-word-estimate.js), consumed by preview-captions.js at render time
  box-mask.js           # window.BoxMask.{maskPolygon, clipPath} (added 2026-07-29, box edge mask): exact JS mirror of app/box_mask.py's mask_polygon, plus clipPath(box) formatting the polygon as a percentage-based CSS clip-path value; consumed by video-box-preview.js/image-box-preview.js
  filler-word-ranges.js  # window.FillerWords.{normalizeWord, detectRanges} (added 2026-07-23): pure JS mirror of app/auto_slice.py's normalize_word/detect_filler_ranges (Unicode-aware punctuation stripping via \p{L}/\p{N}, so non-ASCII filler words like Danish "øh" normalize the same on both sides); consumed by caption-panel-filler-words.js's Auto-remove button
  filmstrip-layout.js     # window.Filmstrip.{frameInterval, frameCount, tilesForBlock, FRAME_W, FRAME_H} (added 2026-07-23, timeline thumbnails): pure JS mirror of app/filmstrip.py layout math plus tilesForBlock (frontend-only, added 2026-07-24 continuous-filmstrip fix): global-row-grid tile positions + nearest-frame indices for one clip block, consumed by timeline-video-row.js
  timeline-snap.js        # Timeline.snapTime/collectBoundaries (added 2026-07-20) — pure, dependency-free snap-to-boundary helpers attached onto window.Timeline; loaded after timeline.js, before editor.js
  timeline-clip-drag.js  # Drag-to-reorder for VIDEO-row clip blocks: mousedown+threshold-classify+transform-follow+snap-to-boundary drop indicator, calling VideoPanel.moveClipTo(clipId, newIndex) on drop
  timeline-overlay-layers.js # window.OverlayLayers.{mergedEntries, renumber} (unified overlay layer stack, added 2026-07-24): pure helpers — mergedEntries(project) merges text blocks + video boxes into one list sorted by z_index descending, renumber(entries) reassigns z_index by array position; consumed by timeline.js's renderOverlaysRow and timeline-overlay-layer-drag.js
  timeline-overlay-layer-drag.js # drag-to-reorder gesture for #row-overlays lanes (unified overlay layer stack, added 2026-07-24): mousedown on a lane's hover-reveal grip (`.overlay-lane-handle`) + vertical drag past a threshold reorders that lane among all overlay lanes; on drop calls OverlayLayers.renumber then saves and re-renders — supersedes the old panel-layers.js side panel. As of 2026-07-31 (locked overlay layers): a plain click (no drag movement) on a lane's handle toggles that entry's `locked` field instead, and drag-follow/reorder is suppressed entirely for a locked lane, so dragging a locked lane past the threshold and releasing does nothing rather than silently unlocking it.
  undo-history.js        # UndoHistory.record/undo/redo/reset/_debug (added 2026-07-21) — pure per-session two-stack (cap 50, dedupe) snapshot state machine over whole-project JSON strings; no DOM/fetch; loaded after timeline-snap.js, before editor.js
  font-fit.js            # window.FontFit.canvasMeasurer only — shared canvas text measurer, used by caption pagination (preview-captions.js); wrapText/fitFontSize deleted 2026-07-30, fit/fixed auto-freeze
  video-box-preview.js   # stage preview for video-box (PiP) layers: one <video> per visible box in #overlay, drag/resize wiring, muted always
  image-box-preview.js   # stage preview for image-box (PiP) layers (image/photo PiP feature): one <img> per visible box in #overlay, drag/resize wiring, no playback sync needed (static image) — mirrors video-box-preview.js; window.ImageBoxPreview.{render, setSelectedImageBox, setOnActivate}
  shape-preview.js       # stage preview for shape (vector overlay) layers (added 2026-07-31, vector shapes): one <div class="shape-box"> per visible shape in #overlay, drag/resize wiring (free-form, no aspect lock), mirrors image-box-preview.js minus mask-guide handling; window.ShapePreview.{render, setSelectedShape, setOnActivate}
  panel-projects.js      # PROJECTS context-panel section: project list (open/rename/delete/duplicate) + "+ New Project"
  box-panel-size-position.js # shared Box-tab SIZE + POSITION fields for VIDEO BOX/IMAGE BOX (added 2026-07-30, box-tab-common-component): window.BoxSizePositionPanel.render(container, box, {onChange, getNaturalSize}) — WIDTH/HEIGHT (aspect-ratio-locked) + X/Y number fields, plus SIZE's two one-shot action icons Maximize (scale up to the largest size that fits the 1080x1920 canvas at the current aspect ratio, centered) and Minimize (shrink to the source media's native pixel size via the caller-supplied async getNaturalSize() probe, scaled down to fit the canvas if larger — never scaled up; position untouched). Rebuilds `container` fresh each call — callers invoke it once per box selection, never from inside a field's own onChange (that was the pre-existing bug where typing a digit rebuilt the whole Box tab body mid-keystroke and kicked focus out of the input); onChange() must only save + re-render the stage preview/timeline. Replaces the duplicated X/Y/WIDTH/HEIGHT markup+logic previously hand-written in panel-video-box.js/panel-image-box.js
  panel-video-box.js     # VIDEO BOX context-panel section: add-from-media-library picker, time/position/size fields, delete; the detail view is split into Box (SIZE + POSITION, via the shared BoxSizePositionPanel above), Time (START) and Mask (EDGE MASK, added 2026-07-29 box edge mask) tab panes via UI.tabBar (Box default, added 2026-07-21), Delete an always-visible footer. No TRIM (IN/OUT) controls — removed 2026-07-30, box-tab-common-component; VideoBoxLayer.in_point/out_point still exist and still drive playback/export, just no longer user-editable here
  panel-image-box.js     # IMAGE BOX context-panel section (image/photo PiP feature): add-from-media-library picker (images only), size/position fields (Box tab, via the shared BoxSizePositionPanel above) + START/DURATION fields (Time tab, no in/out trim — images have no source timeline) + Mask (EDGE MASK, added 2026-07-29 box edge mask), delete; window.ImageBoxPanel.render(selectedId); window.ImageBoxPanel.createImageBox(mediaItem) (added 2026-07-24: pushes a new ImageBoxLayer into project.image_boxes and returns it, no save/render — caller's responsibility; reused by panel-media.js's plus icon); mirrors panel-video-box.js
  panel-shape.js         # SHAPE context-panel section (added 2026-07-31, vector shapes): no add-from-media picker (a shape isn't sourced from the media library) — Box (X/Y/WIDTH/HEIGHT), Style (fill color, opacity, corner radius), and Time (START/DURATION) tab panes, delete; window.ShapePanel.render(selectedId); window.ShapePanel.createShape() (pushes a new ShapeLayer into project.shapes and returns it, no save/render — caller's responsibility); mirrors panel-image-box.js minus the add-picker and Mask tab (predates the shared BoxSizePositionPanel refactor above — its own Box tab still hand-writes X/Y/WIDTH/HEIGHT, no aspect lock, since a shape resizes free-form)
  panel-audio.js         # AUDIO context-panel section (added 2026-07-22, audio batch 7): "ADD MUSIC" (native file picker filtered to audio via Api.pickFile("audio"), delegates to Api.importMedia("audio") 2026-07-31 copy-on-import) when Project.music is unset, else a detail view (name, VOLUME %, mute, Replace, Remove) against the single MusicTrack; one music track only (v1)
  panel-audio-track.js   # VIDEO panel's Auto tab content (renamed target 2026-07-30, AUTO tab relocation — file itself keeps its name, still about the timeline's audio-track automations): as of 2026-07-31 (auto-caption/filler-words panel move) this tab holds only AUTO SILENCE — AUTO CAPTION (Language row + Auto-caption button) moved into the CAPTIONS panel's own Auto tab, see `static/caption-panel-language.js`/`caption-panel-auto-caption.js` below — so `render()` is now just `ensureCaptionTrack(); AutoSlicePanel.render();`. `window.AudioTrackPanel.render()`, called by `panel-video.js` on every VIDEO-panel render. Not reachable from the icon rail; only via VIDEO's Auto tab
  caption-panel-language.js # CAPTIONS panel's Auto tab (renamed from `audio-panel-language.js` 2026-07-31, auto-caption/filler-words panel move — previously VIDEO panel's Auto tab, before that `caption-panel-language.js` pre-2026-07-29): transcription-language settings row (`#caption-language-row`) + drill-down subpanel (`#panel-captions-language`), writing `CaptionTrack.language` via `ensureCaptionTrack()`; `window.CaptionPanel.renderLanguage()`, called from `panel-captions.js`'s `renderCaptionPanel()`
  caption-panel-auto-caption.js # CAPTIONS panel's Auto tab (renamed from `audio-panel-auto-caption.js` 2026-07-31, auto-caption/filler-words panel move): the Auto-caption button (`#caption-auto-caption-btn`) + the global `runAutoCaption()`; also called by `clip-sequence.js`/`editor.js` when an audible clip is added to the sequence. As of 2026-07-31 (auto-caption progress) `runAutoCaption()` starts a background job via `Api.transcribeProject` and polls it with `CaptionTranscribeProgress.start` (live percentage in the button label) instead of a single blocking fetch; guards against a second concurrent run via `btn.disabled`
  panel-auto-slice.js    # detect-silence-and-filler-words flow (`Api.detectAutoSlice`/`Api.applyAutoSlice`): idle (run detection) -> results (approve which ranges to cut) -> confirm (final approval) -> apply; `window.AutoSlicePanel.render()`. Its markup lives inside `#panel-video`'s Auto tab (`#video-auto-body`, moved 2026-07-30 from the now-removed standalone `#panel-audio-track` section, itself moved from an even older dedicated AUTO SLICE rail entry)
  panel-media.js          # FILES/MEDIA context-panel section: media-library list (thumbnail, name, duration) filterable by type via an icon tab bar (All/Video/Images/Audio, `UI.tabBar`, mounted in `#files-type-tabs`, added 2026-07-30 files-icons-tab feature — replaces the earlier always-grouped VIDEOS/IMAGES/AUDIO section-label rows from the 2026-07-22 media-import-multi-select-sort feature), click-to-select (extracted from editor.js 2026-07-20); hover-revealed pencil icon opens inline rename (Task 4, 2026-07-20); a "no audio" icon shows for clips with no audio stream (`has_audio === false`) or, for video clips that do have a stream, when its cached waveform peaks are all silent (`checkSilentAudio`, via `Api.getMediaPeaks`, added 2026-07-23 — a stream can technically exist but carry no actual sound); a hover-revealed plus icon (added 2026-07-24) appends a video row to the VIDEO timeline sequence (`appendMediaClipToSequence`, clip-sequence.js, then re-renders this list so the row's trash button reflects its new usage count) or creates+selects an IMAGE BOX for an image row (`ImageBoxPanel.createImageBox`, panel-image-box.js, routed through `onTimelineSelect` so `selected` tracks the new box), or for an audio row sets/replaces `Project.music` with that file and opens the AUDIO panel via `openAudioPanel()` (mirrors `panel-audio.js`'s `addMusic()`/`replaceMusic()`, one music track only); the video branch (via `appendMediaClipToSequence`) and the audio branch (directly) both call `Preview.load(project)` so `PreviewAudio`'s internal state stays in sync with `Project.music` immediately, not just after a reload — the image branch has no audio state to sync
  panel-video.js          # VIDEO context-panel section: trim/order/delete for the selected clip (extracted from editor.js 2026-07-20); a UI.tabBar (added 2026-07-21) splits it into Design (FILL + SPEED + VOLUME, VOLUME added 2026-07-22 audio batch 6), Time (TRIM + ORDER), and Auto (AUTO SILENCE only as of 2026-07-31, auto-caption/filler-words panel move — AUTO CAPTION moved to the CAPTIONS panel's own Auto tab; was AUTO CAPTION + AUTO SILENCE 2026-07-30 through 2026-07-31, see `panel-audio-track.js`) panes (Design default), with Delete as an always-visible footer below the panes (Duplicate removed 2026-07-30 — duplicateClip() didn't shift/sync captions the way delete/move/insert now do; see caption-clip-sync.js); also hosts the shared clampTrim() helper and the VIDEO-panel's TRIM/ORDER divider wiring, moved here from editor.js 2026-07-21 since panel-video.js/panel-video-box.js are its only consumers. `render(c)` calls `AudioTrackPanel.render()` on every render so the Auto tab's project-level content (the auto-slice flow) stays in sync alongside the per-clip fields
  panel-text.js           # TEXT context-panel section: FONT/STYLES/BOX/TIME tab-bar orchestration (UI.tabBar, replacing the old per-section accordions) + stage resize/move handlers (extracted from editor.js 2026-07-21); addTextBlockAndEdit() (wired to both the empty-state "+ Add text" button and the timeline's TEXT-row + button, Task 6 2026-07-21) creates the block, opens the panel, and calls Preview.enterTextEditMode(block.id) so the new block is immediately live-editable (contentEditable) on the stage
  panel-nav.js           # right-panel navigation extracted from editor.js (2026-07-21): showPanel(), onTimelineSelect() timeline-click routing, PANEL_NAV_ITEMS (left icon rail — as of 2026-07-31 shape-tool feature: PROJECTS, FILES, TEXT, SHAPE, CAPTIONS, SETTINGS, EXPORT, PROJECTS first as of 2026-07-29; the TEXT/CAPTIONS blue "+" badges were removed 2026-07-30 along with ui-icon-rail.js's badge option, even though both entries still insert rather than merely toggle a panel. VIDEO/IMAGE/AUDIO rail entries — added 2026-07-22 audio batch 7 for AUDIO — were removed 2026-07-30 (those panels are already reachable from the FILES panel's per-row add icons, see panel-media.js); the AUTO entry added 2026-07-29 audio panel was separately removed 2026-07-30 AUTO tab relocation — that content now lives in VIDEO's Auto tab, see panel-video.js. `openVideoBoxPanel`/`openImageBoxPanel`/`openAudioPanel`/`PANEL_NAV_HANDLERS`' video-box/image-box/audio entries are unaffected by the rail removal — those panels are still opened programmatically, just not from a rail button; each `PANEL_NAV_TOP_ITEMS`/`PANEL_NAV_BOTTOM_ITEMS` entry carries a `title`/`shortcut` pair as of 2026-07-31 rail-nav-tooltips — see `ui-icon-rail.js`), every openXPanel(), reRenderAfterRestore() (post-undo/redo), and the UI.iconRail() wiring (Task 2, 2026-07-21, extended 2026-07-31 shape-tool feature: the rail's `navOnSelect` special-cases `value === "text"` and `value === "shape"` to arm `ToolMode.set(value)` instead of opening a panel via `PANEL_NAV_HANDLERS` — see "Toolbar & tool modes" below for the full drag-to-create/mutual-exclusion behavior; CAPTIONS needs no special case since openCaptionsPanel() already create-or-opens the track; opening an *existing* text block or shape still goes through onTimelineSelect via a timeline/stage click) — classic script sharing editor.js's project/selected/renderTimeline/saveProject globals at call time, loaded immediately before editor.js
  panel-export.js        # EXPORT context-panel section: FILENAME text input + QUALITY (HIGH/MEDIUM) button group, above the export button (added 2026-07-20)
  panel-settings.js        # SETTINGS context-panel header wiring only (added 2026-07-30, context-panel-header feature) — the panel's one control (theme toggle) stays wired inline in editor.js; this file exists solely to give SETTINGS the same UI.contextPanelHeader treatment every other context panel has
  export-progress.js     # ExportProgress.start(jobId, {onDone, onFailed}): polls Api.exportStatus every 500ms, drives #panel-export's progress bar
  caption-transcribe-progress.js # window.CaptionTranscribeProgress.start(jobId, {onDone, onFailed}) (added 2026-07-31, auto-caption progress): polls a background transcription job every 500ms and writes the live percentage into the Auto-caption button's .button-label span (no progress-bar element, unlike export's poller)
  login.html              # standalone login page (added 2026-07-23, cloud hosting) served at GET /login — password field posting to POST /login, not part of the index.html SPA
  login.js                # login.html's script: shows #login-error when redirected back with ?error=1 (added 2026-07-23, cloud hosting)
  ui-icon-rail.js         # UI.iconRail: left-panel icon rail nav, single-select; items may carry optional `title`/`shortcut` (added 2026-07-31, rail-nav-tooltips) setting a native `title` + `data-tooltip-shortcut`, auto-upgraded by ui-tooltip.js's MutationObserver into the app's styled tooltip
  ui-button.js             # UI.button(container, {label, icon, size, intent, pressed, disabled, onClick}) (rebuilt Task 14, batch 3): builds a whole <button> element (2 sizes "sm"/"md" x 4 intents "neutral"/"accent"/"danger"/"dashed") and appends it to container, rather than stamping a variant onto a caller-built element — every button in the app (panel actions, icon-only controls, toolbar/timeline/number-field steppers) is built through this one function, hydrated from `[data-button]` placeholders in index.html for markup-only sites (see index.html's hydration script) or called directly from JS-created sites; the label span carries a `.button-label` class (added final review finding 2) so callers can target it in CSS (e.g. style-panel.css hiding it when the left panel collapses)
  ui-icon.js               # UI.icon(name, {size = 24}) -> SVG markup string (added UI-component-consistency initiative): the single home for every Lucide icon path used across the app, keyed by kebab-case Lucide name in its ICON_PATHS map; throws on an unrecognized name. Replaces every hand-inlined `<svg>` in markup and JS — adding a new icon means adding a new ICON_PATHS entry here, never pasting `<path>` markup at the call site (see CLAUDE.md's Conventions section and tests/js/no-raw-svg.test.js, which forbids hand-inlined SVGs going forward)
  ui-text.js               # UI.text(container, {variant, content}) -> <span> (added UI-component-consistency initiative): builds the shared typography recipe for one of 4 variants — "eyebrow" (mono-caps section label), "label" (form-field label), "hint" (secondary/help text), "body" (default content text) — via text.css's `.text-*` classes; throws on an unrecognized variant
  ui-tooltip.js            # UI.tooltip(el, {label, shortcut, delay}) (added 2026-07-31, tooltip design refresh): one shared floating dark tooltip (label + optional monospace keyboard-shortcut chip), shown on hover/focus after a delay, replacing the browser's native title tooltip. UI.tooltip.observe(root) self-initializes on load (called against document.body from this file's own bottom, no other wiring needed) and runs a MutationObserver that auto-upgrades every element carrying a native `title` attribute — present now or added/set later by any static/*.js file — moving the text into the custom tooltip, mirroring it onto `aria-label`, and stripping the native `title` so no double tooltip shows; a `data-tooltip-shortcut` attribute (index.html's `[data-button]` hydration script passes it through onto the built button) supplies the optional shortcut chip text
  ui-list-row.js           # UI.listRow(el, {selected, subtle}): stamps shared clickable-row card/hover/selected styling (list-row.css) onto an existing element, applied variant-style like UI.button; used by PROJECTS/FILES clip rows/font-style drill-down lists (added 2026-07-22, list-row component — also fixed a bug where FILES' VIDEOS/IMAGES section-label rows incorrectly inherited card styling from a too-broad #clip-list li selector)
  ui-button-group.js       # UI.buttonGroup: single-select toggle-button row (ex ui-components.js, split per one-function-per-file convention)
  ui-number-field.js       # UI.numberField: labeled number input with unit suffix + custom stepper (ex ui-components.js)
  ui-color-swatch.js       # UI.colorSwatch: small square color-picker swatch with label (ex ui-components.js)
  ui-accordion.js          # UI.accordion: wires an existing header <button> + body <div> collapsible pair (ex ui-components.js)
  ui-accordion-section.js  # UI.accordionSection: builds an accordion header (title + chevron) for a body element, wired via UI.accordion
  ui-divider.js            # UI.divider: plain static 1px separator line
  ui-settings-row.js       # UI.settingsRow: clickable label/value/chevron row that opens a drill-down; optional swatchColor renders a small color square before the value text (added 2026-07-22, Outline subpage)
  ui-sub-panel-header.js   # UI.subPanelHeader: back-arrow + title header for drill-down sub-panels
  ui-context-panel-header.js # UI.contextPanelHeader(container, {icon, label}) (added 2026-07-30, context-panel-header feature; extended same day to all ten context panels): every `#panel-*`'s title row — a type icon + label, idempotent so a render() can call it every time (static-label panels call it once; VIDEO/VIDEO BOX/IMAGE BOX/AUDIO call it on every render() since their label is the selected item's file name, falling back to a static label like "Video" when nothing's selected). Icons match the left rail's icon for panels that have a rail entry (`panel-nav.js`); MAIN video clips use `clapperboard`, VIDEO BOX uses `picture-in-picture`, IMAGE BOX `image`, AUDIO `music` — chosen to stay visually distinct from each other. Wired from `panel-media.js`/`panel-text.js`/`panel-captions.js`/`panel-settings.js`/`panel-export.js`/`panel-projects.js` (static, one-time) and `panel-video.js`/`panel-video-box.js`/`panel-image-box.js`/`panel-audio.js` (dynamic, per render)
  settings-row-value.js    # window.SettingsRowValue.orNone(isOn, text) (added 2026-07-29, settings-row None state): single definition of the "None" label shown by a settings row whose effect is switched off (Outline/Shadow/Highlight/Background/Border)
  style-section-font-family.js # shared style section (added 2026-07-29, shared-style-sections batch 2): Font Family settings row + font-list drill-down subpage, serving both the TEXT and CAPTIONS Design tabs via a StyleTarget; replaces text-panel-font-family.js + caption-panel-font-family.js
  style-section-font-weight.js # shared style section (added 2026-07-29, batch 2): Weight settings row + weight-list drill-down subpage; render() is async (awaits Api.listFontWeights). Its `sampleText` option supplies the per-row preview text — TEXT passes the block's live heading, CAPTIONS a fixed sample, since a caption track has no single heading. Replaces text-panel-font-weight.js + caption-panel-font-weight.js
  style-section-size.js        # shared style section (added 2026-07-29, batch 3): the SIZE (PX) field + two font-size stepper buttons, using FontSizeScale.stepFontSizePreset for a single [12..96] scale on both panels (fixing a real bug where TEXT's old separate scale stopped at 56, so stepping up a fresh 96px block shrank it). `{ compactRow }` option adds the `.style-size-row--compact` modifier (TEXT only — CAPTIONS' size-row alignment divergence was raised and declined, so it's preserved, not converged). Construction-time control values are literal placeholders, not target.getFieldValue(...) — calling that before any text block/caption track exists throws, which is exactly what broke the TEXT panel until this was found and fixed (see panel-text.js's note). Replaces text-panel-font-style.js's SIZE portion + caption-panel-font-style.js's SIZE portion
  style-section-emphasis.js    # shared style section (added 2026-07-29, batch 3): Italic + Underline toggles (FormatRun-capable, target.setField) plus the lowercase/UPPERCASE/As-typed case group (target.setPresetField — text_case has no FormatRun override) on one row via .btn-group-inline, landing the case-buttons-inline divergence on CAPTIONS (previously its own un-inlined .style-group). Replaces text-panel-font-style.js's italic/underline portion + caption-panel-font-style.js's italic/underline portion + text-panel-case.js + caption-panel-case.js
  style-section-color.js       # shared style section (added 2026-07-29, batch 3): a settings row (swatch + hex) opening a drill-down subpage holding the color picker; FormatRun-capable via target.setField. `options.field` (default `"color"`, added 2026-07-30 spotlight per-word styles) lets a caller point the whole section at a different preset field — the CAPTIONS Spotlight subpage (style-section-spotlight.js) reuses this file as-is for `spotlight_color` instead of a new file. Replaces text-panel-font-style.js's Color portion + caption-panel-font-style.js's Color portion
  style-tab-design.js          # Design-tab composer (added 2026-07-29, batch 2; extended batch 3; extended 2026-07-30; align moved in 2026-07-30 text-align-subpage-move): StyleTab.design(container, target, {host, sampleText, compactSizeRow, spotlight}) -> {render()} builds the shared sections in the one fixed order both panels use (final order: fontFamily, fontWeight, size, emphasis, align, color, outline, shadow, highlight, [spotlight]; wrapped via the file's own sectionWrapper() helper — each section gets its own `.style-section` div). `opts.spotlight` (CAPTIONS-only) appends style-section-spotlight.js's per-word karaoke section after highlight — TEXT has no per-word karaoke, so it never renders. style-section-highlight.js itself is identical for both panels (2026-07-30: dropped its old `modes` option, which used to bundle CAPTIONS' karaoke MODE group into the same "Highlight" row — split into style-section-spotlight.js). As of the spotlight per-word style overrides feature (2026-07-30), the two no longer share any fields either — Spotlight's rect uses its own spotlight_highlight*/spotlight_outline*/spotlight_shadow*/spotlight_color set, independent of this row's highlight_color/highlight_border_radius. render() returns a promise because fontWeight's does. TEXT ALIGN (style-section-align.js) moved here from the Box tab (2026-07-30, text-align-subpage-move) so it sits beside the other text-shaping controls (emphasis/case) instead of beside box sizing/position.
  ui-save-indicator.js     # UI.saveIndicator: "Saving…/Saved/Save failed — retry" label in #panel-brand (the grey status dot was removed 2026-07-29 — label only), setSaving()/setSaved()/setFailed(retryFn); in-flight-save counting + 2s fade-out on Saved (added 2026-07-20)
  ui-project-list-row.js   # UI.projectListRow: one project row (inline-editable name, meta, optional duplicate/delete) — shared by picker + PROJECTS panel
  ui-project-picker.js     # UI.projectPicker: full-screen cold-start project picker (list + "+ NEW PROJECT")
  ui-resize-handles.js    # generic 8-handle drag-resize overlay for any positioned element (text box + video box)
  ui-text-interaction.js  # click-to-edit (contentEditable) vs drag-to-move vs drag-to-select for a stage text block; returns `{ enterEditMode, exitEditMode }` (Task 6, 2026-07-21; exitEditMode added 2026-07-29) so a caller can programmatically enter edit mode, not just on user click, and close an edit that no blur will ever end (a stage click on another block never moves focus — the box-move branch preventDefaults its mousedown)
  ui-text-selection.js    # UI.textSelectionOffsets/rangeContainsPoint: native selection -> char offsets + glyph hit-test
  ui-video-box-drag.js    # UI.videoBoxDrag: drag-to-move for stage video boxes (no edit mode — any drag is a move)
  ui-mask-line-drag.js    # UI.maskLineDrag (added 2026-07-29, box edge mask): draggable SVG cut-line guide over a selected masked box's on-stage rect — drag the line to shift mask_offset, drag the round end handle to change mask_angle; returns {render, destroy}
  ui-style-save-form.js    # UI.styleSaveForm: inline "save current style" form for TEXT/CAPTIONS Style tabs (name input + Save/Cancel buttons + overwrite hint), replacing the native prompt()
  api-save-project.js      # Api.saveProject: PUT /api/projects/{id}
  api-create-project.js    # Api.createProject: POST /api/projects
  api-list-projects.js     # Api.listProjects: GET /api/projects -> ProjectSummary[]
  api-delete-project.js    # Api.deleteProject: DELETE /api/projects/{id}
  api-duplicate-project.js # Api.duplicateProject: POST /api/projects/{id}/duplicate
  api-rename-project.js    # Api.renameProject: fetch-patch-PUT a project's name (fresh from disk, not the in-memory copy)
  api-ensure-project.js    # Api.ensureProject: resolve localStorage.projectId -> Project | null (cold-start routing)
  api-export-project.js    # Api.exportProject: POST /api/projects/{id}/export -> {ok, job_id} | error (starts a background export job)
  api-export-status.js     # Api.exportStatus: GET /api/exports/{job_id} -> {status, percent, output_path, error}
  api-transcribe-project.js # Api.transcribeProject(pid) (added 2026-07-31, auto-caption progress): POST /api/projects/{pid}/transcribe -> {job_id} (starts a background transcription job)
  api-transcribe-status.js  # Api.transcribeStatus(jobId) (added 2026-07-31, auto-caption progress): GET /api/transcribe-jobs/{jobId} -> {status, percent, output_path, error}
  api-pick-files.js        # Api.pickFiles: GET /api/pick-files -> string[] (native OS multi-select file dialog on the server, video+image only; used by media import — renamed from api-pick-file.js's Api.pickFile 2026-07-22, media-import-multi-select-sort feature)
  api-pick-file.js         # Api.pickFile(kind="video"): GET /api/pick-file?kind=... (native OS single-select file dialog on the server; kind="audio" filters to music files, added 2026-07-22 for AUDIO panel music import — a separate single-select function from api-pick-files.js's multi-select media import)
  api-get-media-peaks.js   # Api.getMediaPeaks(mediaId, filePath): GET /api/media/{id}/peaks -> number[] (added 2026-07-22, audio batch 5), [] on failure
  api-get-media-filmstrip.js # Api.getMediaFilmstrip(mediaId, filePath) -> Promise<string | null> (added 2026-07-23, timeline thumbnails): `GET /api/media/{id}/filmstrip?path=...`, mirrors api-get-media-thumbnail.js, returns an object-URL for the sprite-sheet JPEG blob or `null` on a failed fetch
  api-import-media.js      # Api.importMedia(projectId, paths, kind?) -> Promise<{project, imported} | null> (added 2026-07-31, copy-on-import): POST /api/projects/{pid}/import-media, copies source files into project's data dir, probes them, builds MediaItems with source_path for dedup
  api-probe-media.js       # Api.probeMedia: GET /api/probe -> {duration, has_audio} | null
  api-list-font-weights.js # Api.listFontWeights(fontName) -> Promise<{value, label}[]>: GET /api/fonts/{name}/weights
  api-list-presets.js     # Api.listPresets: GET /api/presets -> saved TextPreset[]
  api-save-preset.js      # Api.savePreset: POST /api/presets -> saves/updates a saved TextPreset
  api-delete-preset.js    # Api.deletePreset: DELETE /api/presets/{id}
  text-case.js               # window.TextCase.{apply(text, textCase), cssValue(textCase)} (added 2026-07-24, case styling): pure "none"/"upper"/"lower" transform — apply() for measurement inputs, cssValue() -> CSS text-transform value for display; mirrors app/text_case.py's apply_text_case
  text-panel-time.js         # TEXT panel Time tab: start/end
  text-panel-background.js   # TEXT panel Box tab: Background settings row + drill-down subpanel (on/off toggle + color + opacity), same row+subpanel pattern as the old text-panel-shadow.js (added 2026-07-29, settings-row None state)
  text-panel-border.js       # TEXT panel Box tab: Border settings row + drill-down subpanel (on/off toggle + width/radius/color); box_border_width === 0 is "no border", there is no box_border boolean (added 2026-07-29, settings-row None state)
  caption-panel-background.js   # CAPTIONS panel Box tab: Background settings row + drill-down subpanel, same pattern as text-panel-background.js but against the caption track's preset (added 2026-07-29, settings-row None state)
  caption-panel-border.js       # CAPTIONS panel Box tab: Border settings row + drill-down subpanel, same pattern as text-panel-border.js but against the caption track's preset (added 2026-07-29, settings-row None state)
  font-size-scale.js          # pure (added 2026-07-29, shared-style-sections batch 1): FontSizeScale.{FONT_SIZE_PRESETS, stepFontSizePreset(currentSize, direction)} — the one font-size step scale `[12,14,16,18,21,24,36,45,56,72,96]` both panels' SIZE steppers use; guarded dual export (window + module.exports) for node --test
  format-run-write.js         # pure (added 2026-07-29, batch 1): FormatRunWrite.upsertFormatRun(block, start, end, field, value) — the per-range FormatRun upsert behind TEXT's selection-aware writes
  shape-defaults.js           # pure (added 2026-07-31, vector shapes): ShapeDefaults.centeredShape() — the one place default field values for a newly-created shape live (300x300, centered, #4C6FFF, full opacity, no radius, 3s duration)
  shape-color.js              # pure (added 2026-07-31, vector shapes): ShapeColor.toRgba(hex, opacity) — hex+opacity -> CSS rgba() string, used for the stage's live fill
  style-fields.js             # pure (added 2026-07-29, batch 1): StyleFields.{STYLE_FIELD_NAMES, styleFieldsOf} — the one saved-style field list, including `highlight` (the field the old hand-copied caption-panel-style.js silently dropped, fixed in batch 6)
  style-target-text.js        # StyleTarget.forTextBlock(deps?) (added 2026-07-29, batch 1): the adapter every shared section writes through on the TEXT panel — {kind, supportsFormatRuns, getPreset, getFieldValue, setField, setFields, setPresetField, previewField, cancelPreview, clearFormatRuns, rerenderPreview, rerenderPanel, getBoxSize}; setField/setFields upsert a FormatRun when a stage selection on the current block is active (for a small allowlisted set of fields — font is deliberately excluded, per-range fonts were raised and declined), else fall back to a whole-preset write; setPresetField is always whole-preset. deps is optional and injectable, making the target unit-testable outside a browser (tests/js/style-target-text.test.js)
  style-target-caption.js     # StyleTarget.forCaptionTrack(deps?) (added 2026-07-29, batch 1): same object shape as style-target-text.js, but every write is whole-preset (a caption track has no per-range overrides) — setField/setPresetField are literally the same function, and clearFormatRuns() is a no-op — which is what lets one shared section serve both panels with no `if (target.kind === ...)` branch
  style-panel-host.js         # StylePanelHost(mainEl, drillEl) -> {page(title, buildBody, options?), closeAll()} (added 2026-07-29, batch 1): generic drill-down subpage manager, building each subpage's UI.subPanelHeader itself; page(...) returns {open(), close(), bodyEl}; replaces seven hand-copied openXPanel/closeXPanel pairs and the old per-control #panel-text-main/#panel-captions-main juggling
  style-section-outline.js    # shared style section (added 2026-07-29, batch 4): Outline settings row (swatch + "Npx", "None" when outline_px === 0) + drill-down subpanel (color + width fields), FormatRun-capable via target.setField. `options.colorField`/`widthField` (default `"outline_color"`/`"outline_px"`, added 2026-07-30 spotlight per-word styles) let a caller retarget both fields — style-section-spotlight.js reuses this file for `spotlight_outline_color`/`spotlight_outline_px`. Replaces text-panel-outline.js + caption-panel-outline.js
  style-section-shadow.js     # shared style section (added 2026-07-29, batch 4): Shadow settings row (swatch + "None" when off, else "X: <n>px  Y: <n>px  Blur: <n>px") + drill-down subpanel (on/off toggle + color/offset-x/offset-y/blur); whole-preset only, no FormatRun override. `options.fields` (an object of `{toggle, color, offsetX, offsetY, blur}` field names, default the base `shadow*` fields, added 2026-07-30 spotlight per-word styles) lets a caller retarget the whole group — style-section-spotlight.js reuses this file for the `spotlight_shadow*` fields. Replaces text-panel-shadow.js + caption-panel-shadow.js
  style-section-highlight.js  # shared style section (added 2026-07-29, batch 4; rewritten 2026-07-30 shared-style-sections batch 5 to drop its old `modes` option): Highlight settings row ("None" when off, else the color hex) + drill-down subpanel (on/off toggle + color + border-radius); identical for both panels — an always-on background rect behind the whole block/caption box (`preset.highlight`), independent of CAPTIONS' separate per-word karaoke feature (see style-section-spotlight.js, below). highlight/highlight_color are FormatRun-capable (target.setField); highlight_border_radius is not. `options.fields` (default `{toggle:"highlight", color:"highlight_color", radius:"highlight_border_radius"}`, added 2026-07-30 spotlight per-word styles) lets a caller retarget the group — style-section-spotlight.js reuses this file for the `spotlight_highlight*` fields (its own on/off marker for the per-word rect; no fields are shared between the two as of this feature). Replaces text-panel-highlight.js + caption-panel-highlight.js
  style-section-box.js        # Shared Box-tab section (added 2026-07-29, batch 5; SIZE mode toggle removed 2026-07-30, fit/fixed auto-freeze; "SIZE" eyebrow heading added 2026-07-30, box-tab-common-component, to match the VIDEO BOX/IMAGE BOX panels' new SIZE/POSITION heading pair): a "SIZE" label above WIDTH/HEIGHT, built once and refreshed via render() (the label hides alongside the fields when sizeModes hides them); `{ sizeModes }` is true for TEXT and false for CAPTIONS (always-fixed box, WIDTH/HEIGHT always visible) — a text block's fit-vs-fixed sizing is now driven automatically (see `panel-text.js`'s auto-freeze-on-blur, below), not picked by the user, so this section no longer renders a FIT/FREE/FILL button group. Background/Border are NOT built here — they keep their own settings-row + drill-down UI (text-panel-background.js/text-panel-border.js and the CAPTIONS equivalents), rendered as siblings of this section's mount inside the same Box tab body. Every write is target.setPresetField — no box field is FormatRun-capable
  style-section-align.js      # Shared Design-tab section (added 2026-07-29, batch 5; moved from the Box tab to the Design tab 2026-07-30, text-align-subpage-move): the TEXT ALIGN icon button group, styled via `.style-align-group` in style-panel.css (replacing the old #text-align-group/#caption-align-group id selectors); composed by style-tab-design.js, not style-tab-box.js
  style-section-position.js   # Shared Box-tab section (added 2026-07-29, batch 5; rewritten same day to match a same-day live UI change): HORIZONTAL/VERTICAL pixel fields + a stateless single-row six-icon anchor shortcut (top/mid-h/btm/left/mid-v/right, not a 3x3 grid), computing edge-flush values via panel-text.js's anchorPositionX(value, boxWidth, align)/anchorPositionY(value, boxHeight) against target.getBoxSize(), then target.rerenderPanel(); render() clears the group's aria-pressed so no cell stays lit
  style-tab-box.js            # Box-tab composer (added 2026-07-29, batch 5; align section moved out 2026-07-30, text-align-subpage-move — see style-tab-design.js): StyleTab.box(container, target, { sizeModes }) renders the box and position sections in that fixed order into one tab body, owning the divider between them
  style-section-preset-library.js # shared style section (added 2026-07-30, batch 6): StyleSection.presetLibrary(container, target, {}) -> {render()}, the saved-style card grid — "+ Save current style" button + inline UI.styleSaveForm, save-mode overwrite, per-card hover-delete via Api.deletePreset with no confirmation. Applying calls target.clearFormatRuns() (a no-op on the caption target) then bulk-assigns StyleFields.styleFieldsOf(saved) onto target.getPreset() and commits with one target.setPresetField() call. Replaces text-panel-style.js + caption-panel-style.js, and fixes a real bug: the old caption-side hand-copy of styleFieldsOf dropped the `highlight` field, so a saved caption style with MARKER on came back with MARKER off — the shared StyleFields.styleFieldsOf list includes it
  style-tab-style.js          # Style-tab composer (added 2026-07-30, batch 6): StyleTab.styleLibrary(container, target, {}) -> {render()}, a thin wrapper around StyleSection.presetLibrary (the Style tab holds exactly one section)
  style-section-spotlight.js # CAPTIONS-only Design-tab section (added 2026-07-30, reworked same day for per-word style overrides): "Spotlight" settings row (mode label + `spotlight_color` swatch, blank swatch when the mode is Off) opening a drill-down whose body holds a MODE group `off`/`current_word`/`progressive_fill` (`preset.highlight_mode`, replacing the old `off`/`current_word`/`progressive_fill`/`background` set — "background" is now legacy-only, self-healed on load, see panel-captions.js) followed by the active word's own Color/Outline/Shadow/Highlight, built by reusing style-section-color.js/outline.js/shadow.js/highlight.js pointed at the preset's `spotlight_*` fields via each section's field-name options, each wrapped in its own `.style-section` div (`sectionWrapper()`) so their `:last-child` margin math stays correct with four side by side. A second, nested `StylePanelHost` (mainEl = this subpage's own body, drillEl a sibling appended to the subpage's outer element so the four reused sections' own subpages survive this body's rebuilds) gives Color/Outline/Shadow/Highlight their own chevron drill-downs without changing StylePanelHost itself. `syncVisibility()` hides Color/Highlight only in "off" mode; Outline/Shadow additionally hide in "progressive_fill" (ASS's `\k` sweep can't carry per-word outline/shadow overrides — see app/ass_render.py's `_current_word_dialogues`). Never "None" the row itself the way Highlight/Outline/Shadow can be — a mode is always off/current_word/progressive_fill. Shares `highlight_color`/`highlight_border_radius` naming convention with style-section-highlight.js only in spirit now — the two features write disjoint `highlight_*` vs `spotlight_*` fields (see TextPreset below)
  caption-panel-words.js        # CAPTIONS panel Closed-caption tab: every transcribed word, inline-editable text (empty text deletes the word) and inline-editable start/end timing (number inputs, validated via clampWordTiming()) — was a settings-row + drill-down, folded into a permanent tab pane 2026-07-21
  ui-safe-zones.js             # window.SAFE_ZONES (array of 4 bands: key, label, inset — top/right/bottom/left/width/height as percentages of the 1080x1920 canvas) + UI.safeZones(container) (added UI-component-consistency initiative): single source of truth for the safe-zone guide geometry, consumed by both the on-stage preview overlay here and `static/safe-zone-geometry.js`'s pixel derivation. Renders one `<div class="safe-zone safe-zone-{key}">` per band into container, each holding a `.chip .chip--outlined .chip--safe-zone` label span; injects one generated `<style>` element (id `safe-zone-geometry-style`, one CSS rule per band's inset) into `document.head` the first time it runs rather than setting inline `style` per div (final review finding 5) — safe-zones.css keeps only each band's visual accent (border/background)
  safe-zone-geometry.js         # window.SafeZoneGeometry: CANVAS_W/CANVAS_H/TOP_ZONE_BOTTOM/CAPTION_ZONE_TOP/CAPTION_ZONE_BOTTOM/HORIZONTAL_MARGIN — pixel-space mirror of `ui-safe-zones.js`'s SAFE_ZONES percentages, derived (not hand-mirrored) from that one array since 2026-07-29; single source of truth for default text/caption insert positions (panel-text.js, panel-captions.js) and the position anchor grid's horizontal margin (panel-text.js's anchorPositionX)
  css/
    tokens.css            # :root custom properties (colors, fonts, spacing, radius) + @font-face — single source of truth
    base.css               # reset + element defaults (body, button, input) on the tokens
    layout.css               # app shell: 3-column main (left panel, #center-col, right panel), full height, filling #app directly; #center-col is a flex column holding the stage and timeline strip. The top #toolbar strip and its toolbar.css were removed 2026-07-30 (remove-text-tool-top-bar) — the Select tool button lives in rail-tool.css's #rail-tool, and the Text tool is armed via #panel-nav's existing TEXT entry (panel-nav.js)
    rail-tool.css             # #rail-tool: container spacing for the left rail's Select-tool button, above #panel-nav (added 2026-07-30, selector-tool-rail); the button itself reuses icon-rail.css's styling
    components/
      panel.css                # left MEDIA panel: display-only clip rows (thumbnail + name/duration, click-to-select) + .collapsed state (72px icon rail: import button + thumbnails only)
      stage.css                 # 9:16 stage (height-driven via aspect-ratio, fills #stage-wrap's available height, capped to its width) + transport controls + .text-block overlay styling; the shared `.stage-media` class (added 2026-07-24, replacing `#player, #image-player` id selectors) sizes/object-fits #player, #image-player, and preview.js's dynamically-created second <video> alike + .stage-hidden show/hide toggle (image clips, added 2026-07-21); `#overlay` sets `isolation: isolate` (fixed 2026-07-30, video/image box invisible behind main video): neither `#stage` nor `#overlay` are otherwise stacking-context-forming, so a video-box/image-box child's default `z_index: -1` used to escape to the document's root stacking context and lose to `#player` (z-index: auto) every time, rendering PiP boxes invisible; isolation scopes negative z-index comparisons back to #overlay's own children; as of 2026-07-31 (transcript sidebar) `#stage-wrap` changed from a centered flex column to a flex row, with the old centering styles (align-items/justify-content/gap center) moved to the new `#stage-column` rule wrapping the stage and transport
      timeline.css              # timeline strip: ruler, playhead, row tracks, blocks; `.video-clip-filmstrip` (canvas fills the block, added 2026-07-23, timeline thumbnails) + text-shadow on the VIDEO row's label span for legibility over frame content (was a background scrim gradient until 2026-07-29 — since `.timeline-block` centres the span vertically, that band rendered as one continuous horizontal line straight across the whole filmstrip); `.row-add-button` (renamed from `.row-add-btn` Task 17) is layout-only positioning for the VIDEO/TEXT row's "+" button, itself built via UI.button; `.slice-icon-trigger` (renamed from `.slice-icon-btn` Task 17, to avoid colliding with the retired `.icon-btn` substring pinned by the guard test) styles the playhead-grip/slice-action span icons. As of 2026-07-31 (locked overlay layers): `.overlay-lane-label.locked`/`.row-label-lock` style the locked-lane and MAIN/AUDIO static lock icons
      button-group.css           # reusable .btn-group toggle-row styling (used by ui-button-group.js); `.btn-group-inline` (added 2026-07-29) is a `display: contents` modifier making a button group share its parent `.style-row`'s grid instead of opening its own, so its buttons sit on the same row as standalone UI.button toggles (TEXT panel's Italic/Underline + case group); the old standalone `.icon-btn` rule block was retired Task 17 (batch 3 close-out) in favor of UI.button's `.button-sm` — `.icon-hidden` (icon-swap toggle, still used by play/pause and the theme toggle) is unaffected
      number-field.css            # custom up/down stepper for number inputs (native OS spin control is unstylable); `.number-field-arrows`/`.number-field-arrow` (renamed from `.number-field-stepper`/`.number-field-step` Task 17, to avoid colliding with the retired-class guard test) lay out the two UI.button-built arrow buttons in a stacked column; `.number-field-arrows` has an explicit `height: var(--control-size-sm)` (fixed 2026-07-30, box-tab-common-component — the input's own height is fixed by style-panel.css's `.style-field input[type="number"]` rule, but the arrows column had no explicit height of its own, so its two buttons fell back to their combined content height, taller than the input) so the two stacked buttons split it evenly instead of the whole control looking lopsided; used by ui-number-field.js
      style-panel.css            # right-hand context panel: closed by default (#style-panel[hidden]), close button, mutually-exclusive sections (#panel-files/#panel-video/#panel-text/#panel-captions/#panel-video-box/#panel-settings/#panel-export/#panel-projects) toggled via their own `hidden` attribute; mockup-matched (mono-caps section labels, button-group align/position); `#panel-export #export` is styled full-width to sit naturally in the panel; `#clip-list li`'s card/hover/selected styling moved to list-row.css 2026-07-22 (scoped to `#clip-list li.list-row`, so the "VIDEOS"/"IMAGES" section-label rows — which never get that class — no longer inherit it)
      divider.css                 # .ui-divider: a plain static 1px line, no hover effect; pairs with UI.divider (static/ui-divider.js), added 2026-07-17
      list-row.css                # .list-row/.list-row--subtle/.selected: shared clickable-row card/hover/selected recipe (UI.listRow, static/ui-list-row.js), reused by project-list-row.css/style-panel.css/sub-panel.css instead of each hand-rolling its own copy (added 2026-07-22, list-row component); linked first among component stylesheets so consumers' own overrides win the cascade
      color-swatch.css            # .color-swatch-row/.color-swatch/.color-swatch-label: small square input[type=color] + mixed-case label beside it, one field per row
      resize-handles.css      # .resize-handle-* styling for ui-resize-handles.js
      accordion.css            # .accordion-header/.accordion-body/.accordion-chevron collapsible pair (UI.accordion/UI.accordionSection)
      button.css               # .button/.button-sm/-md (sizes)/.button-neutral/-accent/-danger/-dashed (intents)/.button-pressed/.button-disabled (states) shared button variants (UI.button)
      icon-rail.css            # .icon-rail/.icon-rail-btn vertical icon+label nav rail (UI.iconRail)
      settings-row.css         # .settings-row label/value/chevron drill-down row (UI.settingsRow)
      sub-panel.css            # .sub-panel-header back+title + .font-list drill-down list styling (UI.subPanelHeader); .font-list-row's card/hover styling moved to list-row.css's .list-row--subtle variant 2026-07-22, this file keeps only the row's own flex layout; .font-list-row-name-group (added 2026-07-23) groups a .font-list-row-name with an inline status icon (e.g. the filler-words tab's "found in transcript" warning) so they sit together instead of being spread apart by .font-list-row's space-between; `.style-sub-panel[hidden] { display: none; }` (added 2026-07-29, bug fix found during shared-style-sections batch 3, also hotfixed directly to a prior commit on main): `.style-sub-panel { display: flex }` (Batch 1) had no `[hidden]` override, so every StylePanelHost drill-down subpage (Font Family, Weight, Color, ...) stayed visually rendered when closed — StylePanelHost only ever toggles the `hidden` attribute, never inline style/display, and the bare class rule silently beat the browser's default `[hidden]` styling
      save-indicator.css       # .save-indicator "Saving…/Saved" label in #panel-brand (UI.saveIndicator; dot removed 2026-07-29)
      export-progress.css      # .export-progress track + .export-progress-fill: background export job percent bar in #panel-export
      video-box-panel.css      # #panel-video-box internal layout: add-picker list + trim/time/position/size detail view
      image-box-panel.css      # #panel-image-box internal layout, mirrors video-box-panel.css (image/photo PiP feature)
      shape-panel.css          # #panel-shape internal layout, mirrors image-box-panel.css (added 2026-07-31, vector shapes)
      mask-line-guide.css          # .mask-line-guide line + handle styling for ui-mask-line-drag.js (added 2026-07-29, box edge mask)
      project-picker.css       # #project-picker full-screen cold-start picker
      project-list-row.css     # .project-list-row name/meta/action-buttons + shared list reset (UI.projectListRow); row card/hover/selected styling moved to list-row.css 2026-07-22, this file now holds only the row's own flex layout
      safe-zones.css               # #safe-zones: 4 `.safe-zone-*` guide bands (top nav / right action rail / caption area / bottom nav, percentages matching TikTok's real UI chrome) overlaid on #stage — shaded tint + solid accent edge (not dashed) plus opaque label chips (same recipe as .slice-btn) for legibility over arbitrary video content, toggled via [hidden]; #safe-zones-toggle lives in the timeline toolbar (`#timeline-toolbar`, next to zoom −/+, shield icon), preview-only, persisted in localStorage
      login.css                    # full-screen centered password form for GET/POST /login (added 2026-07-23, cloud hosting)
      style-save-form.css          # inline "save current style" form (UI.styleSaveForm, static/ui-style-save-form.js): name input + Save/Cancel row + overwrite hint
      chip.css                     # .chip + .chip--pill/.chip--outlined/.chip--safe-zone/.chip--tinted/.chip--silence/.chip--filler modifiers (added UI-component-consistency initiative): shared small-labeled-chip component for usage counts, status badges, and safe-zone labels — used by `panel-auto-slice.js`'s detected-range badges (`.chip--tinted`) and `ui-safe-zones.js`'s label spans (`.chip--outlined .chip--safe-zone`); `.chip--pill` (the old FILES-row usage-count badge recipe) currently has no consumer — the usage-count indicator it was meant for is now just a disabled trash button with a `title` tooltip (`panel-media.js`), not a visible chip
      text.css                     # .text-eyebrow/.text-label/.text-hint/.text-body (added UI-component-consistency initiative): shared typography component built by `static/ui-text.js` — one recipe per variant instead of the "eyebrow label" pattern being hand-rolled per file
      transcript-sidebar.css        # #transcript-sidebar paragraph-style caption transcript list (added 2026-07-31, transcript sidebar): one `.transcript-sentence` div per sentence, one `.transcript-word` span per word; `.active` class marks the current sentence, `.active-word` marks the current word during playback; styled for live highlight + auto-scroll + click-to-seek
      scroll-list.css               # .scroll-list (added UI-component-consistency initiative): shared max-height-320px + overflow-y:auto utility for any picker/results list capped to a fixed height, no dependencies
  fonts/                # vendored variable woff2 (JetBrainsMono-Regular, PublicSans-Regular, 400-700) + static per-weight .ttf files baked by scripts/generate_font_weights.py (for PIL measurement + libass fontsdir)
scripts/
  generate_font_weights.py  # one-off dev script: bakes the static per-weight .ttf files from the vendored variable fonts
tests/
  test_models.py
  test_store.py
  test_media.py
  test_main.py          # export-route tests: ASS rendered + burned in when text blocks exist, skipped otherwise; also probe/presets/import-media
  test_timeline.py
  test_ffmpeg_cmd.py
  test_ass_render.py
  test_text_case.py
  test_font_metrics.py
  test_transcribe.py
  test_transcribe_route.py
  test_export_smoke.py  # Phase 6: whole-pipeline smoke test, every layer type combined
  test_box_mask.py      # the pure polygon function: vertical/horizontal/angled cuts, flip, line-outside-both-ways, bounds clipping
  test_box_mask_js.py   # runs static/box-mask.js under Node against the same case table, pinning the JS mirror to the Python original
  test_mask_image.py   # generated PNG size/mode plus sampled alpha on the kept and cut sides
  js/                  # `node --test` frontend tests over the dependency-free static/*.js modules; preview-load-refreshes-overlays.test.js is the one source-shape guard among them, pinning preview.js's load() to rebinding textProject/textPresets + repainting (the undo/redo stale-stage fix, 2026-07-29) since that IIFE cannot be exercised without a DOM; transcript-sentences.test.js (added 2026-07-31, transcript sidebar) covers groupBySentence via 7 test cases; transcript-sidebar-hooks.test.js (added 2026-07-31) is a guard pinning the integration hooks (TranscriptSidebar.render in preview.js and panel-captions.js) exist
data/               # gitignored: projects/*.json, presets.json, exports/, peaks/{media_id}.json, thumbnails/{media_id}.jpg
```

## Inventory

Organized by feature. Each feature names the files that implement it — for a plain file→purpose lookup, see the File structure tree above.

### Data model & persistence

Foundational — every other feature below builds on these.

- `app/models.py` — Pydantic entities: `MediaItem(id, file_path, name, duration, has_audio)`, `Project`, `ProjectSummary`, `ClipLayer`, `VideoBoxLayer`, `TextPreset`, `FormatRun`, `TextBlockLayer`, `CaptionWord`, `CaptionTrack`, `MusicTrack`, `new_id()`. Field-level detail lives under each owning feature below (media/import for `MediaItem`, project management for `Project`/`ProjectSummary`, timeline/video clips for `ClipLayer` (volume/muted added batch 1), video boxes for `VideoBoxLayer`, text blocks for `TextPreset`/`FormatRun`/`TextBlockLayer`, captions for `CaptionWord`/`CaptionTrack`, audio for `MusicTrack`).
- `app/store.py` — JSON persistence: `save_project`, `load_project`, `list_projects` (loads every `<data>/projects/*.json`), `delete_project`, `save_preset`, `load_presets`. One JSON file per project under `<data>/projects/`, global presets in `<data>/presets.json`.
- `app/main.py` — FastAPI composition root wiring every route below to its module; static mount at `/static`.

### Media library & import

- `MediaItem(id, file_path, name, duration, has_audio, kind, source_path)` in `app/models.py` — separates imported media library from timeline clip references. `name: str = ""` (Task 3, added 2026-07-20: optional custom media name; display falls back to `file_path` basename if empty). `has_audio: bool = True` (Phase 6, added 2026-07-20: whether ffprobe found an audio stream; defaults `True` so existing saved projects behave unchanged until re-probed) is consumed by the export pipeline (see below) to decide whether a clip needs synthesized silent audio. `kind: str = "video"` (added 2026-07-21, image/photo clips: `"video"` or `"image"`; `"audio"` added batch 1 for music imports) drives the probe route's response, the preview stage's `<video>`/`<img>` hand-off (see Timeline/preview below), and `app/ffmpeg_cmd.py`'s `-loop 1` image-input branch; defaults `"video"` so existing saved projects are unaffected. `source_path: str = ""` (added 2026-07-31, copy-on-import, design spec at `docs/superpowers/specs/2026-07-31-media-import-copy-design.md`) preserves the original external path for dedup-only; `file_path` now points at the copy under `<data_dir>/media/` for playback/export resilience to moved/missing source files. `display_name` property returns `name` if non-empty, else `file_path` basename.
- `app/media.py` — `ffprobe_cmd`, `probe_duration`, `has_audio_stream(path) -> bool` (Phase 6, added 2026-07-20: ffprobes via `-select_streams a`, populates `MediaItem.has_audio` at import time and is exposed through `GET /api/probe`), `is_image_path(path) -> bool` (added 2026-07-21, image/photo clips: extension check against `IMAGE_EXTENSIONS = {.jpg, .jpeg, .png, .webp}`, no ffprobe involved), `copy_into_media_dir(source_path, media_id, data_dir) -> Path` (added 2026-07-31, copy-on-import: copies source into `<data_dir>/media/<media_id><ext>` with ext lowercased; raises FileNotFoundError for missing source), `media_response` (serves a local file via FastAPI, 404s if missing), `pick_files() -> list[str]` (native OS file-open dialog, multi-select, dialog title "Import Media", returns the chosen paths or `[]` if cancelled; renamed from single-select `pick_file` 2026-07-22, media-import-multi-select-sort feature; filetypes list unchanged: Media/Video/Image/All), `generate_thumbnail(media_id, file_path, data_dir) -> Path` (added 2026-07-23, FILES-panel thumbnails: extracts a frame at 1s via ffmpeg for videos, or scales the source directly for images; both scale/pad to 68x102 JPEG; cached at `<data_dir>/thumbnails/{media_id}.jpg`, invalidated by absence only, same cache-by-id convention as `waveform.py`'s peaks cache). `probe_duration`/`has_audio_stream` (and `run_export`, see export pipeline) resolve `ffprobe`/`ffmpeg` from a freshly-read registry PATH rather than the process's inherited env, so a PATH change takes effect without restarting every ancestor process.
- `app/main.py` — `GET /api/probe` (-> `{duration, has_audio, kind}`; added 2026-07-21, image/photo clips: for an image path, short-circuits to `{duration: 0.0, has_audio: False, kind: "image"}` without ffprobing), `GET /api/pick-files` (-> `{"paths": list[str]}`; renamed from `/api/pick-file` 2026-07-22), `GET /api/media/{media_id}/thumbnail?path=...` (added 2026-07-23, FILES-panel thumbnails: thin wrapper over `media.generate_thumbnail`, returns the cached JPEG as a `FileResponse`), `POST /api/projects/{pid}/import-media` (added 2026-07-31, copy-on-import: body `{"paths": [...], "kind"?: "audio"}`; copies each source file into `<data_dir>/media/`, probes it, and builds/dedupes MediaItems with source_path for dedup and file_path pointing at the copy; returns `{project, imported}`; final-review fixes same day: an unreadable source raises `HTTPException(400, detail=...)` instead of an unhandled `FileNotFoundError`; `kind`, when provided, is validated against `("video", "image", "audio")`, else `HTTPException(400, ...)`; dedup also matches a legacy `MediaItem` — `source_path == ""` and `file_path` equal to the picked path — from before this feature shipped), `GET /media`.
- `static/api-pick-files.js` — `Api.pickFiles() -> Promise<string[]>`: `GET /api/pick-files` (renamed from `api-pick-file.js`'s `Api.pickFile()` 2026-07-22, media-import-multi-select-sort feature).
- `static/api-probe-media.js` — `Api.probeMedia(path)`: `GET /api/probe` -> `{duration, has_audio, kind}` or `null`.
- `static/api-import-media.js` — `Api.importMedia(projectId, paths, kind?) -> Promise<{project, imported} | null>` (added 2026-07-31, copy-on-import): `POST /api/projects/{pid}/import-media` with body `{"paths": [...], "kind"?: "audio"}`, copies source files into project's data dir, probes them, builds `MediaItem`s with `source_path` for dedup and `file_path` pointing at the copy, `name` set to original basename; returns `{project, imported}` where `imported` holds only newly-added items (skipping deduped); `null` on failure.
- `static/api-get-media-thumbnail.js` — `Api.getMediaThumbnail(mediaId, filePath) -> Promise<string | null>`: `GET /api/media/{id}/thumbnail?path=...` (added 2026-07-23, FILES-panel thumbnails), returns an object-URL for the JPEG blob or `null` on a failed fetch.
- `static/clip-sequence.js` — `importMedia()` (renamed from `addClip()` 2026-07-22, media-import-multi-select-sort feature; updated 2026-07-31, copy-on-import): calls `Api.pickFiles()` (multi-select) then delegates to `Api.importMedia(projectId, paths)`, which copies files into `data/media/`, probes them, builds `MediaItem`s with `source_path` for dedup; previously called `Api.probeMedia` per path and built `MediaItem`s client-side, now server-side. A `null` result (import failure) shows `alert("Import failed — one of the selected files could not be read.")` and returns (final-review fix — the old client-side path's failure feedback was lost in the rewrite). On success, still calls `await saveProject()` after assigning `project = result.project` (final-review fix, 2026-07-31: the server already persists, but `saveProject()` is also what reseeds the undo baseline `lastSavedJson` and records the import as its own undo step — skipping it left an unrelated later Ctrl+Z able to revert past the import and silently drop the imported `MediaItem`), then `MediaPanel.render()`. Wired to the `#add-clip` click listener (button label "IMPORT MEDIA") and to the timeline VIDEO row's "+" button (`static/editor.js`'s `renderTimeline()` `onAddClip` callback). `appendMediaClipToSequence(m)` (added 2026-07-24) is the separate one-shot helper backing `panel-media.js`'s plus icon: appends `m` as a new `ClipLayer` at the end of the VIDEO sequence via `insertClipIntoSequence`, self-contained save/`Preview.load`/`renderTimeline`/auto-caption. `insertClipIntoSequence(source, dropTime)` (extracted from `editor.js` 2026-07-21: splits the clip under the drop point into two trimmed halves, or inserts at the nearest boundary; also shifts `project.captions.words` after the drop point via `CaptionClipSync.shiftCaptionsAfterEdit`, added 2026-07-30, caption/clip sync; returns the new clip).
- `static/editor.js` — `setPanelCollapsed(bool)` toggles the MEDIA panel's `#panel.collapsed` (72px icon rail) and persists to `localStorage` (`panelCollapsed`). `renderTimeline()`'s `onAddClip` callback calls `importMedia()` (updated 2026-07-22 alongside the `clip-sequence.js` rename).
- `static/panel-media.js` — `MediaPanel.render()` (extracted from `editor.js` 2026-07-20): builds (once, lazily) an icon tab bar into `#files-type-tabs` — All (`layout-grid`)/Video (`video`)/Images (`image`)/Audio (`music`), `UI.tabBar` — that sets a module-local `activeMediaFilter` (added 2026-07-30, files-icons-tab feature, replacing the earlier always-grouped VIDEOS/IMAGES/AUDIO section-label rows from the 2026-07-22 media-import-multi-select-sort feature) and re-renders on selection; `render()` filters `project.media_library` through `TYPE_FILTERS[activeMediaFilter]` and renders the matching items flat (no section labels) as thumbnail+name+duration rows (`formatClipDuration`, mm:ss.s) into `#clip-list` via a `buildRow(m)` helper. Each row's `.clip-thumb` div starts as the striped placeholder background and asynchronously fetches a real thumbnail via `Api.getMediaThumbnail(m.id, m.file_path)` (added 2026-07-23), swapping in an `<img class="clip-thumb-img">` (object-fit: cover, styled in `style-panel.css`) once loaded; a failed fetch leaves the striped placeholder untouched. Click-to-toggle a row's highlight via module-local `selectedMediaId` — purely local state, independent of timeline `selected` and with zero side effects on player/timeline/VIDEO panel; also wires `draggable`/`dragstart` for drag-to-timeline. Each row's name resolves via `m.name || m.file_path.split(/[\\/]/).pop()`; a hover-revealed pencil icon button (`.clip-actions`) swaps the `.clip-name` label for a text `<input>` — Enter/blur commits (`m.name = value.trim(); saveProject(); render()`), Escape cancels and restores the label with no save. `.clip-name` truncates long names with an ellipsis at rest (`text-overflow: ellipsis`, `style-panel.css`) — the 2026-07-30 media-name-hover-expand hover popout (absolutely-positioned, full-width `max-content` box with its own background/shadow/`z-index`) was removed the same day, so hovering a name no longer changes its rendering. Each row also computes `count = project.clips.filter(c => c.media_id === m.id).length` fresh on every render; `count > 0` swaps the row's trash button for a disabled lock icon (`UI.button`, `icon: "lock"`, added 2026-07-30, lock-icon-in-use-media) with `title="Locked — used by N clip(s)"` (the old `.clip-usage-chip` visible badge was retired earlier during the UI-component-consistency initiative; a disabled trash icon was the interim state before this change); `count === 0` shows the normal enabled trash button, which removes the item from `project.media_library` only (`saveProject()` + re-render, no confirmation, no cascade delete of `project.clips`). `appendMutedIcon(durationRow)`/`checkSilentAudio(m, durationRow)` (added 2026-07-23) show a "no audio" icon (`.clip-audio-muted-icon`) in the duration row: immediately for `m.has_audio === false`, or lazily for video clips with a stream whose fetched peaks (`Api.getMediaPeaks`, cached client-side per media id in module-local `silentCache`) are all `<= 0.02` — the existing `has_audio` ffprobe check only detects a *missing* stream, not a present-but-silent one. Each row's hover-revealed action icon(s) (added 2026-07-24, extended 2026-07-30 video-hover-icons-files) add the item directly instead of just importing it: a video row shows two — a clapperboard `addMainBtn` calls `appendMediaClipToSequence(m)` (`clip-sequence.js`) then re-renders this list so the trash button's disabled state reflects the new usage count, and a picture-in-picture `addPipBtn` calls `VideoBoxPanel.createVideoBox(m)` then routes through `onTimelineSelect({ type: "video-box", item: box })` (`panel-nav.js`) plus an explicit `VideoBoxPreview.render(...)` call (same "onTimelineSelect alone doesn't mount into `#overlay`" gap as the image-box branch below); an image row shows one plus `addBtn` that calls `ImageBoxPanel.createImageBox(m)` then routes through `onTimelineSelect({ type: "image-box", item: box })` (`panel-nav.js`) so `selected` (editor.js) tracks the new box, plus an explicit `ImageBoxPreview.render(...)` call since `onTimelineSelect` alone doesn't mount the box into `#overlay`; an audio row shows one plus `addBtn` that sets/replaces `project.music` and calls `Preview.load(project)` + `openAudioPanel()` (`panel-nav.js`), mirroring `panel-audio.js`'s `addMusic()`/`replaceMusic()`.
- `static/css/components/panel.css` — left MEDIA panel's icon rail: `#panel`/`#panel-brand` (fixed 72px collapsed-rail chrome). Does **not** hold `#clip-list` row styling — that lives in `style-panel.css` (see below), since the MEDIA rows render inside the right-hand `#style-panel`'s FILES section, not the left rail.
- `static/css/components/style-panel.css` — (see "Shared UI components" below for the section as a whole) also holds `#clip-list`/`.clip-thumb`/`.clip-info`/`.clip-name`/`.clip-duration` row styling plus `.clip-actions`/`.clip-name-input` (Task 4, 2026-07-20: opacity-0-by-default hover-reveal for the row's action buttons, and the inline-rename `<input>`'s styling), `.clip-section-row` (2026-07-22, media-import-multi-select-sort feature, renamed from `.clip-section-label` during the UI-component-consistency initiative: the `VIDEOS`/`IMAGES` group-label row styling — small, muted, letter-spaced, non-interactive `<li>` inside `#clip-list`), and `.clip-thumb-img` (added 2026-07-23, FILES-panel thumbnails: fills `.clip-thumb`'s 34x52 box, `object-fit: cover`; `.clip-thumb` gained `overflow: hidden` so its existing `border-radius: 3px` clips the image too).

### Project management (multi-project)

Full-screen picker at cold start, plus an in-editor PROJECTS panel — open/rename/duplicate/delete, autosave with a save indicator.

- `Project`/`ProjectSummary` in `app/models.py` — `Project` carries `id`/`created_at`/`updated_at`/`name` plus every layer collection (`media_library`, `clips`, `video_boxes`, `text_blocks`, `text_presets`, `captions`, `music` added batch 1); `ProjectSummary(id, name, created_at, updated_at)` is the lightweight `GET /api/projects` list payload — never the full project.
- `app/store.py` — `list_projects`, `delete_project` (see Data model above for the rest).
- `app/main.py` — `POST /api/projects`, `GET/PUT /api/projects/{id}`, `GET /api/projects` (all as `ProjectSummary[]`), `DELETE /api/projects/{pid}` (204), `POST /api/projects/{pid}/duplicate` (new id, name `"<name> copy"`).
- `static/api-create-project.js` / `api-list-projects.js` / `api-delete-project.js` / `api-duplicate-project.js` / `api-rename-project.js` / `api-ensure-project.js` — one `Api.*` function per file: `createProject`, `listProjects` (-> `ProjectSummary[]`), `deleteProject`, `duplicateProject`, `renameProject` (fetches fresh from disk, patches `name`, PUTs back), `ensureProject` (resolves `localStorage.projectId` to a `Project` or `null` — cold-start routing).
- `static/api-save-project.js` — `Api.saveProject(project)`: `PUT /api/projects/{id}`, persists the whole project.
- `static/ui-project-picker.js` — `UI.projectPicker(container, {onOpen})`: full-screen cold-start picker (`#project-picker`); always re-fetches the list via `Api.listProjects()` on mount, "+ NEW PROJECT" creates via `Api.createProject()` then calls `onOpen`.
- `static/ui-project-list-row.js` — `UI.projectListRow(project, {onOpen, onRename, onDelete, onDuplicate}) -> <li>`: one project row (inline-editable name, relative last-edited meta, optional duplicate/delete icon buttons). Shared by the full-screen picker (open-only) and the in-editor PROJECTS panel; callers own persisting any change.
- `static/panel-projects.js` — `ProjectsPanel.render(currentProjectId, callbacks)`: the `#panel-projects` context section — project list (open/rename/delete/duplicate via `Api.*`) + "+ New Project" (`callbacks.onCreateRequested`); never navigates or saves the currently-open project itself.
- `static/ui-save-indicator.js` — `UI.saveIndicator(container) -> {setSaving, setSaved}`: "Saving…/Saved" label mounted in `#panel-brand` (grey status dot removed 2026-07-29).
- `static/editor.js` — startup calls `Api.ensureProject()`: a still-valid saved project id opens directly via `openProject(target)` (fetches, loads, sets `document.title`), otherwise `showPickerScreen()` mounts `UI.projectPicker`. `openProjectsPanel()` renders `ProjectsPanel` with switch/create routed through `confirmFlushAndSwitch(action)` (saves the current project before switching) and `onDeletedCurrent` falling back to `showPickerScreen()`. A `keepalive: true` PUT on page unload flushes the last unsaved state. `saveProject()` wraps `Api.saveProject` with the `#panel-brand` save indicator (`setSaving()`/`setSaved()`).
- `static/css/components/project-picker.css` — `#project-picker` full-screen cold-start picker + `.new-project-btn` (shared with `panel-projects.js`).
- `static/css/components/project-list-row.css` — `.project-list-row` name/meta/action-buttons + shared list reset.

### Timeline

- `app/timeline.py` — `ordered`, `clip_duration`, `sequence_duration`, `locate` (timeline time -> clip + source-time), `slice_clip(clips, t, eps=0.05)` (splits the clip under timeline-time t into two clips at that point; no-op when t is in no clip or within eps source-seconds of a boundary; returns `(clips, newId)`); `clip_duration`/`locate` are speed-aware (divide by `ClipLayer.speed`, Task 4); `locate` and `clip_duration` are mirrored in `static/preview.js`.
- `static/timeline.js` — `render(project, timelineTime, selected, onSelect)` (ruler, playhead, clip/text/video-box/caption blocks, toolbar time readout, playhead-handle box position, AUDIO placeholder), `tick(timelineTime)` (cheap playhead/handle-box/time-readout-only update, driven every animation frame during playback so motion stays smooth between heavier `render()` calls), `groupWords(words, max)` (caption word grouping, reused by captions — see below), `timeAtX(clips, rulerRect, clientX)` (coordinate math). Zoomable scale: `visibleSeconds` (default 30, wired `#zoom-controls` −/+ buttons step it by 10s) sets pixels-per-second to fit that many seconds across `#timeline-scroll`'s width; `#timeline-scroll` provides horizontal scroll when content overflows. Auto-fit (added 2026-07-23): every `render()` sets `visibleSeconds = fitVisibleSeconds(totalDuration)` (whole project's content + 2s padding, floored at the 30s default) unless the user has manually zoomed (`manualZoom` flag, set `true` by `zoomIn`/`zoomOut`, cleared by `resetZoom` on project open) — so adding a clip that grows the project past the visible window auto-widens it, until manual zoom takes over for that project session; `zoomOut`'s ceiling is `Math.max(120s, fitVisibleSeconds(totalDuration))` so it can never clamp tighter than the content needs. Rows top-to-bottom: TEXT, CAPTIONS, VIDEO BOX, VIDEO (labelled MAIN in the UI as of 2026-07-29, audio panel — row key/id stay `video`/`row-video`/`label-video`), AUDIO (static dummy waveform — deterministic pseudo-random bars regenerated only when track width changes, no real audio-track data yet). Empty rows are collapsed out of the timeline (2026-07-22): `setRowVisible(rowName, visible)` toggles the `hidden` attribute on both a `.timeline-row[data-row=X]` and its aligned `#label-X` together — only VIDEO always shows; TEXT/CAPTIONS/VIDEO BOX appear only when they have content, and AUDIO stays hidden until a real audio feature exists (`.timeline-row[hidden]`/`.row-label[hidden]` CSS in `timeline.css` beats the row's `display:flex`). `#timeline-strip` has a toolbar above the ruler (zoom −/+ buttons; mm:ss.s time readout; safe-zones toggle), and a playhead-handle box (`#slice-btn`) tracking the playhead with a grip-vertical handle (`#playhead-grip`) and a scissors icon (`#slice-action`, click to slice the active clip at the playhead — or, as of 2026-07-30 video-box-slice-priority, a selected video box active there instead; see `static/timeline-slice.js`'s entry below). TEXT and CAPTIONS rows share the same 44px height.
- `static/timeline-slice.js` — `Timeline.sliceClip(clips, t, eps=0.05)` (pure JS mirror of `app.timeline.slice_clip`; returns `{ clips, newId }`), plus `Timeline.isBoxActiveAt(box, t)`/`Timeline.isBoxSliceDisabled(box, t, eps=0.05)`/`Timeline.sliceVideoBox(videoBoxes, box, t, eps=0.05)` (added 2026-07-30, video-box-slice-priority feature): pure DOM-free helpers mirroring `sliceClip`'s shape but keyed off a single `VideoBoxLayer`'s own `start`/`in_point`/`out_point` fields instead of an ordered clip list — no `order`-shifting, since a box isn't part of a sequence. Click-handler wiring for the `#slice-action` scissors button: reads the playhead time via `Preview.currentTimelineTime()`, then prioritizes a selected+active video box over the main clip — when `selected.type === "video-box"` and `Timeline.isBoxActiveAt(selected.item, t)`, it calls `Timeline.sliceVideoBox` instead of `Timeline.sliceClip`, saves, and reselects the newly-created SECOND box (`onTimelineSelect({ type: "video-box", item: newBox })`, the same "select then explicitly render the stage" pattern `editor.js`/`panel-media.js` use) before an explicit `VideoBoxPreview.render(...)` call — reselecting the second half rather than the original keeps it visible/selected at `t` (a sliced box's window is end-exclusive, so the original's window now ends at `t` and is no longer active there) and keeps a same-`t` second click a harmless no-op instead of falling through to slicing the main clip. Otherwise falls through to the original `Timeline.sliceClip` path unchanged: saves the project and re-renders the stage/timeline. The whole file is wrapped in an IIFE (matching `font-size-scale.js`/`style-fields.js`/`format-run-write.js`) so nothing but `window.Timeline` and (for `node --test`) `module.exports = { isBoxActiveAt, isBoxSliceDisabled, sliceVideoBox }` escapes into global scope.
- `static/timeline-clip-drag.js` — drag-to-reorder gesture for VIDEO-row blocks (mousedown + horizontal drag past a 4px threshold, below which the block's existing click-to-select still fires): follows the pointer via CSS transform, snaps the drop target to the nearest clip boundary (`nearestReorderIndex`), and calls `VideoPanel.moveClipTo(clipId, newIndex)` (`static/panel-video.js`) on drop. Delegated on the persistent `#row-video` container so it survives `Timeline.render()` rebuilding its children.
- `static/editor.js` — playhead scrubbing: dragging `#playhead-grip` live-scrubs the playhead via `Timeline.timeAtX` -> `Preview.seek`, re-invoked on every mousemove, with `Timeline.tick` keeping the handle box anchored during the drag. Clip placement: FILES-panel media rows are draggable (`dragstart` sets `text/media-id`); the `#row-video` drop handler inserts the dropped media via the shared `insertClipIntoSequence(source, dropTime)` (`static/clip-sequence.js`, extracted from `editor.js` 2026-07-21: splits the clip under the drop point into two trimmed halves, or inserts at the nearest boundary; returns the new clip). As of 2026-07-31 (overlay-lane-time-drag), dragging a VIDEO BOX onto this row to stitch it into the sequence no longer goes through this handler's `dataTransfer` — see `static/timeline-overlay-time-drag.js`. Clicking a timeline block calls `onTimelineSelect`, opening the matching `#style-panel` context section. Ruler-click and playhead-grip-drag seeking (not step buttons/arrow-key nudge, not clip drag/drop) snap to the nearest clip/text-block/caption-group boundary within an 8px tolerance via `static/timeline-snap.js` (added 2026-07-20) — `Timeline.snapTime(t, boundaries, tolerancePx, pxPerSecond)` (pure nearest-match) and `Timeline.collectBoundaries(project)` (re-derives boundaries fresh from `project` every call, no cached state), both attached onto the existing `window.Timeline` object; Alt held on the triggering click/mousemove event bypasses snapping for that call.
- `static/css/components/timeline.css` — timeline strip: ruler, playhead, row tracks, blocks.

Filmstrip thumbnails (added 2026-07-23): Backend module `app/filmstrip.py` computes frame layout (interval/count capped at 120 frames) and caches one sprite-sheet JPEG per media file via ffmpeg's fps+tile filters; `app/main.py`'s `GET /api/media/{id}/filmstrip` route serves the cached sprite. Frontend layout math in `static/filmstrip-layout.js` mirrors the backend's geometry. `static/timeline-video-row.js` renders a per-clip `<canvas>` inside each VIDEO-row block, fetching the sprite sheet via `Api.getMediaFilmstrip()` and slicing/scaling frames based on clip trim/speed and current zoom; `static/timeline.js`'s VIDEO-row render loop calls `TimelineVideoRow.render()` for each clip, mirroring the audio-row pattern for fetch-once, cache-client-side canvases; since 2026-07-24 tiles are full-row-height 9:16 on a global row grid (`Filmstrip.tilesForBlock`), so thumbnails ignore slice boundaries and fill blocks of any width.

### Toolbar & tool modes

The top `#toolbar` strip (added 2026-07-24, held both Select+Text icons; Select moved into the left icon rail 2026-07-30 selector-tool-rail, leaving Text alone in the top bar) was removed entirely 2026-07-30 (remove-text-tool-top-bar) — `static/ui-toolbar.js` and `static/css/components/toolbar.css` are deleted, and `<div id="toolbar">` is gone from `index.html`. The Select tool button stays in `#rail-tool` (`static/ui-rail-tool-button.js`, unchanged); the Text tool is now armed via the existing TEXT entry already in `#panel-nav`'s icon rail (`static/panel-nav.js`) rather than a second dedicated button — clicking it calls `ToolMode.set("text")` instead of its old `addTextBlockAndEdit()` immediate-insert behavior. As of 2026-07-31 (shape-tool feature), Shape is a third stage tool alongside Select/Text — the SHAPE rail entry (`panel-nav.js`) arms `ToolMode.set("shape")` the same way TEXT arms `"text"`, rather than opening `#panel-shape` directly; drawing a shape (click-drag on the stage) is what opens the panel, via the newly-created shape's selection. `window.ToolMode` drives whether a stage click/drag selects/drags a box (Select), edits/inserts text (Text), or draws a new shape (Shape) — see the gating notes on `ui-text-interaction.js`/`video-box-preview.js`/`stage-click-router.js`/`stage-shape-draw.js` under Video boxes, Text blocks, and Vector shapes below; Select/Text/Shape are mutually exclusive (see `panel-nav.js`'s `armedTool`/`navOnSelect` note below). Select, Text, and Shape (plus every other left-rail entry) got a single-letter keyboard shortcut 2026-07-31 (rail-nav-tooltips) — `V`/`T` for Select/Text (Shape has no letter shortcut), `P`/`F`/`C`/`S`/`E` for Projects/Files/Captions/Settings/Export — wired in `editor.js`'s keydown handler and surfaced via each rail button's tooltip shortcut chip (`ui-tooltip.js`).

- `static/tool-mode.js` — `window.ToolMode.{get, set, onChange}`: the current tool (`"select"`/`"text"`/`"shape"`), default `"select"`, no persistence.
- `static/ui-rail-tool-button.js` — `UI.railToolButton(container)` (added 2026-07-30, selector-tool-rail): renders the Select tool button into `#rail-tool` (top of the left icon rail, sibling of `#panel-nav`), one `.icon-rail-btn.icon-rail-btn-icon-only` wired to `window.ToolMode`; carries a `title`/`shortcut` pair (`"Select tool"`/`"V"`, added 2026-07-31 rail-nav-tooltips) picked up by `ui-tooltip.js`. Text/Shape have no counterpart here — see `panel-nav.js`.
- `static/panel-nav.js` — its `UI.iconRail(...)` wiring special-cases the TEXT and SHAPE rail entries (`value === "text"` / `value === "shape"`) to call `ToolMode.set(value)` instead of going through `PANEL_NAV_HANDLERS`/`openTextPanel` (the old `addTextBlockAndEdit()` immediate-insert was removed 2026-07-30, remove-text-tool-top-bar; SHAPE never had a panel-opening handler at all — `openShapePanel()` doesn't exist) — the rest of the tool-mode click flow (cursor change, edit-existing vs insert-new/draw-new) is unchanged, driven by `stage-tool-cursor.js`/`ui-text-interaction.js`/`stage-click-router.js`/`stage-shape-draw.js` below. A module-local `armedTool` (`"text"`/`"shape"`/`null`, added 2026-07-31 shape-tool feature) tracks which of TEXT/SHAPE is currently rail-highlighted and clears the highlight via a `ToolMode.onChange` subscription whenever the mode reverts to `"select"` from any source — not just a rail click, but also `stage-click-router.js`'s/`stage-shape-draw.js`'s own auto-revert after their one-shot insert/draw completes — so Select/Text/Shape stay a genuinely mutually-exclusive highlighted set under every transition, not just rail-driven ones.
- `static/css/components/rail-tool.css` — `#rail-tool` container spacing (added 2026-07-30, selector-tool-rail); the Select button reuses `icon-rail.css`'s styling.
- `static/stage-tool-cursor.js` — mirrors `ToolMode` onto `#stage`'s `data-tool` attribute (set at load, updated via `ToolMode.onChange`); `stage.css`'s `#stage[data-tool="select"] .text-block { cursor: default }` / `[data-tool="text"] … { cursor: text }` / `#stage[data-tool] .text-block[contenteditable="true"] { cursor: text }` rules do the rest (added 2026-07-29). Replaces `preview-text.js`'s old unconditional inline `div.style.cursor = "text"`, which showed an I-beam over text blocks even with the Select (arrow) tool active and couldn't react to a tool switch without a re-render.
- `static/stage-click-router.js` — self-registered `#stage` `click` listener: Text-tool mode + click not on a `.text-block` (a video box counts as "not on a text-block") inserts a new text block at the click point via `panel-text.js`'s `addTextBlockAndEdit(position)`, then `ToolMode.set("select")` (reverted before the async insert resolves, not after — a second click landing mid-insert must already see `"select"`, or it would start a second concurrent insert; auto-revert, one insert per tool switch). `CanvasPoint.fromClient(clientX, clientY, rect)` (`canvas-point.js`) is the pure client-px → 1080×1920-canvas-px conversion, clamped to canvas bounds. No-ops entirely in Select-tool and Shape-tool modes.
- `static/canvas-point.js` / `static/shape-draw-rect.js` / `static/stage-shape-draw.js` — Shape's own stage-drawing gesture; see the "Vector shapes (overlay layer)" section below for the full drag-to-create behavior.

### Undo/redo

- `static/undo-history.js` — `UndoHistory.record/undo/redo/reset`: pure per-session two-stack snapshot machine (cap 50, dedupe), consumed by `editor.js`'s `saveProject()` capture + Ctrl+Z/Y restore.

### Video clips (VIDEO panel: trim/order/fill-mode/speed/delete)

- `ClipLayer` in `app/models.py` — `media_id: str` required field linking to `MediaItem` (added 2026-07-15). `fill_mode: str = "fit"` (added 2026-07-20, Task 6) selects the export scaling behavior — `"fit"` letterboxes (scale-to-fit + pad, today's default), `"fill"` center-crops to fill the frame with no padding; branched per clip in `app/ffmpeg_cmd.py`'s `build_export_cmd`. Task 7 (2026-07-20) added the matching live-preview toggle — see `static/preview.js`/`static/panel-video.js` below. `speed: float = 1.0` (0.5–2.0, clip-speed feature Task 1, 2026-07-21): timeline duration = `(out_point - in_point) / speed`; export applies `setpts`/`atempo` (pitch-preserved) in `app/ffmpeg_cmd.py`, preview sets the `<video>`'s `playbackRate`.
- `static/panel-video.js` — `VideoPanel.select(c)`/`VideoPanel.render(c)` (extracted from `editor.js` 2026-07-20) populate `#panel-video`'s trim (`UI.numberField`, reusing `clampTrim`), reorder, FILL, and SPEED controls for the selected clip. `#video-name` resolves via the `MediaItem` looked up by `c.media_id` in `project.media_library` (`media.name || media.file_path.split(...).pop()`), falling back to `c.file_path`'s basename if the media item is missing (Task 4, 2026-07-20). A FILL row (`UI.buttonGroup`, FIT/FILL, added Task 7 2026-07-20) sets `c.fill_mode`, saves, and calls `Preview.load(project)` so the stage `<video>`'s `object-fit` updates immediately when the edited clip is the active one. A SPEED row (`UI.numberField`, 0.5–2.0 step 0.1, clip-speed feature Task 4, 2026-07-21) sets `c.speed`, saves, and calls `Preview.load(project)` + `renderTimeline()` so playback rate and the timeline block's speed-scaled width both update immediately. `VideoPanel.deleteClip(clipId)` removes the `ClipLayer`, renumbers the rest's `order` gap-free, drops the clip's `clipDurations` cache entry, and — only if the playhead was inside the deleted clip's range — reseeks it to the deleted clip's former start, clamped against the new (shorter) total duration; also re-times `project.captions.words` via `CaptionClipSync.shiftCaptionsAfterEdit` (added 2026-07-30, caption/clip sync); wired to both `#video-delete` (VIDEO panel, danger-styled) and `editor.js`'s Delete-key handler guarded to `selected.type === "video"` with focus outside any form field. `VideoPanel.moveClipTo(clipId, newIndex)` reorders clips by setting `order` on the moving clip and adjusting affected neighbors; also re-times `project.captions.words` by their clip ownership via `CaptionClipSync.resyncCaptionsAfterReorder` (added 2026-07-30, caption/clip sync), so captions stay with the clips they overlap. The `MediaItem` in `project.media_library` is untouched by deletion. (There is no clip-duplicate feature — `VideoPanel.duplicateClip`/`#video-duplicate` were removed 2026-07-30: duplicating a clip didn't shift/sync captions the way delete/move/insert do; text-block duplication, which used to exist independently via `duplicateTextBlock`/Ctrl/Cmd+D, was removed the same day for consistency — see "Shared UI components" below for the `.panel-danger-footer` pattern `#video-delete` is now also part of.)
- `static/preview.js` — `applyFillModeClass(clip, el = player)` toggles the given element's `.fill-mode-fill` class per `clip.fill_mode`; called from both `playClipAt` and `seek`'s clip-switch branch (the two places the active element's `src` changes for a newly-active clip), added Task 7 2026-07-20; `el` param added 2026-07-21 (image/photo clips) so the same helper applies to `#image-player` as well as `#player`. Image clips (`MediaItem.kind === "image"`, added 2026-07-21): `playClipAt`/`seek` toggle `.stage-hidden` between `#player` and `#image-player`, and hand timing off to `window.ImageClipPlayback` (`static/image-clip-playback.js`) instead of the `<video>`'s `timeupdate` — see "Image clips" note under the File structure tree entry for `preview.js` above.
- `static/css/components/stage.css` — `.stage-media.fill-mode-fill { object-fit: cover; }` (added Task 7 2026-07-20, extended to `#image-player` 2026-07-21, generalized to the shared `.stage-media` class 2026-07-24 so it also covers preview.js's second, dynamically-created `<video>`) overrides the base `object-fit: contain` rule via class specificity, no `!important` needed. `.stage-hidden { display: none; }` (added 2026-07-21, image/photo clips) is the shared show/hide toggle between the stage's video/image elements.
- `static/css/components/style-panel.css` — `#panel-video` (TRIM in/out + ORDER move up/down + FILL FIT/FILL toggle + `#video-delete`, danger-styled via the `--danger` token in `tokens.css`).

### Audio (per-clip volume/mute, background music, real waveforms)

Added 2026-07-22, batches 2–7 of the audio subsystem (Batch 1 data model — `ClipLayer.volume`/`muted`, `MediaItem.kind`, `MusicTrack`/`Project.music` — see "Data model & persistence" and the model field notes above/below).

- `app/ffmpeg_cmd.py` — per-clip `volume=<v>` filter on each clip's real-audio chain (`c.muted` forces `volume=0`, overriding any set volume; default `volume=1.0`/`muted=False` emits no filter, byte-identical baseline). `Project.music`, when set and audible (not muted, `volume > 0`), adds the music file as one more input, trims it to `timeline.sequence_duration(clips)`, applies its volume, and `amix=inputs=2:duration=first`-mixes it with the concatenated clip audio — replacing the final audio map `[a]` with `[amix]` in both the simple and banded export paths (shared `amap` variable). No music: byte-identical to the pre-music baseline.
- `app/waveform.py` — `downsample_pcm16(pcm_bytes, sample_rate, samples_per_second) -> list[float]` (pure, max-abs-per-bucket, normalized against `32768.0`), `peaks_for_media(media_id, file_path, data_dir, samples_per_second=10)` (ffmpeg PCM decode via `app.media`'s registry-PATH resolution, cached to `data/peaks/{media_id}.json`, invalidated by absence only).
- `app/main.py` — `GET /api/media/{media_id}/peaks?path=...` -> `list[float]`, thin wrapper over `waveform.peaks_for_media`. `pick_file(kind="video")` route forwards `kind` to `media.pick_file`.
- `app/media.py` — `_filedialog_options(kind) -> (title, filetypes)` (pure): `"audio"` -> music extensions (mp3/wav/m4a/aac/ogg/flac), anything else -> the existing video+image picker unchanged. `pick_file(kind="video")` uses it.
- `static/preview-audio.js` — `window.PreviewAudio.{load(project), play(), pause(), seek(t)}`: one module-level `<audio>` element synced to the timeline clock for `Project.music`; volume/mute clamped to HTML5's 1.0 cap (export applies the exact ffmpeg gain instead). `static/preview.js` calls `applyClipAudio(clip)` (sets `#player.volume`/`muted` from `ClipLayer.volume`/`muted`, same clamp) at every clip-switch point (`playClipAt`, `seek`), and `PreviewAudio.{load,play,pause,seek}` at `load`/`doPlay`/`doPause`/`doRestart`/`seek`/every sequence-end branch (video, image, zero-clip virtual clock).
- `static/timeline-audio-row.js` — `window.TimelineAudioRow.render(project, pxPerSec, onReady)`: replaces the old static dummy waveform. One `<canvas>` per clip (sliced to its trim window via `sliceForTrim`, proportional to the media's full duration) plus one for the music track (drawn behind the clip canvases, half-opacity), fetched via `Api.getMediaPeaks` and cached client-side per media id (`peaksCache`) so each id fetches once regardless of re-renders; a clip with no audio (or a failed fetch) draws a flat line. `static/timeline.js`'s `render()` calls it with the current zoom-aware `px` and re-renders the whole timeline (`onReady`) once a fetch resolves.
- `static/api-get-media-peaks.js` — `Api.getMediaPeaks(mediaId, filePath) -> Promise<number[]>`, `[]` on a failed fetch (caller falls back to a flat line).
- `static/api-pick-file.js` — `Api.pickFile(kind = "video")`, `kind="audio"` filters the native dialog to music files.
- `static/panel-video.js` — VIDEO panel's Design tab gained a VOLUME group (`UI.numberField` 0–200%, mirrors the SPEED field's wiring) + a mute icon button (`.icon-volume`/`.icon-volume-muted` swap via `.icon-hidden`, same technique as `preview.js`'s play/pause icon).
- `static/panel-audio.js` — `window.AudioPanel.render()`: the `#panel-audio` context-panel section. "ADD MUSIC" (only when `project.music` is unset) imports via `Api.pickFile("audio")` then delegates to `Api.importMedia(projectId, [path], "audio")` (added 2026-07-31, copy-on-import: copies file into `data/media/`, probes it, builds `MediaItem` with `kind: "audio"`, source_path for dedup; previously called `Api.probeMedia` and built the `MediaItem` client-side). A `null` result (import failure) shows `alert("Import failed — the selected file could not be read.")` and returns `null` (final-review fix, restores the pre-rewrite `alert("probe failed")` feedback that was lost). On success, `importMusicFile()` also calls `await saveProject()` right after assigning `project = result.project` (final-review fix, 2026-07-31, same undo-baseline-reseeding reasoning as `clip-sequence.js`'s `importMedia()` — see that entry above), before falling back to finding an existing `source_path` match when `result.imported` is empty (the dedup case). When set, a detail view shows the file name, a VOLUME field + mute button (same pattern as `panel-video.js`'s), and Replace/Remove (Remove clears `project.music` but leaves the `MediaItem` in the library, mirroring `VideoPanel.deleteClip`). One music track only (v1).
- `static/panel-nav.js` — AUDIO entry in `PANEL_NAV_ITEMS`/`PANEL_NAV_HANDLERS` opens `#panel-audio` via `openAudioPanel()`. A separate AUTO entry existed 2026-07-29 through 2026-07-30 (audio panel batch) opening a standalone Auto-caption/Auto-silence panel; removed 2026-07-30 (AUTO tab relocation) — see the AUDIO panel paragraph below.
- `static/timeline.js` — `render()`'s `actions` param gained `onSelectAudio`, wired to a one-time click listener on `#row-audio` (same `dataset.selectBound` guard pattern as the `+` row buttons), so clicking the AUDIO row opens the music-import `#panel-audio` (`openAudioPanel()`) — briefly opened a standalone Auto-caption/Auto-silence panel instead 2026-07-29 through 2026-07-30, reverted when that content moved into VIDEO's Auto tab (see below).
- `static/css/components/timeline.css` — `.audio-clip-waveform`/`.audio-music-waveform`: absolutely-positioned canvases (same coordinate system as `.timeline-block`) replacing the old flex dummy-bar row.

**AUDIO panel (timeline audio track), relocated into VIDEO's Auto tab.** Added 2026-07-29 as a standalone `#panel-audio-track` context-panel section with its own AUTO rail entry; moved 2026-07-30 (AUTO tab relocation) into `#panel-video` as a third "Auto" tab (see panel-video.js/panel-audio-track.js above) since the rail entry and its full-panel real estate weren't earning their keep for what's ultimately per-clip-adjacent tooling. As of 2026-07-31 (auto-caption/filler-words panel move), `static/panel-audio-track.js`'s `AudioTrackPanel.render()` sequences only AUTO SILENCE (`static/panel-auto-slice.js`'s detect → approve → apply flow) — AUTO CAPTION (the language settings row + `runAutoCaption()`) moved out again, into the CAPTIONS panel's own Auto tab (`static/caption-panel-language.js`/`caption-panel-auto-caption.js`, see "Captions & transcription" below), since it pairs more naturally with the transcript it produces and with the filler-word tooling that consumes that transcript than with the timeline's silence-detection tooling. Reached only via VIDEO's Auto tab now — no rail entry, and the timeline's AUDIO row again opens the music-import `#panel-audio`. `CaptionTrack.language` (the model field, unchanged) has moved panel homes three times now (`caption-panel-language.js` → `audio-panel-language.js` 2026-07-29 → `caption-panel-language.js` again 2026-07-31) — only which panel renders its settings row has ever changed.

### Box edge mask (straight-line cut)

Added 2026-07-29 — see `docs/superpowers/specs/2026-07-29-box-edge-mask-design.md`. Cuts a video/image box along one straight line so the layers beneath show through ("clone yourself" without green screen or ML segmentation).

- `app/box_mask.py` / `static/box-mask.js` — `mask_polygon(width, height, angle, offset, flip)` / `BoxMask.maskPolygon(...)`: the one shared geometry function, mirrored across the two languages the same way `app/text_case.py` / `static/text-case.js` are. Returns the KEPT region as a clockwise polygon in box-local px, clipped to the box: `[]` when nothing is kept, the full rectangle when the line misses the box. `angle` is degrees with 0 = vertical and increasing rotating clockwise; `offset` is signed canvas px from the box's center, perpendicular to the line; `flip` selects the other side. `BoxMask.clipPath(box)` additionally formats that polygon as a percentage-based CSS `clip-path` value (JS only, no Python counterpart — same shape as `TextCase.cssValue`).
- `VideoBoxLayer` / `ImageBoxLayer` in `app/models.py` — four defaulted fields carry the cut: `mask_enabled: bool = False`, `mask_angle: float = 0.0` (degrees, 0 = vertical, increasing clockwise), `mask_offset: float = 0.0` (signed canvas px from the box's center, perpendicular to the line), `mask_flip: bool = False` (which side is kept). All defaulted, so projects saved before this feature load and behave unchanged; the line is expressed relative to the box's own center, so moving/resizing the box carries the mask with it.
- `static/video-box-preview.js` / `static/image-box-preview.js` — each `render()` sets `el.style.clipPath = BoxMask.clipPath(box)` right after the position/size/z-index assignments. A static CSS property recomputed only on render — no per-frame work, no libraries — and it composites correctly against the layers below because the boxes already live as siblings in `#overlay` with explicit z-indexes. An unmasked box gets `""`, i.e. today's exact rendering.
- `static/panel-video-box.js` / `static/panel-image-box.js` — a third **Mask** tab (`UI.tabBar`, beside Box and Time; `#video-box-mask-body` / `#image-box-mask-body`) holding an OFF/ON `UI.buttonGroup` bound to `mask_enabled`, ANGLE (degrees) and OFFSET (px) `UI.numberField`s — both disabled while the mask is off — and a "Flip side" button (`UI.button`) toggling `mask_flip`. Every control saves the project and re-renders the stage preview immediately, the same way the panels' existing X/Y/WIDTH/HEIGHT fields do.
- `app/mask_image.py` — `write_mask_png(path, width, height, angle, offset, flip)`: rasterizes `mask_polygon`'s output with Pillow into a `width x height` RGBA PNG, alpha 255 inside the kept region and 0 outside. An empty polygon writes a fully transparent PNG, correctly hiding the box.
- `app/ffmpeg_cmd.py` — a `"video_box"`/`"image_box"` band dict may carry an optional `"mask_path"`. When present, the PNG is added as a `-loop 1 -t <box duration>` input, `alphaextract` pulls its alpha into a gray stream, and `alphamerge` writes that as the box stream's alpha immediately before the existing `overlay`. For a video box the merge happens **before** the `setpts=...+start/TB` timeline offset, so both `alphamerge` inputs start at t=0. Trimming, scaling, positioning, band ordering, and audio are untouched; a project with no masked box produces a byte-identical command to the pre-mask baseline.
- `app/main.py` — the export route's banded branch writes one `{name}-{id[:8]}-band{i}-mask.png` sidecar per masked box (same directory and naming convention as the `.ass` sidecars) and passes its path as the band's `"mask_path"`. Deciding *whether* a box is masked lives here; `build_export_cmd` stays pure and keys only off the presence of `"mask_path"`.
- `static/ui-mask-line-drag.js` — `UI.maskLineDrag(overlay, {getRect, getMask, onChange, onChangeEnd}) -> {render, destroy}`: the on-stage cut guide, mounted as its own absolutely-positioned `<svg>` inside `#overlay` (a `<video>`/`<img>`'s children never render, so it cannot live inside the box element the way `ui-resize-handles.js` does). Dragging the line shifts `mask_offset` along the line's normal; dragging the round end handle sets `mask_angle` from the pointer's direction off the line's center. Stage px are converted to canvas px with the same `1080 / overlay.clientWidth` scale `stageScale()` uses. Presentational only — `panel-video-box.js`/`panel-image-box.js` own the writes and persistence, via `VideoBoxPreview.setOnMaskChange` / `ImageBoxPreview.setOnMaskChange`, which fire `fn({angle, offset}, done)` (live during the drag, then once on mouseup). Aligning a cut to a real pole by typing numbers is impractical, so this is the primary interaction and the Mask tab's number fields are the precise fallback.

### Video & image boxes (picture-in-picture)

- `VideoBoxLayer` in `app/models.py` — `media_id`/`file_path`, `in_point`/`out_point` source trim, `start` timeline seconds (end always derived as `start + out_point - in_point`), `x`/`y`/`width`/`height` px on the 1080×1920 canvas with height set from source aspect at creation and kept locked on resize, `z_index: int = -1` (defaults just below the default text z_index 0 — see "Unified overlay layer stack (z-order)").
- `static/video-box-preview.js` — `VideoBoxPreview.render(videoBoxes, timelineTime)` / `VideoBoxPreview.setSelectedVideoBox(id, callbacks)` / `VideoBoxPreview.setOnActivate(fn)` (added 2026-07-24, top-toolbar): mounts one always-muted `<video>` per visible box into `#overlay` (a sibling of preview.js's text/caption divs — each sets an explicit CSS z-index from its model's `z_index`), keeps position/size/`currentTime` in sync with the timeline clock, wires drag-to-move (`UI.videoBoxDrag`) + resize (`UI.resizeHandles`, shared with text blocks) onto the selected box. A plain click on a not-yet-selected box, Select-tool only (`window.ToolMode`), fires `onActivate(boxId)` — `editor.js` wires this to open the VIDEO BOX panel and re-render so handles mount immediately; in Text-tool mode the click listener no-ops so the click bubbles to `#stage` and is treated as an insert-text-here click (see `stage-click-router.js`).
- `static/panel-video-box.js` — `VideoBoxPanel.render(selectedId)`: the `#panel-video-box` context section — add-from-media-library picker (`#video-box-picker-list`), time/position/size number fields (position/size via the shared `BoxSizePositionPanel`, see below), delete; one box selected at a time. No TRIM (IN/OUT) fields as of 2026-07-30 (box-tab-common-component) — `in_point`/`out_point` are set once at creation from the source media's full duration and still drive playback/export, just aren't user-editable here. `VideoBoxPanel.createVideoBox(mediaItem)` (exposed 2026-07-30, video-hover-icons-files: pushes a new `VideoBoxLayer` into `project.video_boxes` and returns it, no save/render — caller's responsibility) is reused by `panel-media.js`'s hover-reveal PIP icon, mirroring `panel-image-box.js`'s `ImageBoxPanel.createImageBox`.
- `static/box-panel-size-position.js` — `BoxSizePositionPanel.render(container, box, {onChange, getNaturalSize})` (added 2026-07-30, box-tab-common-component): the shared SIZE + POSITION fields for both `panel-video-box.js` and `panel-image-box.js` — WIDTH/HEIGHT (aspect-locked) + X/Y, plus SIZE's Maximize (fit the box to the largest size that fits the 1080x1920 canvas at its current aspect ratio, centered) and Minimize (shrink to the source media's native pixel size via the caller's async `getNaturalSize()` probe, scaled down only if larger than the canvas) one-shot icon actions. Rebuilds on every call — callers invoke it once per box selection; a field's own `onChange` must never trigger another call (that was the root cause of a bug where typing in WIDTH/HEIGHT tore down and rebuilt the whole Box tab body on every keystroke, kicking focus out of the input mid-edit).
- `static/ui-video-box-drag.js` — `UI.videoBoxDrag(div, {onMove, onMoveEnd}) -> destroy()`: click-and-drag-to-move for stage video boxes — no edit-mode distinction (unlike `ui-text-interaction.js`, any drag is always a move).
- `static/editor.js` — `openVideoBoxPanel()` opens `#panel-video-box` (`VideoBoxPanel.render(null)`); opening any non-video-box panel clears the stage selection via `VideoBoxPreview.setSelectedVideoBox(null, null)`. Drag-to-stitch onto the VIDEO row: `stitchVideoBoxIntoSequence(box, dropTime)` (`static/clip-sequence.js`, extracted from `editor.js` 2026-07-21) is a thin wrapper over the shared `insertClipIntoSequence` (see Timeline above) that inserts then removes the box from `project.video_boxes`. As of 2026-07-31 (overlay-lane-time-drag), the drag gesture that triggers this is `static/timeline-overlay-time-drag.js` (a drop inside `#row-video`'s bounds), not this file's own `dataTransfer` handling — that path also now supports dragging a VIDEO BOX left/right to shift its `start` without stitching, when the drop stays inside the overlays row.
- Export path: see `app/ffmpeg_cmd.py`'s `bands`/banded-export branch under Export pipeline below.
- `static/css/components/video-box-panel.css` — `#panel-video-box` internal layout: add-picker list + time/size/position detail view.

Image boxes mirror video boxes above, added for the image/photo PiP feature — same picture-in-picture mechanics, but no source-video trim (a still image has no in/out points) and no playback/currentTime sync (nothing to play).

- `ImageBoxLayer` in `app/models.py` — `media_id`/`file_path`, `start` timeline seconds + `duration` seconds the box is visible (no `in_point`/`out_point` — a still image has no source timeline to trim), `x`/`y`/`width`/`height` px on the 1080×1920 canvas with height set from the image's aspect ratio at creation and kept locked on resize, `z_index: int = -1` (same convention as `VideoBoxLayer`, just below the default text z_index 0).
- `static/image-box-preview.js` — `ImageBoxPreview.render(imageBoxes, timelineTime)` / `ImageBoxPreview.setSelectedImageBox(id, callbacks)` / `ImageBoxPreview.setOnActivate(fn)`: mounts one `<img>` per visible box into `#overlay` (sibling of the text/caption divs and video-box `<video>`s, same CSS-z-index-from-model convention), keeps position/size in sync with the timeline clock, wires the same drag-to-move (`UI.videoBoxDrag`, reused as-is) + resize (`UI.resizeHandles`) onto the selected box; click-to-select is Select-tool only, same as video boxes.
- `static/panel-image-box.js` — `ImageBoxPanel.render(selectedId)`: the `#panel-image-box` context section — add-from-media-library picker restricted to `media_library` items with `kind === "image"`, Box tab (size/position, via the shared `BoxSizePositionPanel` above) + Time tab (`START`/`DURATION` fields, no trim fields), delete; one box selected at a time.
- Export path: `app/timeline.py`'s `banded_layers()` emits an `"image_box"` band per `ImageBoxLayer` alongside `"video_box"` bands; `app/ffmpeg_cmd.py`'s banded-export branch composites an `"image_box"` band the same way as a `"video_box"` band except the input is `-loop 1 -t <duration>` (a looped still image) instead of a trimmed video clip; `app/main.py`'s export route includes `p.image_boxes` in the banded-export trigger condition alongside `p.video_boxes`.

### Vector shapes (overlay layer)

A free-form rectangle overlay with fill color, opacity, and corner radius — a full citizen of the unified overlay z-order stack, added via a dedicated SHAPE rail entry (no add-from-media picker, no edge-mask tab — corner radius already covers its shaping need). As of 2026-07-31 (shape-tool feature), Shape is a stage tool like Text rather than just a panel with an add-picker: the SHAPE rail entry arms `ToolMode.set("shape")` (see "Toolbar & tool modes" above), and the shape itself is created by click-dragging a rect directly on the stage (`stage-shape-draw.js`) — `ShapePanel.createShape()`'s old plain "+ add a default-sized shape" affordance (`#shape-add`, still present in `panel-shape.js`'s empty state as a fallback) is no longer the primary creation path.

- `ShapeLayer` in `app/models.py` — `start`/`duration` timeline seconds (same convention as `ImageBoxLayer`), `x`/`y`/`width`/`height` px on the 1080x1920 canvas (free-form — no aspect lock, unlike `VideoBoxLayer`/`ImageBoxLayer`), `fill_color`, `opacity` (0.0-1.0), `corner_radius` px, `z_index: int = -1` (same convention as the other box layers).
- `app/shape_render.py` — `write_shape_png(path, width, height, fill_color, opacity, corner_radius)`: rasterizes a filled rounded-rect RGBA PNG via Pillow, alpha = `round(opacity * 255)` inside the rect (clamped `corner_radius` to `min(width, height) / 2`), 0 outside.
- `app/timeline.py` — `shape_end(s)` (mirrors `image_box_end`); `banded_layers()` includes a `"shape"` band per `ShapeLayer`, sorted into the same z-order chain as text/video-box/image-box bands.
- `app/ffmpeg_cmd.py` — a `"shape"` band composites its pre-rendered PNG directly via `overlay` (no scale/alphamerge step — the PNG is already exactly the shape's own size with its fill/opacity baked into its own alpha).
- `app/main.py` — the export route always rasterizes one `{name}-{id[:8]}-band{i}-shape.png` sidecar per `ShapeLayer` (not optional, unlike the edge-mask feature's mask PNGs) and includes `p.shapes` in the banded-export trigger condition.
- `static/shape-defaults.js` — `ShapeDefaults.centeredShape()`: the one place default field values for a newly-created shape live (300x300, centered, `#4C6FFF`, full opacity, no radius, 3s duration).
- `static/shape-color.js` — `ShapeColor.toRgba(hex, opacity)`: pure hex+opacity -> CSS `rgba()` string, used for the stage's live fill.
- `static/shape-preview.js` — `window.ShapePreview.{render, setSelectedShape, setOnActivate}`: mounts one `<div class="shape-box">` per visible shape into `#overlay`, mirrors `image-box-preview.js` minus mask-guide handling; resize is free-form (no aspect lock).
- `static/panel-shape.js` — `window.ShapePanel.{render, createShape, createShapeAt}`: the `#panel-shape` context section — Box (X/Y/WIDTH/HEIGHT)/Style (fill color, opacity, corner radius)/Time (START/DURATION) tabs, Delete footer; mirrors `panel-image-box.js` minus the add-picker and Mask tab. `createShapeAt(rect)` (added 2026-07-31, shape-tool feature) pushes a new `ShapeLayer` into `project.shapes` with `{ id, ...ShapeDefaults.centeredShape(), ...rect }` — i.e. `rect`'s keys (from `stage-shape-draw.js`'s drag, `{x, y, width, height, start}`) override the centered defaults; `createShape()` is now `createShapeAt({})`, kept as the `#shape-add` empty-state button's fallback creation path.
- `static/canvas-point.js` / `static/shape-draw-rect.js` / `static/stage-shape-draw.js` (added 2026-07-31, shape-tool feature) — the Shape tool's stage drag-to-create gesture. `CanvasPoint.fromClient(clientX, clientY, rect)` converts a mouse event's client coordinates to the 1080x1920 canvas space (shared with `stage-click-router.js`'s Text-tool click, extracted from its former private `canvasPointFromClient`). `ShapeDragRect.fromPoints(p1, p2)` normalizes the drag's two corner points into a top-left-anchored `{x, y, width, height}` rect regardless of drag direction. `stage-shape-draw.js` wires `#stage` mousedown (only when `ToolMode.get() === "shape"`) to track the drag, render a live `.shape-box`-styled preview div in `#overlay`, and on mouseup either no-op (drag under 8 canvas px in either dimension — tool stays armed) or: revert `ToolMode` to `"select"` (before the async work, mirroring `stage-click-router.js`'s ordering, so a second drag/click landing mid-flight can't start a second create and a thrown error downstream can't leave the tool stuck armed), call `ShapePanel.createShapeAt({ ...rect, start: Preview.currentTimelineTime() })` (the explicit `start` means the new shape's visible window begins at the current playhead, not at 0 — otherwise selecting it would immediately seek the playhead back to the reel's start, see `panel-shape.js`'s `render()`'s out-of-window seek), save, select the new shape (`onTimelineSelect`, which opens `#panel-shape`), and re-render the stage.
- `static/panel-nav.js` — the SHAPE rail entry (between TEXT and CAPTIONS) arms `ToolMode.set("shape")` the same way the TEXT entry arms `"text"` (see "Toolbar & tool modes" above) rather than opening `#panel-shape` directly — there is no `openShapePanel()`. Opening an *existing* shape (timeline click or the newly-drawn shape's auto-select) still goes through `onTimelineSelect`/`reRenderAfterRestore`, which handle `type: "shape"` the same way they handle `"image-box"`.
- `static/timeline.js` — `renderOverlaysRow` renders a shape's lane labeled "SHAPE" with a resize handle (start/duration), alongside text/video-box/image-box lanes in the unified overlay z-order row.
- `static/timeline-shape-resize.js` — drag-to-resize driver for that handle: mousedown on the lane's `.timeline-resize-handle` changes the dragged `ShapeLayer`'s `duration`, clamped to a 0.1s minimum; exact structural mirror of `static/timeline-image-resize.js` (same `#row-overlays` delegated-mousedown pattern), retargeted at `project.shapes`/`selected.type === "shape"`/`ShapePanel.render`.
- `static/timeline-overlay-layers.js` — `mergedEntries`/`renumber` include shapes in the cross-layer z-order list.
- `static/css/components/shape-panel.css` — `#panel-shape`'s internal layout, mirrors `image-box-panel.css`.
- `static/css/components/stage.css` — `.shape-box` base styling (position/box-sizing), alongside `.video-box`/`.image-box`.

### Text blocks & rich-text formatting

- `TextPreset` in `app/models.py` — `font` defaults to `"Public Sans"` (2 vendored families); `weight: int = 400` (400/500/600/700, replaced `bold: bool` 2026-07-19, migrated on load); `italic`/`underline: bool = False`; `box_width_mode`/`box_height_mode: str = "fit"` + `box_width`/`box_height: int` (independent per-axis fixed-vs-fit sizing); `box_background`/`box_background_color` (replaces old `box`/`box_color`, migrated on load); `box_border_width`/`box_border_color`/`box_border_radius`; `usage_count: int = 0` (drives the Style tab's most-used list — see Saved style presets); `highlight_color`/`highlight_mode` (shared with captions, see below; `max_words_per_line` removed 2026-07-24, caption box sizing — word/line counts are now derived from box size via `app/caption_layout.py`'s pagination, not a manual field; `highlight_mode: str = "current_word"` is captions-only per-word karaoke mode, one of `"off"`/`"current_word"`/`"progressive_fill"` — the old `"background"` value is legacy-only as of 2026-07-30, spotlight per-word styles: it self-heals to `"current_word"` on load via `app/models.py`'s own `@model_validator(mode="before")` (`_migrate_legacy_box_fields`, the server-authoritative path) and, for in-memory presets, `static/panel-captions.js`'s `ensureCaptionPreset`); `highlight_border_radius: int = 4` (added 2026-07-24, highlight background + border-radius feature): corner radius for TEXT's/CAPTIONS' always-on marker-highlight rect (`_highlight_dialogues`/`_caption_highlight_dialogues`), replacing the old hardcoded `HIGHLIGHT_RADIUS = 4` constant in `app/ass_render.py`; `highlight: bool = False` (block-level highlight default); `spotlight_color: str = "#FFD400"`/`spotlight_outline_color: str = "#000000"`/`spotlight_outline_px: int = 0`/`spotlight_shadow: bool = False`/`spotlight_shadow_color: str = "#000000"`/`spotlight_shadow_offset_x/_y: int = 4`/`spotlight_shadow_blur: int = 0`/`spotlight_highlight: bool = False`/`spotlight_highlight_color: str = "#FFD400"`/`spotlight_highlight_border_radius: int = 4` (added 2026-07-30, spotlight per-word styles, captions-only): the active word's own independent Color/Outline/Shadow/Highlight overrides, entirely separate fields from the base `color`/`outline_*`/`shadow*`/`highlight*` fields they parallel — `spotlight_outline_px > 0` is the sole outline on/off gate (0 = off), the same "0 means off" convention as `box_border_width`, with no separate `spotlight_outline` boolean (the base Outline control has no on/off boolean either); rendered by `static/style-section-spotlight.js`'s Design-tab subpage and `app/ass_render.py`'s `_current_word_dialogues`/`_active_word_highlight_dialogues`; `shadow: bool = False` + `shadow_color`/`shadow_offset_x`/`shadow_offset_y`/`shadow_blur` (added 2026-07-22, text shadow feature): whole-preset drop shadow, shared with captions (same `TextPreset`), rendered live via CSS `text-shadow` (`static/preview-text.js`/`static/preview-captions.js`) and burned into export via ASS `\4c\4a\xshad\yshad\blur` override tags (`app/ass_render.py`'s `_shadow_tag` helper, applied in `_block_dialogue`/`_karaoke_dialogue`/`_current_word_dialogues`); `text_case: str = "none"` (added 2026-07-24, case styling: `"none" | "upper" | "lower"`, whole-preset only, no `FormatRun` override) — displayed via CSS `text-transform` in preview (stored `heading`/caption word text is never mutated) and burned into export as transformed characters via `app/text_case.py`'s `apply_text_case` (ASS has no text-transform primitive), UI control in `static/style-section-emphasis.js` (shared by both panels as of shared-style-sections batch 3, replacing the old `text-panel-case.js`/`caption-panel-case.js`).
- `FormatRun` in `app/models.py` — Phase 5, added 2026-07-19: `start`/`end` character offsets into a `TextBlockLayer.heading` string, plus sparse-optional style overrides (`font`/`size_px`/`color`/`outline_color`/`outline_px`/`weight`/`italic`/`underline`/`highlight`/`highlight_color`), each `None` by default so an unset field falls through to the block's base `TextPreset`.
- `TextBlockLayer` in `app/models.py` — single `heading` line, may contain `\n` for multiline; `formatting_runs: list[FormatRun] = []` (empty list renders/exports identically to pre-Phase-5); `z_index: int = 0` (see "Unified overlay layer stack (z-order)").
- `static/text-panel-background.js` / `text-panel-border.js` / `text-panel-time.js` — remaining TEXT-panel-only Box/Time tab controls: Background settings row + drill-down subpanel (on/off toggle + color/opacity, `text-panel-background.js`) and Border settings row + drill-down subpanel (on/off toggle + width/radius/color, `text-panel-border.js`), both added 2026-07-29 (settings-row None state) — Border has no boolean field, `box_border_width === 0` IS "no border", so its ON toggle sets width to `2` only when currently `0`; TIME holds start/end (`text-panel-time.js`). TEXT ALIGN and POSITION (the absolute x/y fields + stateless anchor shortcut that used to live in `text-panel-align.js`/`text-panel-position.js`) moved to the shared `static/style-section-align.js`/`static/style-section-position.js` (2026-07-29/30, shared-style-sections batch 5) and are no longer TEXT-only files — POSITION stays on the Box tab (see the Box tab section below); TEXT ALIGN moved again, into the Design tab, 2026-07-30 (text-align-subpage-move). Font Family/Weight/Size/Italic/Underline/Color/Outline/Shadow/Highlight are likewise shared `static/style-section-*.js` files as of shared-style-sections batches 2-4 — see "Shared style sections (TEXT + CAPTIONS)" below. As of 2026-07-29 (settings-row None state): the Background/Border rows read "None" when off (Background `!preset.box_background`, Border `box_border_width === 0`) via the shared `SettingsRowValue.orNone(isOn, text)` (`static/settings-row-value.js`).
- `static/panel-text.js` — `anchorPositionX(value, boxWidth, align)`/`anchorPositionY(value, boxHeight)` (added 2026-07-22, positioning-rules, replacing the old fixed `POSITION_ANCHORS_X/Y` thirds-of-canvas constants): pure, edge-flush against the 1080x1920 canvas using the box's own actual rendered width/height — `left`/`top` → `0`, `right`/`btm` → `canvas - size`, `mid` → centered, `anchorPositionX`'s third `align` argument compensates for `stage.css`'s align-keyed CSS transform — so TOP/BTM/LEFT/RIGHT place the box's *edge* flush with the canvas edge rather than its top-left corner at a fixed fraction; shared by `static/style-section-position.js` (via `target.getBoxSize()`, used by both TEXT and CAPTIONS as of shared-style-sections batch 5). `ensureTextPreset(id)` (lazily creates/loads a preset by id); multi-block selection (added 2026-07-21, replacing the old single-implicit-block `ensureTextBlock()`): `currentTextBlock() -> block | null` (the explicitly-selected `selectedTextBlockId` block, else falls back to and selects the first block, else `null` — never creates), `selectTextBlock(id)`, `addTextBlock() -> block` (explicit creation: new block + its own preset at the playhead, selects it, does not render), `addTextBlockAndEdit()` (wraps `addTextBlock()` with the panel/timeline re-render + save, wired to `#text-add-block-btn`), `deleteSelectedTextBlock()` (added 2026-07-21, Task 7: removes `currentTextBlock()`'s block from `project.text_blocks` and its preset from `project.text_presets`, clears `selectedTextBlockId`, saves and re-renders the panel/timeline so `currentTextBlock()`'s own fallback picks the next block or shows the empty state — wired to `#text-delete` and to the Delete key in `editor.js` when `selected.type === "text"`; no `duplicateTextBlock()`/`#text-duplicate` — removed 2026-07-30 alongside every other panel's Duplicate button, see "Shared UI components"). `showTextTab()` (the TEXT tab-bar's `onSelect` callback) no longer hides `#text-edit-actions` on the Style tab (fixed 2026-07-30 — Delete is now visible and bottom-pinned on every tab, including Style, via the `.panel-danger-footer` pattern). `renderTextPanel()` is empty-state aware: when `currentTextBlock()` is `null` it shows `#text-empty-state` ("No text yet." + "+ Add text"), hides `#text-accordions`, clears the stage selection, and returns early. As of 2026-07-30 (shared-style-sections batch 5) `renderBoxPanel()` is gone: `renderBoxTab()` calls `TextPanel.renderBackground()`/`TextPanel.renderBorder()` (unchanged, still their own settings-row + subpage files) then lazily builds and renders `StyleTab.box(#text-box-shared-body, textStyleTarget, { sizeModes: true })` — replacing the old inline SIZE/WIDTH/HEIGHT fields plus the now-deleted `text-panel-align.js`/`text-panel-position.js`. `handleBoxResizeEnd()` calls `renderBoxTab()` where it used to call `renderBoxPanel()`; as of 2026-07-30 (fit/fixed auto-freeze) `handleBoxResizeEnd()` was simplified — dragging a resize handle no longer has a FILL-preserving branch, since FILL sizing mode no longer exists. A text block's box sizing now auto-freezes from `"fit"` (auto-size-to-content) to `"fixed"` (manually resizable) automatically the moment the user finishes editing (blurs) it with non-empty text, via `onEditEnd`, rather than via an explicit user-picked SIZE toggle (the old FIT/FREE/FILL button group, removed from `static/style-section-box.js` the same day) — `onEditEnd` resolves the block that actually ended editing by the id `preview-text.js` passes through (not the currently-selected block, which can differ — see `preview-text.js`'s entry below). `handleBoxResize()`/`handleBoxResizeEnd()`/`handleBoxMove()`/`handleBoxMoveEnd()` (stage resize/move callbacks — position is `TextPreset.x`/`y` absolute pixel coordinates, align-aware horizontal edge, always-top vertical edge, see [2026-07-19-absolute-pixel-coordinates-design.md](docs/superpowers/specs/2026-07-19-absolute-pixel-coordinates-design.md)). No side-panel heading field — the heading is edited by clicking the block directly on the stage. `editor.js` still calls these as plain globals, and drives per-block selection itself: `onTimelineSelect`'s text branch and the `Preview.setOnStageTextActivate` callback both call `selectTextBlock(id)` before opening the panel, so clicking a block in the timeline TEXT row or on the stage targets that specific block. As of 2026-07-29 (shared-style-sections batch 2) it builds a StyleTarget.forTextBlock(), a StylePanelHost(#panel-text-main, #panel-text) and a StyleTab.design(#text-design-mount, …) once at load, and calls `await textDesignTab.render()` from renderTextPanel() in place of the deleted TextPanel.renderFontFamily/renderFontWeight calls; renderTextPanel()'s per-subpanel `hidden` resets became `textStyleHost.closeAll()`.
- `static/preview-text.js` (`window.PreviewText`, extracted from `static/preview.js` 2026-07-21) — `renderText(project, presets, timelineTime)` composites one `.text-block` div per visible block into `#overlay`. Phase 5 rich-text: splits `block.heading` into one `<span class="text-run">` per `formatting_runs` boundary, each independently resolving style from `(run && run.X) || preset.X`. Box sizing (as of 2026-07-30, fit/fixed auto-freeze; previously included a `maybeRefitFillText`/`fitCache` FILL auto-font-refit path, now deleted along with FILL mode): `widthIsBoxed`/`heightIsBoxed` check `box_width_mode`/`box_height_mode !== "fit"` — i.e. anything other than actively auto-sizing (including a legacy `"fill"` preset from before this mode existed) is treated as an explicit fixed box for stage layout, matching `app/ass_render.py`'s own `("fixed", "fill")` tolerance on export — a block auto-freezes from `"fit"` to `"fixed"` on blur once it has content (see `panel-text.js`'s `onEditEnd`), rather than via a user-facing FILL toggle. `onEditEnd(text, blockId)` passes the *ending* block's own id (captured where its div was built) through to `panel-text.js`, rather than relying on whichever block happens to be selected — a stage/timeline selection change doesn't blur an open edit, so the editing block and the selected block can differ at blur time. `getActiveFormatSelection() -> {blockId, start, end} | null` tracks the current non-collapsed selection for the FONT-accordion files to target. `setSelectedTextBlock(blockId, callbacks)` mounts `UI.textInteraction`/`UI.resizeHandles` on the selected block. `getBoxSizeCanvasPx(blockId)` (added 2026-07-22, positioning-rules) reads a block's live on-stage rendered size back in canvas px, via `dataset.blockId` on each `.text-block` div. Renders `preset.shadow`'s CSS `text-shadow` on the block div (added 2026-07-22, text shadow feature). Highlighted-run spans set CSS `border-radius` from `preset.highlight_border_radius` alongside their existing `background-color` (added 2026-07-24, highlight background + border-radius feature; previously only `background-color` was set, no radius). Passes `onSelectClick` (added 2026-07-24, top-toolbar) to each block's `UI.textInteraction`, wired to the same `onStageTextActivate` callback `onEditStart` already uses — so a Select-tool click and a Text-tool click both route through the one place that switches the right panel to TEXT. Exactly one block is ever in edit mode (added 2026-07-29): both activation paths call `takeEditOnOtherBlock(blockId)` first, which clears the tracked edit state (`editingBlockId`/`editingDiv`/`editingHandle`) and hands back the other block's handle for the caller to `exitEditMode()` once its own state is in place — closing re-renders, and `renderText` only preserves the div it currently tracks as `editingDiv`. Without this the previously-edited block stayed `contentEditable` and kept the focus `renderText` restores to it, so keystrokes landed in a block the right panel had already moved off; `onEditEnd` correspondingly only clears the tracked edit when the ending block still owns it. `static/preview.js` exposes all of these as thin `Preview.*` delegating wrappers (`getTextBoxSize`/`getCaptionBoxSize` for the size getters) so no caller changes.
- `static/ui-text-interaction.js` — `UI.textInteraction(div, {onEditStart, onInput, onEditEnd, onMove, onMoveEnd, onSelectionChange, onSelectClick})`: click-to-edit (`contentEditable`) vs drag-to-move vs drag-to-select, hit-tested at `mousedown` via `UI.rangeContainsPoint`. Tool-mode gated (added 2026-07-24, top-toolbar): a plain click enters edit mode only when `window.ToolMode.get() === "text"`; otherwise (Select tool) it fires `onSelectClick` instead and glyph hit-testing/native text-selection is skipped entirely (every mousedown is a box-move drag). The returned `enterEditMode()` handle is never gated — programmatic calls (e.g. `PreviewText.enterEditMode`) always work regardless of tool. `exitEditMode()` (added 2026-07-29) runs the same teardown a real blur would (commit text via `onEditEnd`, drop `contentEditable`) without waiting for a blur event — needed because a stage click on a *different* block never moves focus off this one, so nothing else would ever close the edit.
- `static/ui-text-selection.js` — `UI.textSelectionOffsets(div)`/`UI.rangeContainsPoint(div, x, y)`: native selection -> char offsets, and glyph-only hit-testing (excludes the resize-handles overlay sibling).
- `static/ui-resize-handles.js` — `UI.resizeHandles(container, {getSize, onResize, onDragEnd}) -> destroy()`: 8 corner/edge drag handles, shared with video boxes.
- `static/font-fit.js` — `FontFit.canvasMeasurer`: shared canvas text measurer, used by caption pagination (`static/preview-captions.js`'s `CaptionLayout.paginateWords`) — mirrors `app/font_metrics.py`'s `pil_font_measurer`. (`wrapText`/`fitFontSize`, the old BOX FILL auto-font-sizing binary search, were deleted 2026-07-30, fit/fixed auto-freeze, along with the FILL sizing mode that was their only caller.)
- `app/ass_render.py` — text-block dialogue: `\pos` anchor, `\fad`+`\t` scale pop for `entrance="fade_pop"`, `\n` -> ASS `\N`. Box background/border via a separate vector-drawn (`\p1`) `Dialogue` line from `_box_dialogue()`, sized/wrapped via `_wrapped_lines_and_size()` (uses `app/font_metrics.py`; padding-free as of 2026-07-29 — the box hugs the glyphs exactly, matching `static/preview-text.js`'s now-unpadded `.text-block` div; the surviving `CAPTION_PAD_X_EM`/`CAPTION_PAD_Y_EM` constants, renamed from `BOX_PAD_*`, are captions-only). Phase 5 rich-text: `_measure_range_for`/`_run_style_tag`/`_tagged_text` build per-run inline ASS override tags; `_highlight_dialogues` emits one rounded-rect `Dialogue` per highlighted run × visual line, corner radius from `preset.highlight_border_radius` (added 2026-07-24, highlight background + border-radius feature, replacing the old hardcoded `HIGHLIGHT_RADIUS = 4` constant). `_active_word_highlight_dialogues` (added 2026-07-24 as `_background_word_dialogues`, renamed and generalized 2026-07-30 spotlight per-word styles) draws a rounded rect behind the currently-active word (same per-line offset/width math as `_highlight_dialogues`, via the shared `_line_word_offsets`/`_line_left_origin` helpers), keyed off `preset.spotlight_highlight`/`spotlight_highlight_color`/`spotlight_highlight_border_radius` rather than a "background" mode — it is no longer mode-exclusive: `render_caption_ass` calls it whenever `highlight_mode != "off"`, alongside (not instead of) `_current_word_dialogues`'s text-color swap. `_caption_highlight_dialogues` (added 2026-07-25, captions marker highlight; reworked 2026-07-30, caption highlight/spotlight spacing fix — plural, one call per page) is the captions-side counterpart to `_highlight_dialogues` — an always-on background rect **per visible line** (not one rect for the whole caption box), gated on `preset.highlight` rather than `highlight_mode`; see the Captions section below.
- `app/font_metrics.py` — `wrap_text_runs(text, measure_range, max_width_px)`: Phase 5 range-aware word-wrap returning per-line character-offset spans, consumed by `ass_render.py`'s per-run tagging.
- `static/css/components/style-panel.css` — `#panel-text`'s four tab panes Style/Design/Box/Time, switched by `UI.tabBar` (`static/ui-tab-bar.js`).

### Shared style sections (TEXT + CAPTIONS)

Added 2026-07-29/30. The TEXT and CAPTIONS panels style the same entity — a `TextPreset` — so as of this initiative they share one set of markup-owning components instead of two hand-copied sets (15 `text-panel-*.js`/`caption-panel-*.js` files deleted across six batches). Four layers, each with one job.

- **Pure modules.** `static/font-size-scale.js` (`FontSizeScale.{FONT_SIZE_PRESETS, stepFontSizePreset}` — one scale `[12,14,16,18,21,24,36,45,56,72,96]` for both panels), `static/format-run-write.js` (`FormatRunWrite.upsertFormatRun(block, start, end, field, value)`), `static/style-fields.js` (`StyleFields.{STYLE_FIELD_NAMES, styleFieldsOf}` — the saved-style field list, including `highlight`). Each exposes `window.X` for the browser and a guarded `module.exports` for `node --test "tests/js/**/*.test.js"`.
- **Style targets.** `static/style-target-text.js` (`StyleTarget.forTextBlock(deps?)`) and `static/style-target-caption.js` (`StyleTarget.forCaptionTrack(deps?)`) return the same object shape: `{ kind, supportsFormatRuns, getPreset, getFieldValue, setField, setFields, setPresetField, previewField, cancelPreview, clearFormatRuns, rerenderPreview, rerenderPanel, getBoxSize }`. The target absorbs every real difference between the panels — which preset is read, which preview re-renders, and TEXT's selection-aware `FormatRun` writes — so every section built on top of it is branch-free. On the text target, `setField`/`setFields` upsert a per-range `FormatRun` when a stage selection on the current block is active (for a small allowlisted set of fields — `font` is deliberately excluded, per-range fonts were raised and declined), else fall back to a whole-preset write; `setPresetField` is always whole-preset. On the caption target every write is whole-preset (a caption track has no per-range overrides) and `clearFormatRuns()` is a no-op. `deps` is optional and injectable, which is what makes both targets unit-testable outside a browser (`tests/js/style-target-*.test.js`).
- **Panel host.** `static/style-panel-host.js` — `StylePanelHost(mainEl, drillEl)` returns `{ page(title, buildBody, options?), closeAll() }`; `page(...)` returns `{ open(), close(), bodyEl }` and the host builds each subpage's `UI.subPanelHeader` itself. Replaces seven hand-copied `openXPanel`/`closeXPanel` pairs and the old per-control `#panel-text-main`/`#panel-captions-main` juggling.
- **Sections.** `static/style-section-*.js`, one control group per file, namespaced `window.StyleSection.*`, signature `(container, target, options) -> { render() }`. Each **builds its own markup** into `container` — the property that stops the two panels drifting apart again, since the divergences this initiative fixed (a missing `.btn-group-inline` class, a font-size scale that stopped at 56 on one side, a caption-only `styleFieldsOf` silently dropping `highlight`) were all markup/copy-paste bugs, not logic bugs. **Built once, rendered many times:** the factory builds DOM and attaches listeners; `render()` only refreshes displayed values — never call a `UI.*` builder from `render()`. `render()` may be async (`fontWeight` awaits `Api.listFontWeights`, `presetLibrary` awaits `Api.listPresets`) and callers ignore the returned promise. The eleven sections: `font-family`, `font-weight`, `size`, `emphasis`, `align`, `color`, `outline`, `shadow`, `highlight` (Design tab, composed by `style-tab-design.js`; `align` moved here from the Box tab 2026-07-30, text-align-subpage-move), `spotlight` (Design tab, CAPTIONS-only), `box`, `position` (Box tab, composed by `style-tab-box.js`), and `preset-library` (Style tab, composed by `style-tab-style.js`).
- **Tab composers.** `static/style-tab-design.js`, `style-tab-box.js`, `style-tab-style.js` — `StyleTab.{design, box, styleLibrary}(container, target, options) -> { render() }`, each the single place its tab's section order is defined rather than re-enumerated by both panels. `StyleTab.design` renders `fontFamily → fontWeight → size → emphasis → align → color → outline → shadow → highlight → [spotlight]` (`opts.spotlight`, CAPTIONS-only; `align` moved here 2026-07-30, text-align-subpage-move); `StyleTab.box` renders `box → position` (`opts.sizeModes` true for TEXT, false for CAPTIONS); `StyleTab.styleLibrary` renders the one preset-library section.
- **Not shared, by design.** Background and Border (`text-panel-background.js`/`border.js`, `caption-panel-background.js`/`border.js`) predate this initiative's settings-row pattern and were deliberately kept as their own per-panel files rather than folded into `style-section-box.js` — see that file's own map entry and its commit `645ccf3` for why. `text-panel-time.js`/`caption-panel-{words,filler-words}.js` are genuinely single-panel, no counterpart to share with.
- **Tests.** `tests/js/*.test.js` via `node --test "tests/js/**/*.test.js"`. Covers the pure modules and both targets. **Stated gap:** the section components build DOM and are not unit-tested — kept thin, with decision logic pushed into the pure modules/targets, and each migration batch verified live in the browser on a throwaway project instead.

### Saved style presets

Shared preset library (distinct from a block's live working style) used by both TEXT and CAPTIONS panels.

- `static/style-tab-style.js` / `static/style-section-preset-library.js` — the Style tab, shared verbatim by both panels as of 2026-07-30 (shared-style-sections batch 6, replacing `text-panel-style.js` + `caption-panel-style.js`). `StyleTab.styleLibrary` is a one-section wrapper; `StyleSection.presetLibrary(container, target, {})` owns everything: builds its markup once (save button, inline form container, card `<ul>`) and `render()` refetches via `Api.listPresets()`, sorts by `usage_count` descending, and repaints `UI.stylePresetCard`s. "+ Save current style" hides itself and opens `UI.styleSaveForm` ("save mode"); while open, every card's click handler is `overwriteSavedPreset` (replaces the saved style's fields in place, keeping `id`/`name`/`usage_count`) instead of `applySavedPreset`. Every card carries a hover-revealed trash button wired to `Api.deletePreset`, no confirmation step. `applySavedPreset` calls `target.clearFormatRuns()` first (a whole-look reset that empties a text block's per-range `FormatRun` overrides — a deliberate no-op on the caption target, which is why one file serves both panels branch-free), then bulk-assigns `StyleFields.styleFieldsOf(saved)` (`static/style-fields.js`) onto `target.getPreset()`, commits with one `target.setPresetField()` call, bumps `usage_count`, re-POSTs the preset, and calls `target.rerenderPanel()`. Fixes a real bug from the old per-panel files: `caption-panel-style.js`'s hand-copied field list omitted `highlight`, so a saved caption style with MARKER on came back with MARKER off — the shared `StyleFields.STYLE_FIELD_NAMES` list includes it, so this no longer happens on either panel.
- `static/ui-style-preset-card.js` — `UI.stylePresetCard(preset, {onClick, onDelete}) -> <li>` (added 2026-07-23): renders a saved `TextPreset` as a grey `.list-row` card showing its actual rendered styling (font/size/weight/italic/underline/color/outline/shadow, scaled down from the 1080x1920 canvas basis to a fixed preview width) plus its name — replacing the old plain-text-name row, shared by both TEXT and CAPTIONS Style tabs; optional hover-revealed trash button (`onDelete` callback) that calls `e.stopPropagation()` before invoking the callback so clicking trash doesn't also apply the style (omitting `onDelete` renders the card exactly as before).
- `static/api-list-presets.js` / `api-save-preset.js` / `api-delete-preset.js` — `Api.listPresets()`/`Api.savePreset(preset)`/`Api.deletePreset(presetId)`: `GET`/`POST`/`DELETE /api/presets`.
- `app/main.py` — `GET/POST/DELETE /api/presets`.
- `app/store.py` — `save_preset`, `load_presets` (global `<data>/presets.json`).

### Captions & transcription

- `CaptionWord`/`CaptionTrack` in `app/models.py` — `CaptionTrack.z_index: int = 0` (see "Unified overlay layer stack (z-order)"; note CAPTIONS itself stays a separate fixed row, not part of the draggable overlay stack); `preset_id: str` links to a `TextPreset`, defaulted via `new_id()` so old saved projects self-heal. The linked `TextPreset`'s `max_words_per_line` field was removed 2026-07-24 (caption box sizing) — see `app/caption_layout.py` below.
- `app/transcribe.py` — `words_from_segments(segments) -> list[CaptionWord]` (pure), `transcribe_file(path)` (lazy `WhisperModel("large-v3", device="cuda")`, module-level cache; requires the `ml` optional dependency group). CUDA-runtime-present-but-unusable fallback (added 2026-07-23, auto-detect-captions-bug fix): a CUDA device can exist (so `WhisperModel(...)` constructs fine) without its CUDA Toolkit runtime libs (e.g. `cublas64_12.dll`) actually installed — that only surfaces as a `RuntimeError` once transcription runs, not at construction. `transcribe_file` catches that and retries once via `_fall_back_to_cpu()` (rebuilds the cached model with `device="cpu", compute_type="int8"`); a second failure propagates.
- `app/caption_word_estimate.py` — `estimate_word_timings(word: CaptionWord) -> list[CaptionWord]` (pure): splits a multi-word entry into per-word sub-ranges by character-offset interpolation within the entry's own `[t_start, t_end]`; a single-word entry passes through unchanged. Consumed by `app/caption_layout.py`'s `paginate_words`.
- `app/caption_layout.py` / `static/caption-layout.js` — `paginate_words(words, measure, box_width_px, box_height_px, font_size_px, line_height=1.15) -> list[list[list[CaptionWord]]]` / `CaptionLayout.paginateWords` (same signature/shape, added 2026-07-24, caption box sizing): pure word-wrap + pagination — packs words onto lines by measured pixel width, then paginates lines by box height, so what renders on stage/export always fits the caption preset's fixed box size instead of a flat manual max-words-per-line count. Replaces `app/ass_render.py`'s deleted `group_words` (Python) and `static/preview-captions.js`'s prior use of `Timeline.groupWords` (JS) as the caption-layout algorithm — `static/timeline.js`'s own `groupWords` is unrelated (still used for the timeline CAPTIONS row) and untouched. Consumed by `app/ass_render.py`'s `render_caption_ass` and `static/preview-captions.js`'s `renderCaptions`.
- `app/main.py` — `POST /api/projects/{pid}/transcribe` (reworked 2026-07-31, auto-caption progress): starts transcription as a background job via `app/export_jobs.py` and returns `{job_id}` immediately (200) instead of blocking; `GET /api/transcribe-jobs/{job_id}` polls `{status, percent, output_path, error}`. `transcribe.transcribe_file`'s `ImportError` (the `ml` extra isn't installed, as on the Railway deployment, added 2026-07-23 cloud hosting) or `RuntimeError` (transcription failed even after the CPU fallback above, e.g. no usable CPU/GPU backend at all — added 2026-07-23, auto-detect-captions-bug fix) now surface as the job's `error` field rather than an HTTP error response, same user-facing messages as before.
- `static/panel-captions.js` — `defaultCaptionPreset(id)`/`ensureCaptionPreset(id)`/`ensureCaptionTrack()` (lazily creates/loads `project.captions` and its preset), `renderCaptionPreview()`, `renderCaptionPanel()` (the `#panel-captions` orchestrator: delegates to each `CaptionPanel.render*()`), its tab-bar/divider wiring (`UI.tabBar`; tab order as of 2026-07-31 (auto-caption/filler-words panel move) is Closed captions/Auto/Style/Design/Box, Closed captions first — added 2026-07-23; "Filler words" was renamed "Auto" and its icon switched `slice`→`sparkles`; Design tab is one body, `#caption-font-body`). Transcription lives in this panel's own Auto tab as of 2026-07-31 — `runAutoCaption()` (`static/caption-panel-auto-caption.js`) and the Language settings row (`static/caption-panel-language.js`, `CaptionPanel.renderLanguage()`, called from `renderCaptionPanel()`) — after a stint in the AUDIO/VIDEO panel's Auto tab from 2026-07-29 through 2026-07-31 (`static/audio-panel-auto-caption.js`/`audio-panel-language.js`, since renamed back). The Closed-captions tab body (`#caption-words-body`) holds only the transcript word list (`caption-panel-words.js`). As of 2026-07-29 (shared-style-sections batch 2) it does the same as panel-text.js with StyleTarget.forCaptionTrack(), StylePanelHost(#panel-captions-main, #panel-captions) and StyleTab.design(#caption-design-mount, …) — which also moved the Weight row above SIZE, matching TEXT's order.
- `static/caption-panel-background.js` / `caption-panel-border.js` — CAPTIONS' remaining panel-only Box-tab controls: Background settings row + drill-down subpanel and Border settings row + drill-down subpanel (both added 2026-07-29, settings-row None state), same row+subpanel pattern as their TEXT-panel equivalents, pointed at the caption track's preset via `ensureCaptionTrack()`/`ensureCaptionPreset()` (`panel-captions.js`). Font Family/Weight/Size/Italic/Underline/Case/Color/Outline/Shadow are shared `static/style-section-*.js` files (shared-style-sections batches 2-3) — the once-separate `caption-panel-font-*.js`/`caption-panel-outline.js`/`caption-panel-shadow.js`/`caption-panel-case.js` files no longer exist. As of 2026-07-30 (shared-style-sections batch 5): CAPTIONS' Box tab (WIDTH/HEIGHT — always visible, a caption box is always a fixed size; TEXT ALIGN; POSITION) is `static/style-tab-box.js` with `{ sizeModes: false }`, replacing the deleted `caption-panel-box.js` — Background/Border stay as siblings of that shared mount inside `#caption-box-body`, unaffected by the move. As of 2026-07-29 (settings-row None state): the Background/Border rows read "None" when off via the same shared `SettingsRowValue.orNone` (`static/settings-row-value.js`) used by the TEXT panel.
- CAPTIONS' Design tab holds two related-but-independent settings rows (see `app/ass_render.py`'s `_caption_highlight_dialogues` vs. `_current_word_dialogues`/`_active_word_highlight_dialogues`): **Highlight** (`static/style-section-highlight.js`, shared verbatim with TEXT — on/off toggle + `highlight_color` + `highlight_border_radius`, an always-on background rect behind the whole caption box, `preset.highlight`) and **Spotlight** (`static/style-section-spotlight.js`, CAPTIONS-only — MODE group `off`/`current_word`/`progressive_fill` plus, per active word, its own Color/Outline/Shadow/Highlight built from the four shared `style-section-*.js` sections reused against the preset's `spotlight_*` fields; never "None" since a mode is always set). As of 2026-07-30 (spotlight per-word styles) the two rows no longer share any fields — Spotlight's per-word rect uses `spotlight_highlight`/`spotlight_highlight_color`/`spotlight_highlight_border_radius`, fully independent of Highlight's `highlight_color`/`highlight_border_radius`. Before 2026-07-30 these were one bundled "Highlight" row/subpanel (formerly `static/caption-panel-highlight.js`, since removed) — split because a static box background (TEXT's meaning of "highlight") and a per-word karaoke mode are different features that happened to share one row and one label.
- `static/caption-panel-words.js` — `CaptionPanel.renderWords()`: the CAPTIONS panel's Closed-caption tab pane (was a "Caption words" settings-row + drill-down, folded into a permanent tab 2026-07-21), every `CaptionWord` sorted by `t_start` with inline-editable text (empty text deletes the word) and inline-editable `t_start`/`t_end` (number inputs, one decimal; a pure `clampWordTiming()` helper clamps `t_start >= 0` and rejects the edit — reverting the field, no save — unless `t_start < t_end`; a valid commit saves and re-renders both the stage caption preview and the timeline CAPTIONS row). The whole `#caption-transcript-section` (TRANSCRIPT label + list) stays `hidden` until the caption track has at least one word (added 2026-07-23).
- `static/caption-panel-filler-words.js` — the CAPTIONS panel's Auto tab: `Project.filler_words` list, plus an "Auto-remove filler words" button that cuts every transcribed word matching that list straight out of the timeline via `FillerWords.detectRanges` (`static/filler-word-ranges.js`) + the same `/auto-slice/apply` endpoint AUTO SLICE uses (`Api.applyAutoSlice`) — no silence detection, no review step. As of 2026-07-31 (auto-caption/filler-words panel move): the list itself (add-field + entries) is no longer always-inline — it's a drill-down subpanel (`#panel-captions-filler`) opened from a `UI.settingsRow` (`#caption-filler-words-row`, label "Filler words", value "`N words`"); the whole FILLER WORDS section (button + row, `#caption-filler-section`) stays `hidden` unless `project.captions && project.captions.words.length` — matching this list against no transcript was never meaningful. Each list entry that occurs anywhere in the current transcript gets a `message-circle-warning` icon next to it (grouped with the word in a `.font-list-row-name-group` span so `.font-list-row`'s `space-between` layout doesn't push it away from the word). `window.CaptionPanel.renderFillerWords()` toggles the section's `hidden` and refreshes both the row and the list on every call, so the gating stays correct even while the subpanel is closed.
- `static/timeline.js` — `groupWords(words, max)` (shared with preview.js, not duplicated; expands each entry through `Timeline.estimateWordTimings` before sorting/chunking, so multi-word entries split into per-word groups).
- `static/caption-word-estimate.js` — `Timeline.estimateWordTimings(word)` (pure, mirrors `app/caption_word_estimate.py`): splits a multi-word `CaptionWord`-shaped object into per-word sub-ranges by character-offset interpolation. Consumed by `static/timeline.js`'s `groupWords`.
- `static/filler-word-ranges.js` — `FillerWords.{normalizeWord, detectRanges}` (pure, Unicode-aware mirror of `app/auto_slice.py`'s `normalize_word`/`detect_filler_ranges`): given a project's caption words + filler-word list, returns each matching word's `{start, end}` timeline range. Consumed by `static/caption-panel-filler-words.js`'s Auto-remove button.
- `static/caption-clip-sync.js` — `window.CaptionClipSync.{clipRanges, shiftCaptionsAfterEdit, resyncCaptionsAfterReorder}` (added 2026-07-30, caption/clip sync): pure helpers keeping caption words aligned with the MAIN clip they overlap in time — `clipRanges(clips)` mirrors `app/timeline.py`'s clip_starts walk; `shiftCaptionsAfterEdit(words, editStart, oldDuration, newDuration)` handles a delete or insert as one splice point (delete: newDuration=0, insert: oldDuration=0); `resyncCaptionsAfterReorder(words, oldRanges, newRanges)` shifts each word by its owning clip's own start delta, for drag-reorder's non-monotonic permutation. Consumed by `panel-video.js`'s `deleteClip()`/`moveClipTo()` and `clip-sequence.js`'s `insertClipIntoSequence()` (see "Video clips" and "Media library & import" below).
- `static/preview-captions.js` (`window.PreviewCaptions`, extracted from `static/preview.js` 2026-07-21) — `renderCaptions(project, presets, timelineTime)`: paginates `project.captions.words` via `CaptionLayout.paginateWords` (added 2026-07-24, caption box sizing, replacing the old flat `Timeline.groupWords` chunking) against the caption box's fixed width/height, finds the *page* active at `timelineTime` (not a flat word group), and renders one `.caption-block` div containing one `.caption-line` div per line, each line's words wrapped in a `line-height: 1` inline-block span; the paginated pages are memoized per `(words, box size, font)`. Highlight background (added 2026-07-25, reworked 2026-07-30 spotlight per-word styles): when `preset.highlight` is true, each line's inline-block wrapper — not the whole `.caption-block` — gets `background-color`/`border-radius` from `preset.highlight_color`/`highlight_border_radius` plus equal padding on all 4 sides (`HIGHLIGHT_PAD_EM * preset.size_px`), so the background hugs that line's actual text instead of spanning the caption's fixed box; independent of the per-word Spotlight rendering below. Per-word span rendering (reworked 2026-07-30, spotlight per-word styles, replacing the old `highlight_mode`-colored-text-only branch; highlight-rect gating fixed 2026-07-30, preview/export progressive_fill parity): two separate "is this word active" flags, not one — `isActive` (color swap) is cumulative in `"progressive_fill"` (`timelineTime >= word.t_start`, true for every word spoken so far, matching ASS's `\k` sweep semantics) but the current-word window (`timelineTime >= word.t_start && timelineTime < word.t_end`) in `"current_word"`; `isCurrentWord` (highlight-rect gating) is ALWAYS the narrow current-word window regardless of mode, since only one word is ever "being spoken right now" — using the cumulative flag there previously painted a highlight rect behind every already-spoken word in `"progressive_fill"`, unlike the export path's per-word-timed rect. The active word gets `preset.spotlight_color` text color whenever `highlight_mode !== "off"` (via `isActive`), `-webkit-text-stroke` from `spotlight_outline_color`/`spotlight_outline_px` and `text-shadow` from `spotlight_shadow*` only in `"current_word"` mode and only when `spotlight_outline_px > 0`/`spotlight_shadow` are set, and a `spotlight_highlight_color`/`spotlight_highlight_border_radius` background rect (via `isCurrentWord`) when `spotlight_highlight` is true — all independent toggles, reset to neutral on inactive words/other modes so switching modes or moving off a word never leaves stale styling. `static/preview.js` exposes `Preview.renderCaptions` as a thin delegating wrapper.
- `app/ass_render.py` — `render_caption_ass(project, preset)`: standalone ASS script — for each page, `_caption_highlight_dialogues(page, preset)` (added 2026-07-25, captions marker highlight; reworked 2026-07-30, spotlight per-word styles — moved from a single pre-loop call into the per-page loop) emits, when `preset.highlight` is true, one rounded-rect `Dialogue` **per visible line** — sized to that line's measured text width, not the fixed caption box — spanning the page's full first-word-start to last-word-end range, appended before that page's own text dialogue so it renders underneath; independent of `highlight_mode`. Then, when `preset.highlight_mode != "off"`, `_active_word_highlight_dialogues(page, preset)` (renamed from `_background_word_dialogues` 2026-07-30, spotlight per-word styles, and no longer mode-exclusive — gated on `preset.spotlight_highlight` instead of a "background" mode, so it now layers alongside `_current_word_dialogues` rather than replacing it). Then one `Caption` style + karaoke dialogue: `highlight_mode == "current_word"` emits per-word `_current_word_dialogues` (per-active-word `\1c` text-color override plus, when set, `\3c`/`\bord` outline and `\4c`/`\4a`/`\xshad`/`\yshad`/`\blur` shadow overrides from the `spotlight_outline*`/`spotlight_shadow*` fields — `spotlight_outline_px > 0` is the sole outline gate); anything else (including the legacy `"off"`/`"progressive_fill"` distinction — `"off"` skips per-word coloring entirely since `_caption_style`'s `primary`/`secondary` colors only special-case `"progressive_fill"`) falls through to `_karaoke_dialogue`'s native `\k` sweep, which can't carry per-word outline/shadow overrides (see `_current_word_dialogues`'s header). Paginates via `app.caption_layout.paginate_words` against the preset's box width/height (falling back to `CAPTION_DEFAULT_BOX_WIDTH = 900`/`CAPTION_DEFAULT_BOX_HEIGHT = 350` module constants for a preset that predates fixed-size captions), and `_karaoke_dialogue`/`_current_word_dialogues` take a `page: list[list[CaptionWord]]` (lines within a page), joining lines with ASS `\N`. The old flat `group_words(words, max_words)` helper was deleted 2026-07-24 (caption box sizing). Both `_caption_highlight_dialogues` and `_active_word_highlight_dialogues` size their rect(s) to a **tight** glyph height (`size_px`, not the looser `size_px*LINE_HEIGHT` line pitch) plus `HIGHLIGHT_PAD_EM * size_px` padding equal on all 4 sides, vertically centered within the line's pitch slot — sharing `_line_word_offsets`/`_line_left_origin` helpers for per-line word layout/alignment math; mirrored live in `static/preview-captions.js` via a `line-height: 1` inline-block wrapper with the same equal padding.
- `app/main.py` — `export_project` burns in karaoke captions when `p.captions.words` is non-empty (see Export pipeline below).
- `static/css/components/style-panel.css` — `#panel-captions`'s five tab panes Closed-caption/Auto/Style/Design/Box (Auto renamed from Filler 2026-07-31), switched by `UI.tabBar` (Design also holds the captions-only HIGHLIGHT group; Closed-caption holds the words list; Auto holds AUTO CAPTION plus the Auto-remove button + filler-words settings row, added 2026-07-23, reworked 2026-07-31).

### Transcript sidebar

Paragraph-style live-updating caption transcript beside the stage — auto-scrolling and highlighting the active sentence/word during playback, with click-to-seek. See `docs/superpowers/specs/2026-07-31-transcript-sidebar-design.md`.

- `static/transcript-sentences.js` — `window.TranscriptSentences.groupBySentence(words) -> [{start, end, words}]` (added 2026-07-31): pure sentence-grouping helper, splits a flat `CaptionWord[]` list into sentences by punctuation (./!/?), a trailing run with no terminal punctuation still forms a final group; consumed by transcript-sidebar.js.
- `static/transcript-sidebar.js` — `window.TranscriptSidebar.render(project)` (added 2026-07-31): renders `#transcript-sidebar` div (one `.transcript-sentence` div per sentence, one `.transcript-word` span per word), self-registers onto `Preview.onTimeUpdate` for live highlight/auto-scroll during playback (`.active` class on the current sentence, `.active-word` class on the current word), word click calls `Preview.seek(word.t_start)` for click-to-seek. Called from `preview.js`'s `load()` and `panel-captions.js`'s `renderCaptionPreview()`.
- `static/css/components/transcript-sidebar.css` (added 2026-07-31): `#transcript-sidebar` paragraph-list container + `.transcript-sentence` (neutral text, `.active` bright) + `.transcript-word` (neutral, `.active-word` gold accent) styling for live highlight + auto-scroll + click-to-seek.

### Unified overlay layer stack (z-order)

`z_index` fields still drive stacking (unchanged), but reordering used to happen in a dedicated side panel (`#panel-layers`) — as of 2026-07-24 that panel is gone, replaced by drag-to-reorder directly on the timeline's merged overlays row. Same underlying z_index-based stacking model, same "top = frontmost" convention, just relocated onto the timeline itself. CAPTIONS is not part of this stack (matches the old Layers panel's scope, which never included captions either) — it stays its own separate, fixed, non-draggable timeline row.

- `z_index` fields on `TextBlockLayer`, `CaptionTrack`, `VideoBoxLayer`, `ImageBoxLayer` (`app/models.py`; `ImageBoxLayer` added by the image/photo PiP feature) drive cross-layer stacking; `preview.js`/`video-box-preview.js`/`image-box-preview.js` set each `#overlay` child's CSS z-index from its model's `z_index`. As of 2026-07-31 (locked overlay layers), `TextBlockLayer`/`VideoBoxLayer`/`ImageBoxLayer` also carry a `locked: bool = False` field (not `CaptionTrack`, which stays outside the overlay stack) blocking only the timeline drag-reorder gesture below — stage selection/move/resize are unaffected.
- `static/timeline-overlay-layers.js` — `window.OverlayLayers.mergedEntries(project)` (merges text blocks, video boxes, and image boxes — the latter added by the image/photo PiP feature — into one list sorted by `z_index` descending) and `window.OverlayLayers.renumber(entries)` (reassigns `z_index` by array position) — pure helpers, no DOM.
- `static/timeline-overlay-layer-drag.js` — drag-to-reorder gesture: mousedown on a lane's hover-reveal grip handle (`.overlay-lane-handle`) + vertical drag past a threshold reorders that lane among all overlay lanes; on drop, calls `OverlayLayers.renumber` then saves and re-renders. As of 2026-07-31, a plain click (no drag movement) on a lane's handle toggles that entry's `locked` field instead of being a no-op, and a locked lane's mousedown never enters drag-follow state.
- `static/timeline-overlay-time-drag.js` (added 2026-07-31, overlay-lane-time-drag) — drag-to-reposition-in-time: mousedown on a lane's `.timeline-block` (not its `.timeline-resize-handle`) and horizontal drag past a 4px threshold shifts `start` (TEXT also shifts `end` by the same delta), clamped to `>= 0`, no snapping; a locked entry never enters drag-follow. A VIDEO BOX dropped inside `#row-video`'s bounds instead calls `stitchVideoBoxIntoSequence` directly (`static/clip-sequence.js`) — the same stitch-into-sequence behavior the removed native-DnD wiring provided.
- `static/timeline.js` — `renderOverlaysRow(project, px, selected, onSelect)` renders one 44px lane per text block, video box, or image box (via `OverlayLayers.mergedEntries`) into the merged `#row-overlays`/`#label-overlays` row, replacing the old separate `#row-text`/`#row-videobox` rows; each lane keeps its item's existing behavior (text block resize handle, video box drag-to-timeline-insert — image boxes are overlay-only and do not support drag-to-timeline-insert) plus a "TEXT"/"VIDEO BOX"/"IMAGE BOX" label and grip handle. As of 2026-07-31, each lane's handle renders `UI.icon("lock")` instead of `UI.icon("grip-vertical")` when `entry.item.locked`, and a new `ensureFixedRowLockIcons()` (called from `render()`, idempotent) prepends a static non-interactive lock icon to the MAIN/AUDIO row labels, since those rows were never part of the draggable overlay stack. As of 2026-07-31 (overlay-lane-time-drag), the VIDEO BOX branch no longer sets `draggable`/`dragstart` — every lane's block instead carries `dataset.blockId` (video boxes included), and `static/timeline-overlay-time-drag.js` drags any lane's block left/right to shift its `start` (TEXT also shifts `end`, preserving duration), folding in the old drag-to-stitch-onto-VIDEO-row gesture as a special case.
- Removed: `static/panel-layers.js`, `static/css/components/layers-panel.css`, the `#panel-layers` context-panel section, and the LAYERS entry in `#panel-nav` (`static/panel-nav.js`'s `PANEL_NAV_ITEMS`/`PANEL_NAV_HANDLERS`/`showPanel`) — fully superseded by the mechanism above.
- Export-side: `app/timeline.py`'s `banded_layers(project)` partitions text blocks, video boxes, and image boxes into the same z-order bands (`"text"`/`"video_box"`/`"image_box"`), consumed by `app/ffmpeg_cmd.py`'s banded-export filter chain — see "Video & image boxes" and "Export pipeline" below.

### Export pipeline

- `app/ffmpeg_cmd.py` — `build_export_cmd` (per-clip trim/scale/pad-or-crop, concat, optional ASS burn-in with `fontsdir=static/fonts`), `escape_filter_path`, `build_audio_cmd(project, wav_path)` (audio-only concat for transcription; **known gap:** still assumes every clip has audio — a video-only clip breaks transcription's audio export). Phase 6 (2026-07-20): synthesizes silent audio (`anullsrc`) for clips whose `MediaItem.has_audio` is `False`, tracked via a running `input_index` counter so interleaved silence inputs don't collide with real ones. Also accepts `caption_ass_path`: chains one more `ass` filter as the always-final stage after any text-block/video-box output, so captions always render on top. Also (2026-07-20): `-crf` is derived from `Project.export_quality` via `_crf_for()` (`"high"` -> 18, `"medium"` -> 23, default 18) at both mp4-output call sites; `-preset fast` is unchanged. Task 6 (2026-07-20): the per-clip scale segment branches on `ClipLayer.fill_mode` — `"fit"` keeps the existing `scale=...:force_original_aspect_ratio=decrease,pad=...` chain, `"fill"` uses `scale=...:force_original_aspect_ratio=increase,crop=W:H` (no pad); the shared trim/setpts/fps prefix/suffix is untouched, `build_audio_cmd` is unaffected (video-only concern).
- `app/ass_render.py` — `render_ass(project, presets)` (full ASS file), `ass_time`, `hex_to_ass`. See Text blocks and Captions above for block/caption-specific rendering detail.
- `app/font_metrics.py` — `pil_font_measurer(font_name, size_px, weight)`, `FONT_WEIGHT_PATHS`, `WEIGHT_LABELS`, `available_weights(font_name)` — measures the static per-weight `.ttf` files for export-path PIL measurement.
- `scripts/generate_font_weights.py` — one-off dev script baking those static per-weight `.ttf` files from the vendored variable fonts.
- `app/export_jobs.py` — in-memory job registry (`start_job`/`get_job`/`update_progress`) backing background exports; `start_job(run)` runs `run(on_progress) -> output_path` via the module's swappable `_executor` (a real daemon thread by default, an injectable synchronous call in tests).
- `app/media.py` — `run_export(cmd, on_progress=None, total_duration=0.0)`: runs ffmpeg via `subprocess.Popen`; when `on_progress`/`total_duration` are given, appends `-progress pipe:1 -nostats` and streams stdout through `percent_from_progress_line(line, total_duration) -> float | None` (pure), calling `on_progress` per parsed line. Stderr is captured via a temp file (not a second pipe) to avoid a pipe-buffer deadlock while stdout is being read concurrently; still raises `RuntimeError(stderr[-2000:])` on nonzero exit.
- `app/main.py` — `POST /api/projects/{id}/export` builds the ffmpeg command synchronously (ASS files, filename/CRF resolution unchanged, `total_duration = timeline.sequence_duration(timeline.ordered(p.clips))`) then hands it to `export_jobs.start_job`, returning `{job_id}` immediately instead of blocking until ffmpeg finishes. `GET /api/exports/{job_id}` reports `{status, percent, output_path, error}` (404 for an unknown id). Also `GET /api/fonts/{name}/weights`. `Project.export_filename`/`export_quality` (added 2026-07-20) drive the output filename/CRF: `sanitize_export_filename(name)` (strips filesystem-unsafe characters, trims dots/whitespace, drops a redundant `.mp4`) and `resolve_export_path(out_dir, stem)` (appends `-2`, `-3`, … on collision) are pure helpers in `app/main.py`; `export_project` uses `p.export_filename` when set, else falls back to the pre-existing `{name}-{id[:8]}` stem. The `.ass` sidecar file paths are unaffected (still `{name}-{id[:8]}`-derived).
- `static/api-export-project.js` — `Api.exportProject(projectId)`: `POST /api/projects/{id}/export` -> `{ok, job_id}` or error (starts a background job).
- `static/api-export-status.js` — `Api.exportStatus(jobId)`: `GET /api/exports/{job_id}` -> `{status, percent, output_path, error}`.
- `static/api-list-font-weights.js` — `Api.listFontWeights(fontName)`: `GET /api/fonts/{name}/weights`.
- `static/panel-export.js` — `ExportPanel.render()` (added 2026-07-20): the `#panel-export` context section's FILENAME (text input, placeholder = derived default `{name}-{id[:8]}`) and QUALITY (`UI.buttonGroup` HIGH/MEDIUM) rows, mutating `project.export_filename`/`export_quality` and calling `saveProject()`.
- `static/export-progress.js` — `ExportProgress.start(jobId, {onDone, onFailed})`: 500ms poller driving `#panel-export`'s progress bar (`#export-progress`/`#export-progress-fill`, `static/css/components/export-progress.css`); `static/editor.js`'s `exportProject()` disables `#export` during the job (starts it via `Api.exportProject`, then `ExportProgress.start`) and re-enables it on `onDone`/`onFailed`, matching the pre-change `#export-result` output on success/failure.
- `static/editor.js` — `openExportPanel()` opens `#panel-export` (calls `ExportPanel.render()`, plus the existing `#export`/`exportProject()`/`#export-result` wiring).
- `static/fonts/` — vendored variable woff2 + generated static per-weight `.ttf` files.

### Hosting, auth & deployment

Added 2026-07-23 for the "run this app on Android" project's piece 1 (cloud hosting + access gate) — see `docs/superpowers/specs/2026-07-23-cloud-hosting-auth-design.md`.

- `app/auth.py` — `create_session_token(secret) -> str`/`verify_session_token(token, secret) -> bool`: signs/verifies a stateless session cookie (itsdangerous `URLSafeTimedSerializer`, 30-day max age). No accounts or DB — one shared `APP_PASSWORD`, not per-user.
- `app/main.py` — `GET /login` serves `static/login.html`; `POST /login` (form-encoded `password`) compares against `APP_PASSWORD` via `hmac.compare_digest`, sets a signed session cookie and redirects to `/` on match, else redirects to `/login?error=1`. `AuthMiddleware` (Starlette `BaseHTTPMiddleware`) gates every other route: skipped entirely when `APP_PASSWORD` is unset; `/login`/`/static` paths always pass through; elsewhere a missing/invalid session cookie redirects to `/login` (or returns `401` for `/api/*` paths).
- `static/login.html`/`static/login.js`/`static/css/components/login.css` — standalone login page (not part of the `index.html` SPA): password field, posts to `/login`, `login.js` shows `#login-error` when redirected back with `?error=1`.
- `Dockerfile`/`.dockerignore` — `python:3.12-slim` + `ffmpeg`, base (non-`dev`/`ml`) dependencies only; Railway's GitHub-connected service builds from this on push.
- `docs/deploy-railway.md` — one-time Railway dashboard setup (volume, env vars) and the manual post-deploy verification checklist (login flow, volume persistence, `ffmpeg` on `PATH`).

### Settings & safe zones

- `static/editor.js` — `openSettingsPanel()` opens `#panel-settings`, currently holding only the `#theme-toggle` dark/light control. `setSafeZonesVisible(bool)` toggles `#safe-zones`'s `hidden` + `#safe-zones-toggle`'s `aria-pressed`, persists to `localStorage` (`safeZonesVisible`).
- `static/css/components/safe-zones.css` — `#safe-zones`: 4 guide bands (top nav / right action rail / caption area / bottom nav) matching TikTok's real UI chrome, preview-only.

### Shared UI components

Framework-free presentational helpers, one function per file, attached to `window.UI`. (Formerly grouped in a single `ui-components.js`, split per the one-function-per-file convention.)

**Bottom-pinned panel danger footer (added 2026-07-30, side-panel delete layout).** A CSS-only pattern, not a JS component — every side panel with a single destructive action (Delete/Remove) needed the same fix: the action used to sit wherever its tab's content happened to end, so a short tab (e.g. TEXT's Time tab) left it stranded high up with dead space below, and TEXT's Style tab hid it outright. Three classes in `static/css/components/style-panel.css` fix this everywhere at once: `.context-panel` (every `#panel-*` root) is a flex column with `min-height: 100%`, filling the panel; `.context-panel-body` (a panel's main content wrapper, excluding drill-down subpages that are later siblings — e.g. `#video-main`, `#panel-text-main` + `#text-accordions`, `#video-box-detail`, `#image-box-detail`, `#audio-detail`) is itself a flex column with `flex: 1` so it can grow to fill whatever space the panel has left, plus a `[hidden] { display: none }` override (same fix `sub-panel.css` needed for `.style-sub-panel`, since a bare class rule otherwise beats the browser's default hidden-attribute styling); `.panel-danger-footer` (on the `.style-group`/wrapper directly containing the danger button) is just `margin-top: auto`, which pushes it to the true bottom of its `.context-panel-body` when there's spare room, and falls back to sitting right after the content (today's old behavior) when the content already fills or exceeds the panel. Applied to `#video-delete` (VIDEO), `#video-box-delete` (VIDEO BOX), `#image-box-delete` (IMAGE BOX), `#audio-remove` (AUDIO), and `#text-delete` (TEXT, `#text-edit-actions`, plus `#text-accordions` itself needs `.context-panel-body` since the footer is nested one level deeper there). TEXT's Duplicate button (`duplicateTextBlock`) was removed the same day rather than migrated — see panel-text.js above — matching VIDEO's earlier clip-duplicate removal, so every panel this pattern covers now has exactly one bottom-pinned danger action and no duplicate action.

- `static/ui-button-group.js` — `UI.buttonGroup(container, options, activeValue, onSelect, {containerSpan})`: single-select toggle-button row. Each option may set `span` (default 1) for its own `.col-{span}` width; `containerSpan` (default 8) sets the container's `.col-{containerSpan}`.
- `static/ui-number-field.js` — `UI.numberField(container, {label, unit, value, step, min, max, decimals, disabled, span, onChange})`: labeled number input + custom stepper (native OS spin control is unstylable). `span` (default 8) applies `.col-{span}` to the container. Returns a `setValue(v)` updater.
- `static/ui-color-swatch.js` — `UI.colorSwatch(container, {label, value, onChange, showLabel, span})`: small square color-picker swatch with label. `span` (default `showLabel ? 8 : 1`) applies `.col-{span}` to the container; internally the swatch itself is `.col-1` and the label `.col-7`.
- `static/ui-accordion.js` — `UI.accordion(header, body, {expanded})`: wires an existing header/body pair.
- `static/ui-accordion-section.js` — `UI.accordionSection(container, body, {title, expanded})`: builds the header for `UI.accordion` — the base of every TEXT/CAPTIONS panel accordion.
- `static/ui-divider.js` — `UI.divider(container)`: plain static separator line.
- `static/ui-settings-row.js` — `UI.settingsRow(container, {label, value, valueFontFamily, swatchColor, onClick})`: clickable label/value/chevron row opening a drill-down; `swatchColor` (optional, added 2026-07-22) renders a small color square before the value text — used by the Outline row (color square + "Npx").
- `static/ui-sub-panel-header.js` — `UI.subPanelHeader(container, {title, onBack})`: back-arrow + title for drill-down sub-panels.
- `static/settings-row-value.js` — `window.SettingsRowValue.orNone(isOn, text)` (added 2026-07-29, settings-row None state): the single definition of the "None" label shown by a `UI.settingsRow` whose effect is switched off, shared by every Outline/Shadow/Highlight/Background/Border row across the TEXT and CAPTIONS panels.
- `static/ui-icon-rail.js` — `UI.iconRail(container, items, activeValue, onSelect)`: vertical icon+label nav rail, used for `#panel-nav`. The `badge: true` plus-overlay option (Task 2, 2026-07-21) was removed 2026-07-29 — no caller wanted it, and `.icon-rail-badge` went with it.
- `static/ui-tab-bar.js` — `UI.tabBar(container, tabs, activeValue, onSelect)`: horizontal row of icon tab buttons (Style/Design/Box/Time/Closed-caption), single-select, mirrors `UI.iconRail`'s API shape; replaces the per-panel `UI.accordionSection` stacks on TEXT/CAPTIONS/VIDEO/VIDEO BOX (see each panel's entry).
- `static/ui-button.js` — `UI.button(container, {label, icon, size, intent, pressed, disabled, onClick})`: rebuilt Task 14 (batch 3, replacing the old "stamp a variant onto an existing `<button>`" signature) — builds and appends a whole `<button>` element. `size`: `"sm"` (28px square, icon-only) or `"md"` (33px, full-width, label + optional leading icon). `intent`: `"neutral"` | `"accent"` (the app's one primary action, e.g. Export) | `"danger"` | `"dashed"`. Every button in the app is now built through this one function — either hydrated from a `[data-button]` placeholder in `index.html` (see its hydration `<script>`, Task 15) for markup-first sites, or called directly from JS for JS-created sites (timeline row-add button, number-field steppers, saved-style save/cancel, etc.). The old `.icon-btn`/`.panel-button*` CSS classes it replaced are retired (Tasks 15-17); `tests/js/no-legacy-button-classes.test.js` pins both the class retirement and the old `{variant}` call shape.
- `static/ui-list-row.js` — `UI.listRow(el, {selected, subtle})`: stamps the shared clickable-row `.list-row`/`.list-row--subtle`/`.selected` classes (list-row.css) onto an already-built element, mirroring `UI.button`'s "apply variant to existing element" pattern. Used by PROJECTS/FILES clip rows and the font-family/font-weight/saved-style/caption-word drill-down lists (added 2026-07-22, list-row component — also the fix for FILES' VIDEOS/IMAGES section-label rows previously inheriting card styling from the too-broad `#clip-list li` selector they've now retired).
- `static/ui-style-save-form.js` — `UI.styleSaveForm(container, {onSave, onCancel})`: inline "save current style" form for the TEXT/CAPTIONS Style tabs (name input + Save/Cancel buttons + overwrite hint), replacing the native `prompt()`. `onSave(name)` fires on Save click or Enter with a non-empty trimmed name; `onCancel()` fires on Cancel click or Escape.
- `static/ui-mask-line-drag.js` — `UI.maskLineDrag(overlay, {getRect, getMask, onChange, onChangeEnd})`: draggable straight-line guide for a box's edge mask (see "Box edge mask" above).
- `static/ui-icon.js` — `UI.icon(name, {size = 24})` (added UI-component-consistency initiative): returns an SVG markup string for a Lucide icon, keyed by kebab-case name in the file's own `ICON_PATHS` map (not a shared catch-all — every entry is spot-checked against its real call site). Throws on an unrecognized name. Replaces every hand-inlined `<svg>` in `static/*.js`/`static/index.html`; `tests/js/no-raw-svg.test.js` guards against new ones reappearing.
- `static/ui-text.js` — `UI.text(container, {variant, content})` (added UI-component-consistency initiative): builds a `<span>` with one of 4 shared typography recipes — `"eyebrow"` (mono-caps section label), `"label"` (form-field label), `"hint"` (secondary/help text), `"body"` (default content text) — via `text.css`'s `.text-*` classes. Throws on an unrecognized variant. Replaces the six independently-reinvented "eyebrow label" CSS recipes the design review flagged (`.style-group-label`, `.style-panel-header`, `.clip-section-row`, `.sub-panel-title`, `.settings-row-label`, `.accordion-header`).
- `static/ui-safe-zones.js` — `window.SAFE_ZONES` (array of 4 bands: `key`, `label`, `inset` as `{top, right, bottom, left, width, height}` percentages of the 1080x1920 canvas) + `UI.safeZones(container)` (added UI-component-consistency initiative): single source of truth for the safe-zone guide geometry. Renders one `<div class="safe-zone safe-zone-{key}">` per band (each holding a `.chip .chip--outlined .chip--safe-zone` label span) into `container`, and injects one generated `<style>` element (id `safe-zone-geometry-style`, idempotent) into `document.head` containing the per-band position/size CSS rules — no inline `style` attributes on the divs (final review finding 5). `static/safe-zone-geometry.js` derives its pixel constants from this same array.
- `static/ui-tooltip.js` — `UI.tooltip(el, {label, shortcut, delay})` (added 2026-07-31, tooltip design refresh): the app's one hover/focus tooltip, styled as a dark rounded chip (label text + an optional right-aligned monospace `.ui-tooltip-shortcut` badge), replacing the browser's plain native tooltip. Self-initializes via `UI.tooltip.observe(document.body)` at the bottom of the file — a `MutationObserver` watches the whole document for any element carrying (or gaining) a native `title` attribute, rebinds it onto this component (mirroring the text onto `aria-label`, then stripping `title` so nothing double-shows), and picks up a `data-tooltip-shortcut` attribute for the shortcut chip if present. No call site needed editing: every existing `el.title = "..."` assignment across `static/*.js` is upgraded automatically, present or future.
- `static/css/components/` — `button-group.css`, `number-field.css`, `color-swatch.css`, `accordion.css`, `divider.css`, `settings-row.css`, `sub-panel.css`, `icon-rail.css`, `tab-bar.css`, `button.css`, `resize-handles.css`, `list-row.css`, `style-preset-card.css` (added 2026-07-23, extends `.list-row` with the rendered-style preview + name for `UI.stylePresetCard`), `style-save-form.css` (added 2026-07-24, inline save-current-style form for the TEXT/CAPTIONS Style tabs), `chip.css` (added UI-component-consistency initiative: `.chip`+modifiers, backs `UI.safeZones`'s labels and `panel-auto-slice.js`'s badges), `text.css` (added UI-component-consistency initiative: `.text-*` variants, backs `UI.text`), `scroll-list.css` (added UI-component-consistency initiative: `.scroll-list` max-height+scroll utility), `tooltip.css` (added 2026-07-31, tooltip design refresh: `.ui-tooltip`/`.ui-tooltip-shortcut`, backs `UI.tooltip`) — one stylesheet per component above, same naming. `list-row.css` is linked first among component stylesheets in `static/index.html` so its consumers' own overrides win the cascade at equal specificity. `panel-button.css` (the old `.panel-button`/`.panel-button-danger`/`.panel-button-dashed` plain-CSS-class component) was deleted Task 17 (batch 3 close-out) — every former `.panel-button*` site now builds through `UI.button` (`button.css`).
- `static/css/components/style-panel.css` — `#style-panel` (right aside, 320px, hidden by default, padding `18px 20px`): close button + every `.context-panel` section listed under its owning feature above. Built on an 8-column grid foundation: `.style-group`/`.style-row` are `grid-template-columns: repeat(8, 28px)` with 8px gap, `.col-1`…`.col-8` utility classes size/position children, and a nested `.style-group > .style-row` spans the full grid (`grid-column: 1 / -1`) so its own children can place themselves. Matches the north-star mockup (`docs/superpowers/specs/assets/2026-07-10-design-foundation-mockup.html`); preset swatches (CLEAN/BOXED/POP/MINIMAL) from the mockup deliberately not built yet. `.context-panel`/`.context-panel-body`/`.panel-danger-footer` (added 2026-07-30, side-panel delete layout — see "Shared UI components" below) are the shared bottom-pinned-danger-button pattern every panel's Delete/Remove button now uses. `.context-panel-header`/`.context-panel-header-icon`/`.context-panel-header-label` (added 2026-07-30, context-panel-header feature) style `UI.contextPanelHeader`'s icon+label row — the layout lives directly on the base `.context-panel-header` class since every context panel uses it now (no separate modifier class).
- `static/css/tokens.css` — `:root` design tokens (colors, fonts, spacing, radius) + `@font-face`; every other stylesheet builds on this.
